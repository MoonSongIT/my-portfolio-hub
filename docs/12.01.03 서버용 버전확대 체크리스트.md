# 12.01.03 서버용 버전확대 체크리스트

> 작성일: 2026-05-27  
> 참조: [12.01.02 서버용 버전확대 구현계획.md](./12.01.02%20서버용%20버전확대%20구현계획.md)

---

## 진행 현황

| 묶음 | 상태 | 완료일 |
|------|------|--------|
| 묶음 A — Supabase 기반 | ✅ 완료 | 2026-05-27 |
| 묶음 B — DB & API | ⬜ 미시작 | — |
| 묶음 C — 동기화 엔진 | ⬜ 미시작 | — |
| 묶음 D — UI | ⬜ 미시작 | — |
| 묶음 E — PWA & 마감 | ⬜ 미시작 | — |

---

## 묶음 A — Supabase 기반 (Step 1~7)

### Step 1 — Supabase 프로젝트 생성 및 환경변수 설정

- [ ] Supabase 대시보드에서 신규 프로젝트 생성
- [ ] Project URL, Anon Key, Service Role Key 복사
- [ ] `.env` 파일에 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` 추가
- [ ] Vercel 대시보드에 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` 환경변수 등록
- [x] `.env.example` 업데이트 (실제 값 제외, 키명만 명시)
- [ ] **검증:** 앱 실행 시 콘솔에 `[Supabase] 환경변수 미설정` 경고 없음

---

### Step 2 — Supabase 클라이언트 초기화

- [x] `@supabase/supabase-js` 패키지 설치 (`npm install @supabase/supabase-js`)
- [x] `src/lib/supabaseClient.js` 파일 생성
- [x] 환경변수 미설정 시 `null` 반환 (로컬 전용 모드 폴백) 처리 확인
- [ ] **검증:** `import { supabase } from '@/lib/supabaseClient'` 정상 동작

---

### Step 3 — authService.js 작성

- [x] `src/services/authService.js` 파일 생성
- [x] `signUpWithEmail(email, password)` 구현
- [x] `signInWithEmail(email, password)` 구현
- [x] `signInWithGoogle()` 구현 (OAuth 리디렉션 URL 포함)
- [x] `signOut()` 구현
- [x] `getSession()` 구현
- [x] `onAuthStateChange(callback)` 구현
- [x] `supabase === null` 일 때 폴백 처리 (에러 방지)
- [ ] **검증:** 이메일 가입 후 `getSession()` 호출 시 세션 반환 확인

---

### Step 4 — authStore.js Supabase 통합

- [x] `isSupabaseUser: false` 필드 추가
- [x] `supabaseSession: null` 필드 추가
- [x] `setSupabaseSession(session)` 액션 구현
- [x] 기존 `login()` / `logout()` 액션 호환성 유지
- [x] `logout()` 에서 Supabase 로그아웃 연동 (`isSupabaseUser` 체크)
- [x] store version 3으로 업데이트 (persist version 충돌 방지)
- [ ] **검증:** 로컬 전용 사용자 기존 동작 영향 없음

---

### Step 5 — App.jsx 세션 리스너 등록

- [x] `useEffect`에 `authService.getSession()` 최초 세션 복구 추가
- [x] `authService.onAuthStateChange()` 구독 등록
- [x] 언마운트 시 `subscription.unsubscribe()` 클린업 확인
- [ ] **검증:** 다른 탭에서 로그아웃 → 현재 탭 자동 로그아웃 처리

---

### Step 6 — SupabaseLoginModal.jsx 작성

- [x] `src/components/auth/SupabaseLoginModal.jsx` 파일 생성
- [x] 이메일/비밀번호 로그인 폼 구현
- [x] 이메일 회원가입 폼 구현 (mode 전환)
- [x] Google 로그인 버튼 구현
- [x] 로딩 상태 및 에러 메시지 표시 구현
- [x] 로그인 성공 시 모달 닫힘 처리
- [ ] **검증:** 이메일 가입 → 로그인 → 모달 닫힘 → authStore `isSupabaseUser: true`

---

### Step 7 — OAuth 콜백 API 작성

- [x] `api/auth/callback.js` 파일 생성
- [ ] Supabase 대시보드 → Authentication → URL Configuration에 OAuth 콜백 URL 등록
- [ ] **검증:** Google 로그인 후 앱으로 정상 복귀

---

## 묶음 B — DB & API (Step 8~10)

### Step 8 — Supabase DB 스키마 생성

- [ ] Supabase SQL Editor에서 `transactions` 테이블 생성
- [ ] Supabase SQL Editor에서 `accounts` 테이블 생성
- [ ] Supabase SQL Editor에서 `cash_flows` 테이블 생성
- [ ] Supabase SQL Editor에서 `watchlist` 테이블 생성
- [ ] Supabase SQL Editor에서 `calendar_events` 테이블 생성
- [ ] Supabase SQL Editor에서 `user_sync_meta` 테이블 생성
- [ ] **모든 테이블 RLS 활성화** (`enable row level security`) — 보안 필수
- [ ] 각 테이블에 `"사용자 본인 데이터만 접근"` 정책 생성
- [ ] **검증:** Table Editor에서 다른 `user_id` 데이터 접근 시 차단 확인

---

### Step 9 — db.js v11 마이그레이션

- [ ] `db.version(11).stores({...})` 블록 추가
- [ ] `transactions`, `cashFlows`, `calendarEvents` 인덱스에 `syncVersion` 추가
- [ ] `.upgrade()` 콜백에서 기존 레코드에 `syncVersion: 0, syncedAt: null, deletedAt: null` 설정
- [ ] `bumpSyncVersion(record)` 유틸 함수 추가 (db.js 하단)
- [ ] **검증:** 기존 레코드 데이터 손실 없음, `db.verno` === 11 확인

---

### Step 10 — 동기화 API 작성 (Vercel Serverless)

- [ ] `api/sync/version.js` 파일 생성 (GET — 글로벌 버전 반환)
- [ ] `api/sync/upload.js` 파일 생성 (POST — 로컬→서버 업로드 + 충돌 감지)
- [ ] `api/sync/download.js` 파일 생성 (GET — 서버→로컬 다운로드, `since` 파라미터 지원)
- [ ] 모든 API에서 JWT 토큰 검증 (`Authorization: Bearer ...`)
- [ ] Service Role Key는 `process.env.SUPABASE_SERVICE_ROLE_KEY` 사용 (절대 `VITE_` 금지)
- [ ] `TABLE_MAP` 에서 `aiCredentials` 제외 확인 — **보안 필수**
- [ ] **검증:** Postman/curl로 각 API 엔드포인트 개별 테스트

---

## 묶음 C — 동기화 엔진 (Step 11~13)

### Step 11 — syncStore.js 작성

- [ ] `src/store/syncStore.js` 파일 생성
- [ ] `syncStatus` 상태 정의 (`'idle' | 'syncing' | 'conflict' | 'error' | 'synced'`)
- [ ] `pendingChanges`, `localVersion`, `serverVersion` 상태 정의
- [ ] `conflicts: []` 상태 정의
- [ ] `syncEnabled`, `autoSync`, `syncInterval` 설정 상태 정의
- [ ] `setSyncStatus`, `setLastSyncedAt`, `incrementPending`, `resetPending` 액션 구현
- [ ] `addConflict`, `clearConflicts` 액션 구현
- [ ] `persist` 미들웨어 적용 (`name: 'sync-storage'`)
- [ ] **검증:** React DevTools에서 상태 변경 확인

---

### Step 12 — syncService.js 작성

- [ ] `src/services/syncService.js` 파일 생성
- [ ] `SYNC_TABLES` 배열 정의 (aiCredentials 제외 — **보안 필수**)
- [ ] `getAuthToken()` 내부 헬퍼 구현
- [ ] `checkServerVersion()` 구현
- [ ] `uploadPending()` 구현 (미동기화 레코드 필터링 → 서버 업로드 → 충돌 처리)
- [ ] `downloadFromServer(since)` 구현 (서버→로컬 병합, 버전 비교 포함)
- [ ] `migrateLocalToServer()` 구현 (최초 이전용)
- [ ] 소프트 삭제 처리 — `deleted_at` 있는 레코드는 로컬 `db.delete()`
- [ ] **검증:** 로컬 레코드 추가 후 `uploadPending()` 호출 시 서버 반영 확인

---

### Step 13 — useSyncStatus.js 훅 작성

- [ ] `src/hooks/useSyncStatus.js` 파일 생성
- [ ] `syncStatus`, `pendingChanges`, `lastSyncedAt` 구독
- [ ] `triggerSync()` 콜백 구현 (isSupabaseUser 체크 포함)
- [ ] `window.addEventListener('online', ...)` 오프라인 복구 자동 동기화 구현
- [ ] 언마운트 시 이벤트 리스너 클린업
- [ ] `isOnline: navigator.onLine` 반환
- [ ] **검증:** 오프라인 → 온라인 전환 시 자동 동기화 트리거 확인

---

## 묶음 D — UI (Step 14~17)

### Step 14 — Header.jsx 동기화 상태 인디케이터

- [ ] `useSyncStatus` 훅 import 및 적용
- [ ] `SyncIcon` 컴포넌트 구현 (syncStatus별 아이콘 분기)
  - [ ] 오프라인: `CloudOff` (회색)
  - [ ] 동기화 중: `RefreshCw` (파란색, 회전 애니메이션)
  - [ ] 충돌: `AlertCircle` (노란색)
  - [ ] 오류: `AlertCircle` (빨간색)
  - [ ] 완료: `Cloud` (초록색)
  - [ ] 기본: `Cloud` (회색)
- [ ] 아이콘 클릭 시 `triggerSync()` 호출
- [ ] **검증:** syncStatus 변경 시 아이콘 실시간 변경 확인

---

### Step 15 — ConflictModal.jsx 작성

- [ ] `src/components/settings/ConflictModal.jsx` 파일 생성
- [ ] `conflicts.length === 0` 시 null 반환
- [ ] 서버 버전 / 로컬 버전 비교 표시
- [ ] 버전 차이 경고 메시지 표시
- [ ] `[서버 버전 유지]` 버튼 → `downloadFromServer()` 호출 후 `clearConflicts()`
- [ ] `[로컬로 덮어쓰기]` 버튼 → 로컬 syncVersion 증가 후 `uploadPending()` 호출 후 `clearConflicts()`
- [ ] 복수 충돌 시 개수 표시 ("N개 중 첫 번째")
- [ ] **검증:** 충돌 데이터 주입 시 모달 표시, 선택 후 정상 상태 복구

---

### Step 16 — SyncSettings.jsx 작성

- [ ] `src/components/settings/SyncSettings.jsx` 파일 생성
- [ ] 비로그인 상태 안내 메시지 표시
- [ ] 계정 이메일, 마지막 동기화 시각 표시
- [ ] `[지금 동기화]` 버튼 (pendingChanges 수 표시)
- [ ] `[서버에서 다운로드]` 버튼
- [ ] 자동 동기화 토글 스위치
- [ ] 동기화 주기 선택 (즉시 / 15분 / 1시간 / 수동)
- [ ] 위험 구역 — `[서버 데이터로 로컬 초기화]` + confirm 다이얼로그
- [ ] **검증:** 토글 상태 저장 후 새로고침 시 유지

---

### Step 17 — MigrateDataModal.jsx 작성 + Settings.jsx 탭 추가

- [ ] `src/components/auth/MigrateDataModal.jsx` 파일 생성
- [ ] `[서버로 이전하기]` 버튼 → `syncService.migrateLocalToServer()` 호출
- [ ] 이전 중 로딩 상태 표시
- [ ] 이전 완료 메시지 표시
- [ ] AI API 키 비이전 안내 문구 포함
- [ ] `[나중에 하기]` 버튼 (skipMigration 처리)
- [ ] `src/pages/Settings.jsx` 탭 배열에 `{ id: 'sync', label: '동기화' }` 추가
- [ ] 동기화 탭 활성 시 `<SyncSettings />` 렌더링
- [ ] **검증:** 최초 Supabase 로그인 시 모달 표시, 완료 후 재표시 안 됨

---

## 묶음 E — PWA & 마감 (Step 18~22)

### Step 18 — settingsStore.js 동기화 설정 추가

- [ ] `syncEnabled: false` 필드 추가
- [ ] `enableSync()`, `disableSync()` 액션 추가
- [ ] Supabase 로그인 성공 시 `enableSync()` 자동 호출 연동
- [ ] **검증:** 로그인 후 `settingsStore.syncEnabled === true`

---

### Step 19 — bumpSyncVersion 각 스토어에 적용

- [ ] `src/store/journalStore.js` — `addEntry`, `updateEntry`, `deleteEntry` 수정
- [ ] `src/store/portfolioStore.js` — 쓰기 작업에 `bumpSyncVersion` 적용
- [ ] `src/store/watchlistStore.js` — 쓰기 작업에 `bumpSyncVersion` 적용
- [ ] `src/store/accountStore.js` — 쓰기 작업에 `bumpSyncVersion` 적용
- [ ] `src/store/cashFlowStore.js` — 쓰기 작업에 `bumpSyncVersion` 적용
- [ ] `src/store/calendarStore.js` — 쓰기 작업에 `bumpSyncVersion` 적용
- [ ] 각 쓰기 액션에서 `useSyncStore.getState().incrementPending()` 호출
- [ ] **검증:** 레코드 저장 후 `syncVersion` 증가, `syncedAt: null` 확인

---

### Step 20 — 자동 동기화 인터벌 설정

- [ ] `src/App.jsx` — syncInterval 설정에 따라 `setInterval` 등록
- [ ] `'realtime'`, `'manual'` 선택 시 인터벌 미등록 처리
- [ ] `'15m'` → 15분 인터벌, `'1h'` → 1시간 인터벌
- [ ] 언마운트 시 `clearInterval` 클린업
- [ ] **검증:** 15m 설정 후 개발자 도구에서 15분 후 동기화 로그 확인

---

### Step 21 — 통합 테스트 시나리오 실행

- [ ] **시나리오 1 — 기본 동기화:** PC에서 매매일지 작성 → 모바일에서 동일 내용 확인
- [ ] **시나리오 2 — 오프라인 내성:** 오프라인에서 레코드 추가 → 온라인 복구 시 자동 동기화 확인
- [ ] **시나리오 3 — 충돌 해결:** PC/모바일 동일 레코드 동시 수정 → ConflictModal 표시 → 정상 복구
- [ ] **시나리오 4 — AI API 키 비동기화:** PC에서 키 저장 → 모바일에서 해당 키 없음 확인 (**보안 검증**)
- [ ] **시나리오 5 — 서버 초기화:** 로컬 삭제 → "서버에서 다운로드" → 전체 복구 확인
- [ ] **시나리오 6 — RLS 검증:** 다른 계정의 데이터 API 직접 호출 → 403 차단 확인

---

### Step 22 — 배포 설정 완료

- [ ] `package.json`에 `@supabase/supabase-js` 의존성 추가 확인
- [ ] `.env.example` 키명 전부 업데이트 확인
- [ ] Vercel 환경변수 3종 등록 확인
  - [ ] `VITE_SUPABASE_URL`
  - [ ] `VITE_SUPABASE_ANON_KEY`
  - [ ] `SUPABASE_SERVICE_ROLE_KEY` (서버 전용)
- [ ] Supabase Auth → URL Configuration → Site URL 등록
- [ ] Supabase Auth → URL Configuration → Redirect URLs에 OAuth 콜백 URL 등록
- [ ] `npm run build` 에러 없이 정상 빌드 확인
- [ ] Vercel Preview 배포 후 동기화 동작 확인

---

## 보안 최종 확인 (배포 전 필수)

- [ ] `SYNC_TABLES` 배열에 `aiCredentials` 없음 — 코드 검색으로 재확인
- [ ] `api/` 코드 내 `SUPABASE_SERVICE_ROLE_KEY` 사용 (ANON KEY 사용 금지)
- [ ] `src/` 클라이언트 코드에 `SUPABASE_SERVICE_ROLE_KEY` 없음 — 코드 검색으로 재확인
- [ ] 모든 Supabase 테이블 RLS 활성화 — Supabase Table Editor에서 육안 확인
- [ ] 다른 계정 데이터 접근 차단 동작 테스트 완료
- [ ] `.env` 파일 `.gitignore`에 포함 확인

---

## CLAUDE.md Phase 로드맵 업데이트 (완료 후)

```markdown
| Phase 12 | 서버용 버전 확대 (Supabase Auth + 동기화) | ✅ 완료 (YYYY-MM-DD) |
```
