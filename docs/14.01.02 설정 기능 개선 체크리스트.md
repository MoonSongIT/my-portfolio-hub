# 설정 기능 개선 체크리스트

작성일: 2026-06-04
수동 검증 완료: 2026-06-04
계획 문서: `14.01.01 설정 기능 개선 계획.md`

---

## A. API 키 패널 통합 (Must #1) ✅ 완료 (2026-06-04)

> AiKeyPanel, DartKeyPanel, FinnhubKeyPanel → 단일 ApiKeyPanel로 통합

- [x] `src/components/settings/ApiKeyPanel.jsx` 신규 생성
  - [x] 3개 서비스를 목록 형태로 표시 (서비스명 / 상태 / 용도 / 액션)
  - [x] 각 행에 "변경/설정" 버튼 클릭 시 인라인 입력 폼 확장 (accordion)
  - [x] 보안 안내 문구를 패널 하단에 한 번만 표시
  - [x] Anthropic 키는 "검증" 버튼 포함 (DART, Finnhub는 미포함)
  - [x] 삭제 확인 모달 패널 내부에 단일 구현
- [x] `Settings.jsx`에서 AiKeyPanel, DartKeyPanel, FinnhubKeyPanel import 제거
- [x] `Settings.jsx`에 ApiKeyPanel import 및 렌더링 추가
- [x] `src/components/settings/AiKeyPanel.jsx` 파일 삭제
- [x] `src/components/settings/DartKeyPanel.jsx` 파일 삭제
- [x] `src/components/settings/FinnhubKeyPanel.jsx` 파일 삭제
- [x] **수작업 검증 완료** (2026-06-04): 각 키 저장 / 검증(Anthropic) / 삭제 동작 확인

---

## B. 서버 계정 + 동기화 섹션 통합 (Must #2) ✅ 완료 (2026-06-04)

> "서버 계정" + "동기화" 두 섹션 → "연결 & 동기화" 단일 섹션으로 통합

- [x] `Settings.jsx`에서 기존 "서버 계정" 섹션과 "동기화" 섹션 제거
- [x] "연결 & 동기화" 단일 섹션 추가
  - [x] 미로그인 상태: 로그인 버튼만 표시 (동기화 설정 UI 숨김)
  - [x] 로그인 상태: 연결 정보(연결됨 표시) + 동기화 설정 UI 표시
  - [x] 로컬 데이터 이전 버튼은 로그인 상태에서만 표시 (기존과 동일)
  - [x] 연결 해제 버튼 유지
- [x] `SyncSettings.jsx`의 "미로그인 시 안내 텍스트" 조건 분기 제거 (부모에서 제어)
- [x] **수작업 검증 완료** (2026-06-04)
  - [x] 미로그인: 동기화 UI가 보이지 않는지 확인
  - [x] 로그인 후: 동기화 UI 펼쳐지는지 확인

---

## C. 데이터 관리 + 데이터 정리 섹션 통합 (Must #3) ✅ 완료 (2026-06-04)

> "데이터 관리" (9번) + "데이터 정리" (10번) → "데이터 관리" 단일 섹션으로 통합

- [x] `Settings.jsx`에서 두 섹션을 하나의 `<section>` 블록으로 병합
  - 순서: 백업&복원 → HTS 가져오기 → 저장소 현황 → 전체 초기화
- [x] 섹션 제목을 "데이터 관리"로 통일
- [x] 앱 정보를 이 섹션 하단에 이어서 배치 (별도 섹션 제거)
- [x] **수작업 검증 완료** (2026-06-04): 백업/복원/초기화 각 동작 확인

---

## D. 종목 DB 단순화 (Must #4) ✅ 완료 (2026-06-04)

> 일반 사용자에게 "원본 직접 수집" 관련 버튼 숨김 처리

- [x] `StockMasterPanel.jsx`에서 "데이터 출처" 선택 UI 제거 (origin / server 토글 버튼 삭제)
- [x] 일반 사용자 화면 구성:
  - [x] 서버 종목 수 + 마지막 업데이트 일시 표시
  - [x] "서버에서 받기" 버튼 하나만 표시
  - [x] 진행바 및 취소 버튼 유지
  - [x] 커스텀 종목 폼 유지
- [x] 관리자 전용 영역 (`isAdmin && isSupabaseUser`) 유지:
  - [x] 원본 직접 수집 버튼 (전체/증분/국내/해외)
  - [x] 서버로 업로드 버튼
  - [x] 거래소 카드 그리드 (국내/해외)
  - [x] "개발 서버 실행 중에만 업데이트 가능" 경고 텍스트
- [x] `preferredSource` 관련 로직은 관리자 UI에서만 사용하도록 범위 축소
- [x] **수작업 검증 완료** (2026-06-04)
  - [x] 일반 사용자: 서버 다운로드 버튼만 보이는지 확인
  - [x] 관리자: 원본 수집 버튼과 거래소 그리드가 보이는지 확인

---

## E. 버전 하드코딩 수정 (Must #5) ✅ 완료 (2026-06-04)

> `"1.1.0 (Phase 11)"` → package.json에서 읽은 실제 버전으로 교체

- [x] `vite.config.js`에 `define` 항목 추가하여 `__APP_VERSION__` 주입
- [x] `Settings.jsx` 앱 정보 섹션에서 버전 표기를 `__APP_VERSION__`으로 수정
- [x] `"Phase 11"` 텍스트 제거
- [x] **수작업 검증 완료** (2026-06-04): 설정 화면 → 앱 정보 → 버전 `0.1.0` 표시 확인

---

## G. 알림 비활성화 시 하위 옵션 conditional hide (Must #6) ✅ 완료 (2026-06-04)

> A~E 구현 중 Settings.jsx 수정 시 함께 적용

- [x] `calendarNotification.enabled === false`이면 알림 시점/중요도 필터 행 숨김
- [x] "브라우저가 닫혀 있으면 발송되지 않습니다" → 노란색 경고 박스로 격상
- [x] **수작업 검증 완료** (2026-06-04): 알림 끄면 시점/필터 영역이 사라지는지 확인

---

## 추가 수정 사항 (세션 중 발견 및 수정)

- [x] `api/claude/validate` 404 수정 → `vercel.json` rewrite + `api/claude.js` 통합 (2026-06-04)
- [x] Vercel Hobby 12함수 한도 초과 → 13개 → 9개 통합 (2026-06-04)
- [x] DART 키 발급 링크 URL 수정 (`easyLogin.do` → `intro/main.do`) (2026-06-04)
- [x] 일반 사용자에게 "관리자 기능" 문구 노출 수정 (isAdmin 조건부) (2026-06-04)
- [x] `checkIsAdmin` `.single()` → `.maybeSingle()` — 비관리자 406 에러 제거 (2026-06-04)

---

## F. 공용 ConfirmModal 컴포넌트 추출 (Should #9) — 보류

- [ ] `src/components/common/ConfirmModal.jsx` 신규 생성
- [ ] `Settings.jsx`의 백업 복원 confirm 모달 → ConfirmModal 교체
- [ ] `Settings.jsx`의 전체 초기화 confirm 모달 → ConfirmModal 교체
- [ ] `ApiKeyPanel.jsx` (새 통합 패널)에서 ConfirmModal 사용

---

## H. 관심종목 기본값 — onBlur 즉시 저장 전환 (Should #7) — 보류

- [ ] `wlDraft` state 및 `isWlDirty` 계산 제거
- [ ] 각 input의 `onBlur` 시 `setWatchlistDefaults()` 직접 호출
- [ ] 저장 버튼 / 미저장 배지 / 경고 텍스트 제거

---

## 빌드 검증

- [x] `npm run build` 에러 없음 확인 (2026-06-04, 12.22s)
