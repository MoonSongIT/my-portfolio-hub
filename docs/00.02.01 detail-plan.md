# Phase 1 상세 구현 계획 — 프로젝트 초기화

> **기간**: 1-2일
> **목표**: Vite 6 + React 18 + Tailwind CSS + shadcn/ui 기반 프로젝트 틀 완성
> **산출물**: 구동 가능한 개발 환경 + 기본 라우팅 + 컴포넌트 프레임워크

---

## 📋 Phase 1 개요

### 달성 목표
- ✅ Vite 6 프로젝트 초기화 (SWC 번들러 포함)
- ✅ React 18 + React Router v6 라우팅 구조
- ✅ Tailwind CSS + shadcn/ui 컴포넌트 시스템
- ✅ Zustand 상태관리 초기화
- ✅ 폴더 구조 및 파일 레이아웃 완성
- ✅ 환경변수 설정 (.env, .env.example)
- ✅ GitHub (MoonSongIT) 저장소 연동
- ✅ npm run dev 실행 확인

### 최종 결과물
```
my-portfolio-hub/
├── node_modules/
├── public/
├── src/
│   ├── main.jsx
│   ├── App.jsx
│   ├── index.css
│   ├── agents/
│   ├── api/
│   ├── components/
│   ├── pages/
│   ├── store/
│   └── utils/
├── .env
├── .env.example
├── .gitignore
├── package.json
├── vite.config.js
├── tailwind.config.js
├── postcss.config.js
├── index.html
└── CLAUDE.md
```

---

## 🚀 단계별 구현 계획

### Step 1: Vite 6 + React 18 프로젝트 생성 (5분)

**작업 내용**
Vite 최신 템플릿으로 프로젝트 초기화

**실행 명령어**
```bash
# 현재 경로: C:\Users\dohay\ClaudeWork\my-portfolio-hub
# 기존 프로젝트 폴더가 있으므로 수동으로 package.json 생성

# 1. package.json 생성 (수동 작성 또는 아래 npm init)
npm init -y

# 2. Vite + React 필수 의존성 설치
npm install vite@latest @vitejs/plugin-react-swc@latest react@18.3.1 react-dom@18.3.1

# 3. 개발 의존성 설치
npm install -D vite@latest postcss autoprefixer

# 4. package.json 스크립트 추가
# (수동으로 scripts 섹션에 추가)
```

**생성할 파일: package.json**
```json
{
  "name": "my-portfolio-hub",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext .jsx --fix"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.20.0",
    "zustand": "^5.0.0",
    "immer": "^10.0.0",
    "@tanstack/react-query": "^5.25.0",
    "recharts": "^2.10.3",
    "axios": "^1.6.0",
    "lucide-react": "^0.428.0",
    "date-fns": "^4.0.0",
    "@radix-ui/react-dialog": "^1.1.1",
    "@radix-ui/react-select": "^2.0.0",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.0.0",
    "tailwind-merge": "^2.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react-swc": "^4.2.0",
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.31",
    "autoprefixer": "^10.4.16",
    "eslint": "^8.54.0",
    "eslint-plugin-react": "^7.33.0"
  }
}
```

**주의사항**
- Vite 6.x의 최신 버전 사용
- SWC 번들러로 빌드 속도 최적화
- React 18.3.1 이상 필수

---

### Step 2: Vite 설정 파일 생성 (3분)

**생성할 파일: vite.config.js**
```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react-swc'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    open: true, // 자동으로 브라우저 열기
    proxy: {
      // Phase 3에서 추가: Yahoo Finance, KRX API 프록시
      '/api/yahoo': {
        target: 'https://query1.finance.yahoo.com',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api\/yahoo/, ''),
      },
    },
  },
  build: {
    outDir: 'dist',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          'recharts': ['recharts'],
          'query': ['@tanstack/react-query'],
        },
      },
    },
  },
})
```

**설명**
- port 5173: Vite 기본 포트
- `open: true`: 개발 시작 시 브라우저 자동 열기
- `manualChunks`: 주가 데이터, 차트 라이브러리 별도 청크로 분리 (성능 최적화)

---

### Step 3: Tailwind CSS 설정 (5분)

**3-1. Tailwind 의존성 설치**
```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

**생성되는 파일: tailwind.config.js**
```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx,ts,tsx}",
    "./node_modules/@radix-ui/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      colors: {
        primary: '#1e40af',    // 파란색 (금융 앱 표준)
        success: '#10b981',    // 초록색 (수익)
        danger: '#ef4444',     // 빨강색 (손실)
        warning: '#f59e0b',    // 주황색 (주의)
      },
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
      },
    },
  },
  plugins: [],
}
```

**생성되는 파일: postcss.config.js** (자동 생성)
```javascript
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

**3-2. CSS 진입점 생성: src/index.css**
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* 프로젝트 커스텀 스타일 */
@layer components {
  .card {
    @apply rounded-lg border border-gray-200 bg-white shadow-sm;
  }

  .btn-primary {
    @apply px-4 py-2 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition;
  }

  .input {
    @apply px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500;
  }
}
```

---

### Step 4: shadcn/ui 초기화 (10분)

**4-1. shadcn/ui CLI 설치 및 초기화**
```bash
# shadcn/ui 초기화 (대화형)
npx shadcn@latest init
```

**초기화 답변 예시**
```
Would you like to use TypeScript? → No (선택: JavaScript 사용)
Which style would you like to use? → Default (기본)
Which color would you like as primary color? → Blue
Where is your global CSS file? → src/index.css
Do you want to use CSS variables for colors? → Yes
Are you using a CSS framework like Tailwind CSS? → Yes
Where is your tailwind.config.js located? → ./tailwind.config.js
Configure the import alias for components? → @/components
Configure the import alias for utils? → @/lib/utils
```

**4-2. 필수 shadcn/ui 컴포넌트 설치**
```bash
npx shadcn@latest add table
npx shadcn@latest add card
npx shadcn@latest add dialog
npx shadcn@latest add tabs
npx shadcn@latest add select
npx shadcn@latest add command
npx shadcn@latest add tooltip
npx shadcn@latest add sheet
npx shadcn@latest add button
npx shadcn@latest add input
```

**생성되는 폴더 구조**
```
src/
├── components/
│   ├── ui/
│   │   ├── button.jsx
│   │   ├── card.jsx
│   │   ├── dialog.jsx
│   │   ├── table.jsx
│   │   ├── tabs.jsx
│   │   └── ... (기타 shadcn/ui 컴포넌트)
│   └── (프로젝트 커스텀 컴포넌트)
└── lib/
    └── utils.js
```

---

### Step 5: 폴더 구조 및 기본 파일 생성 (10분)

**5-1. 폴더 구조 생성**
```bash
# src 폴더 내 필수 폴더 생성
mkdir -p src/{agents,api,components/common,components/portfolio,components/charts,components/chat,pages,store,utils}
```

**5-2. 기본 파일 생성**

**src/main.jsx**
```javascript
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

**index.html** (기존 파일 수정)
```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Portfolio Hub</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

**src/App.jsx** (기본 라우팅 구조)
```javascript
import { BrowserRouter, Routes, Route } from 'react-router-dom'
import Dashboard from './pages/Dashboard'
import Portfolio from './pages/Portfolio'
import Research from './pages/Research'
import Watchlist from './pages/Watchlist'
import Reports from './pages/Reports'
import Header from './components/common/Header'
import Sidebar from './components/common/Sidebar'

function App() {
  return (
    <BrowserRouter>
      <div className="flex h-screen bg-gray-50">
        <Sidebar />
        <div className="flex-1 flex flex-col overflow-hidden">
          <Header />
          <main className="flex-1 overflow-y-auto">
            <Routes>
              <Route path="/" element={<Dashboard />} />
              <Route path="/portfolio" element={<Portfolio />} />
              <Route path="/research" element={<Research />} />
              <Route path="/watchlist" element={<Watchlist />} />
              <Route path="/reports" element={<Reports />} />
            </Routes>
          </main>
        </div>
      </div>
    </BrowserRouter>
  )
}

export default App
```

---

### Step 6: 필수 컴포넌트 스켈레톤 생성 (15분)

**src/components/common/Header.jsx**
```javascript
export default function Header() {
  return (
    <header className="bg-white border-b border-gray-200 px-6 py-4">
      <h1 className="text-2xl font-bold text-gray-900">My Portfolio Hub</h1>
    </header>
  )
}
```

**src/components/common/Sidebar.jsx**
```javascript
import { Link } from 'react-router-dom'
import { Home, Briefcase, Search, Eye, BarChart3 } from 'lucide-react'

export default function Sidebar() {
  const menu = [
    { label: '대시보드', path: '/', icon: Home },
    { label: '포트폴리오', path: '/portfolio', icon: Briefcase },
    { label: '종목 탐색', path: '/research', icon: Search },
    { label: '관심종목', path: '/watchlist', icon: Eye },
    { label: '리포트', path: '/reports', icon: BarChart3 },
  ]

  return (
    <aside className="w-64 bg-gray-900 text-white p-6">
      <nav className="space-y-4">
        {menu.map((item) => {
          const Icon = item.icon
          return (
            <Link
              key={item.path}
              to={item.path}
              className="flex items-center gap-3 px-4 py-2 rounded-lg hover:bg-gray-800 transition"
            >
              <Icon size={20} />
              <span>{item.label}</span>
            </Link>
          )
        })}
      </nav>
    </aside>
  )
}
```

**src/components/common/LoadingSpinner.jsx**
```javascript
export default function LoadingSpinner() {
  return (
    <div className="flex items-center justify-center p-8">
      <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600"></div>
    </div>
  )
}
```

**페이지 파일들 (스켈레톤)**
```javascript
// src/pages/Dashboard.jsx, Portfolio.jsx, Research.jsx, Watchlist.jsx, Reports.jsx
export default function PageName() {
  return (
    <div className="p-6">
      <h2 className="text-xl font-bold text-gray-900">Page Title</h2>
      {/* 내용은 Phase 2에서 추가 */}
    </div>
  )
}
```

---

### Step 7: Zustand 상태관리 초기화 (5분)

**src/store/portfolioStore.js**
```javascript
import { create } from 'zustand'
import { persist } from 'zustand/middleware'
import { immer } from 'zustand/middleware/immer'

export const usePortfolioStore = create(
  persist(
    immer((set) => ({
      holdings: [
        // Phase 2에서 샘플 데이터 추가
      ],
      cash: 10000000,
      currency: 'KRW',

      // 액션 메서드
      addHolding: (stock) => set((state) => {
        state.holdings.push(stock)
      }),

      updatePrice: (ticker, price) => set((state) => {
        const holding = state.holdings.find(h => h.ticker === ticker)
        if (holding) {
          holding.current_price = price
        }
      }),

      removeHolding: (ticker) => set((state) => {
        state.holdings = state.holdings.filter(h => h.ticker !== ticker)
      }),
    })),
    { name: 'portfolio-storage' }
  )
)
```

**src/store/watchlistStore.js**
```javascript
import { create } from 'zustand'
import { persist } from 'zustand/middleware'

export const useWatchlistStore = create(
  persist((set) => ({
    watchlist: [],

    addToWatchlist: (ticker) => set((state) => ({
      watchlist: [...state.watchlist, { ticker, addedAt: new Date() }]
    })),

    removeFromWatchlist: (ticker) => set((state) => ({
      watchlist: state.watchlist.filter(item => item.ticker !== ticker)
    })),
  })),
  { name: 'watchlist-storage' }
)
```

**src/store/settingsStore.js**
```javascript
import { create } from 'zustand'
import { persist } from 'zustand/middleware'

export const useSettingsStore = create(
  persist((set) => ({
    theme: 'light',
    language: 'ko',

    setTheme: (theme) => set({ theme }),
    setLanguage: (language) => set({ language }),
  })),
  { name: 'settings-storage' }
)
```

---

### Step 8: 환경변수 설정 (3분)

**생성할 파일: .env**
```env
# Anthropic Claude API (서버사이드 프록시용)
ANTHROPIC_API_KEY=sk-ant-여기에_실제_API_키_입력

# 프론트엔드 API URL
VITE_API_BASE_URL=http://localhost:5173/api

# 주가 데이터 API (선택사항)
VITE_ALPHA_VANTAGE_KEY=여기에_키_입력
VITE_KRX_API_KEY=여기에_KRX_키_입력

# 한국투자증권 OpenAPI (Phase 3~)
KIS_APP_KEY=여기에_앱키_입력
KIS_APP_SECRET=여기에_앱시크릿_입력
KIS_ACCOUNT_NO=여기에_계좌번호_입력
```

**생성할 파일: .env.example** (실제 키값 없음)
```env
# Anthropic Claude API
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxx

# 프론트엔드 API URL
VITE_API_BASE_URL=http://localhost:5173/api

# 주가 데이터 API
VITE_ALPHA_VANTAGE_KEY=xxxxxxxxxxxxxxxx
VITE_KRX_API_KEY=xxxxxxxxxxxxxxxx

# 한국투자증권 OpenAPI (Phase 3~)
KIS_APP_KEY=xxxxxxxxxxxxxxxx
KIS_APP_SECRET=xxxxxxxxxxxxxxxx
KIS_ACCOUNT_NO=xxxxxxxxxxxxxxxx
```

**생성할 파일: .gitignore**
```
# 의존성
node_modules/
dist/
build/

# 환경변수
.env
.env.local
.env.*.local

# 개발 도구
.DS_Store
*.swp
*.swo
.idea/
.vscode/
*.code-workspace

# 로그
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 빌드 결과
coverage/
.parcel-cache/
.cache/
```

---

### Step 9: 유틸리티 함수 작성 (10분)

**src/utils/formatters.js**
```javascript
// 숫자 포맷팅
export const formatCurrency = (value, currency = 'KRW') => {
  if (currency === 'KRW') {
    return new Intl.NumberFormat('ko-KR', {
      style: 'currency',
      currency: 'KRW',
      minimumFractionDigits: 0,
    }).format(value)
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: 'USD',
    minimumFractionDigits: 2,
  }).format(value)
}

// 수익률 포맷팅
export const formatPercent = (value, decimals = 2) => {
  const formatted = value.toFixed(decimals)
  const sign = value > 0 ? '+' : ''
  return `${sign}${formatted}%`
}

// 날짜 포맷팅
export const formatDate = (date) => {
  return new Intl.DateTimeFormat('ko-KR', {
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
  }).format(new Date(date))
}
```

**src/utils/calculator.js**
```javascript
// 수익률 계산
export const calculateReturn = (buyPrice, currentPrice) => {
  return ((currentPrice - buyPrice) / buyPrice) * 100
}

// 포트폴리오 총 평가액
export const calculateTotalValue = (holdings, cash) => {
  const stockValue = holdings.reduce((sum, h) => sum + (h.quantity * h.current_price), 0)
  return stockValue + cash
}

// 포트폴리오 수익률
export const calculatePortfolioReturn = (holdings, initialInvestment) => {
  const currentValue = holdings.reduce((sum, h) => sum + (h.quantity * h.current_price), 0)
  return ((currentValue - initialInvestment) / initialInvestment) * 100
}
```

**src/utils/storage.js**
```javascript
// LocalStorage 헬퍼
export const storage = {
  get: (key) => {
    try {
      return JSON.parse(localStorage.getItem(key))
    } catch {
      return null
    }
  },

  set: (key, value) => {
    localStorage.setItem(key, JSON.stringify(value))
  },

  remove: (key) => {
    localStorage.removeItem(key)
  },

  clear: () => {
    localStorage.clear()
  },
}
```

---

### Step 10: GitHub 저장소 초기화 및 push (5분)

**10-1. Git 저장소 초기화**
```bash
# 로컬 git 초기화
git init
git add .
git commit -m "Initial commit: Vite 6 + React 18 + Tailwind + shadcn/ui setup"
```

**10-2. GitHub (MoonSongIT) 저장소와 연동**
```bash
# MoonSongIT에 원격 저장소 생성 후 실행
git remote add origin https://github.com/MoonSongIT/my-portfolio-hub.git
git branch -M main
git push -u origin main
```

**초기 커밋 메시지**
```
Initial commit: Phase 1 프로젝트 초기화 완료

- Vite 6 + React 18 기본 설정
- Tailwind CSS + shadcn/ui 컴포넌트 시스템
- React Router v6 기본 라우팅
- Zustand 상태관리 초기화
- 폴더 구조 및 기본 파일 생성
- 환경변수 설정 (.env, .env.example)
- 유틸리티 함수 작성
```

---

### Step 11: 프로젝트 실행 및 검증 (3분)

```bash
# 의존성 설치 (한 번만)
npm install

# 개발 서버 실행
npm run dev

# 브라우저에서 http://localhost:5173 접속
# 확인사항:
# - 페이지 정상 로드
# - 네비게이션 메뉴 작동
# - 콘솔 에러 없음
```

---

## 📝 예상 파일 목록 (Phase 1 완료 후)

```
my-portfolio-hub/
├── .git/
├── .github/
├── node_modules/
├── public/
├── src/
│   ├── agents/
│   │   ├── orchestrator.js (빈 파일)
│   │   ├── researchAgent.js (빈 파일)
│   │   ├── portfolioAgent.js (빈 파일)
│   │   ├── alertAgent.js (빈 파일)
│   │   └── reportAgent.js (빈 파일)
│   ├── api/
│   │   ├── claudeApi.js (빈 파일)
│   │   └── stockApi.js (빈 파일)
│   ├── components/
│   │   ├── common/
│   │   │   ├── Header.jsx ✅
│   │   │   ├── Sidebar.jsx ✅
│   │   │   └── LoadingSpinner.jsx ✅
│   │   ├── portfolio/
│   │   │   ├── PortfolioTable.jsx (빈 파일)
│   │   │   ├── StockCard.jsx (빈 파일)
│   │   │   └── AddStockModal.jsx (빈 파일)
│   │   ├── charts/
│   │   │   ├── ProfitLineChart.jsx (빈 파일)
│   │   │   └── AllocationPieChart.jsx (빈 파일)
│   │   ├── chat/
│   │   │   ├── ChatPanel.jsx (빈 파일)
│   │   │   └── MessageBubble.jsx (빈 파일)
│   │   └── ui/ (shadcn/ui 컴포넌트) ✅
│   ├── pages/
│   │   ├── Dashboard.jsx ✅
│   │   ├── Portfolio.jsx ✅
│   │   ├── Research.jsx ✅
│   │   ├── Watchlist.jsx ✅
│   │   └── Reports.jsx ✅
│   ├── store/
│   │   ├── portfolioStore.js ✅
│   │   ├── watchlistStore.js ✅
│   │   └── settingsStore.js ✅
│   ├── utils/
│   │   ├── formatters.js ✅
│   │   ├── calculator.js ✅
│   │   ├── storage.js ✅
│   │   └── lib/utils.js (shadcn/ui) ✅
│   ├── main.jsx ✅
│   ├── App.jsx ✅
│   └── index.css ✅
├── .env ✅
├── .env.example ✅
├── .gitignore ✅
├── index.html ✅
├── vite.config.js ✅
├── tailwind.config.js ✅
├── postcss.config.js ✅
├── package.json ✅
├── package-lock.json ✅
├── CLAUDE.md ✅
└── docs/
    ├── ResearchForProject.md ✅
    ├── master-plan.md (기존)
    ├── master-check.md (기존)
    ├── 00.02.01_detail-plan.md ✅ (현재)
    └── 00.03.01_detail-check.md (다음)
```

✅ = 완성, 빈 파일 = Phase 2 이후 구현

---

## ⚠️ 주의사항

1. **Node.js 버전**: v18.0.0 이상 필수
2. **npm 버전**: v9.0.0 이상 추천
3. **Tailwind CSS**: shadcn/ui 초기화 후 자동으로 설정되지만, `tailwind.config.js`의 `content` 배열 확인
4. **shadcn/ui**: 복사-붙여넣기 방식이므로 컴포넌트는 프로젝트 소유, 독립적 커스터마이징 가능
5. **환경변수**: `.env` 파일은 git에 업로드하지 않음 (`.gitignore`에 추가됨)

---

## ✅ Phase 1 완료 기준

- [ ] `npm run dev` 실행 시 브라우저에서 페이지 로드됨
- [ ] 네비게이션 메뉴가 모든 페이지로 이동 가능
- [ ] 콘솔에 에러 메시지 없음
- [ ] Tailwind CSS 스타일이 적용됨 (회색 배경, 폰트 등)
- [ ] shadcn/ui 컴포넌트 임포트 가능
- [ ] Zustand store에서 상태 조회 가능 (localStorage 저장 확인)
- [ ] GitHub 저장소에 커밋 및 push 완료

---

*작성: Claude Code | 날짜: 2026-04-01*
*다음 단계: Phase 2 (샘플 데이터로 포트폴리오 UI 구현)*
