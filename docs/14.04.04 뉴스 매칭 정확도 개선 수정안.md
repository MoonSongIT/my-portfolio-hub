# 14.04.04 뉴스 매칭 정확도 개선 수정안

## 작성일
2026-06-16

## 배경
Phase 2·3 라이브 검증 중, AI 분석이 종목과 **무관한 뉴스**를 받는 현상이 반복 확인되었다.

- 사례: "SFA반도체(036540, KOSDAQ) 왜 움직였어?" 질의 시, 프리패치된 뉴스 5건이 모두
  **EU-우크라이나 협상 / 미군 폭격기 추락 / 미국 투자사 인사 / GM-록히드마틴 계약 / 우크라이나 모병** —
  반도체·SFA반도체와 전혀 무관한 글로벌 기사였다.
- 결과: ① 감성 점수가 전부 중립(0)으로 산출되고, ② 원인 분석이 종목을 직접 설명하지 못하고 시장 전반으로 우회했다.
- 즉 감성 엔진(P3)·분석(P5)은 정상이며, **병목은 입력 뉴스의 종목 매칭 정확도**다.

## 진단 (코드 추적)

`src/api/newsApi.js`의 `fetchNews(ticker, market)`:

```js
// L106~118 (현재)
const isKorean = market === 'KRX' || market === 'KOSDAQ'
const pureTicker = ticker.replace(/\.(KS|KQ)$/, '')
if (isKorean) {
  const [naverNews, yahooNews] = await Promise.all([
    fetchNewsNaver(pureTicker),
    fetchNewsYahoo(`${pureTicker}.KS`),   // ← 항상 .KS (KOSPI 접미사)
  ])
  result = sortByDate(dedupeByTitle([...naverNews, ...yahooNews])).slice(0, 5)
}
```

### 근본 원인

| # | 원인 | 설명 |
|---|------|------|
| RC1 | **KOSDAQ에 `.KS` 접미사 하드코딩** | KOSDAQ 종목(036540)인데 Yahoo에 `036540.KS`(KOSPI 형식)로 질의. Yahoo는 해당 심볼을 찾지 못함. 올바른 접미사는 `.KQ`. |
| RC2 | **Yahoo 무매칭 시 글로벌 뉴스 폴백** | Yahoo `/v1/finance/search`는 심볼이 매칭되지 않아도 일반/글로벌 top 뉴스를 반환. 이 무관 뉴스가 결과에 섞임. |
| RC3 | **Naver·Yahoo 무조건 병합** | Naver(종목 전용)가 좋은 뉴스를 줘도, Yahoo 폴백 junk가 항상 합쳐져 상위 5건을 오염시킴. 관련성 가드 없음. |

> RC1이 1차 트리거, RC2·RC3이 증폭. KOSDAQ뿐 아니라 Yahoo 커버리지가 약한 한국 중소형주 전반에서 재현 가능.

## 수정안

### Fix 1 — 시장별 올바른 Yahoo 접미사
- KOSPI/KRX → `.KS`, KOSDAQ → `.KQ`.

### Fix 2 — 한국 종목은 Naver 우선, Yahoo는 폴백 (핵심)
- Naver(종목 전용 엔드포인트)가 충분한 뉴스를 주면 **Yahoo를 병합하지 않는다**(글로벌 junk 차단).
- Naver가 부족할 때만(예: 3건 미만) Yahoo로 보강하되 **올바른 접미사** 사용.

```js
// 수정안 (newsApi.js fetchNews 한국 분기)
const isKorean = market === 'KRX' || market === 'KOSDAQ'
const pureTicker = ticker.replace(/\.(KS|KQ)$/, '')

if (isKorean) {
  const naverNews = await fetchNewsNaver(pureTicker)        // 1순위: 종목 전용
  if (naverNews.length >= 3) {
    result = sortByDate(dedupeByTitle(naverNews)).slice(0, 5)
  } else {
    // Naver 부족 시에만 Yahoo 보강 — 시장별 접미사 적용
    const suffix = market === 'KOSDAQ' ? '.KQ' : '.KS'
    const yahooNews = await fetchNewsYahoo(`${pureTicker}${suffix}`)
    result = sortByDate(dedupeByTitle([...naverNews, ...yahooNews])).slice(0, 5)
  }
} else {
  result = (await fetchNewsYahoo(ticker)).slice(0, 5)
}
```

장점.
- RC1 해소(접미사 정정), RC2·RC3 해소(Naver 충분 시 Yahoo 미병합).
- Naver가 충분하면 Yahoo 호출 자체를 생략 → 지연·비용도 소폭 개선.

### Fix 3 (옵션) — 약한 관련성 필터
- 한국 종목인데 제목이 전부 영문/글로벌 키워드면 1차 필터링. 다만 오탐 위험이 있어 **Fix 1·2로 충분하면 보류**.
- 더 근본적으로는 종목명(예: "SFA반도체")으로 Naver 검색을 보강하는 방안도 검토 가능(별도 작업).

## 검증 방법

| 검증 | 방법 | 기대 결과 |
|------|------|----------|
| 재현 | 수정 전 "SFA반도체 왜 움직였어?" | 무관 글로벌 뉴스 5건 |
| Fix 효과 | 수정 후 동일 질의 | SFA반도체/반도체 관련 뉴스 위주, 무관 글로벌 뉴스 제거 |
| 접미사 | KOSDAQ 종목 Yahoo 폴백 경로 | `.KQ`로 질의 |
| 폴백 | Naver 0건인 종목 | Yahoo(.KS/.KQ)로 보강되어 빈 결과 방지 |
| 회귀 | KOSPI 대형주(삼성전자 005930) | 기존과 동등 이상 품질 |
| 미국 종목 | AAPL 등 | 기존 Yahoo 단독 경로 불변 |

## 영향 범위 / 리스크
- 변경 파일: `src/api/newsApi.js` 단일(`fetchNews`).
- 하위 호환: 반환 형태 불변. 감성(P3)·분석(P5)·리서치 등 뉴스 소비처는 그대로.
- 리스크: Naver 종목 전용 엔드포인트가 일부 종목에서 비어 있을 수 있음 → Fix 2의 폴백이 방어. 임계값(3건)은 튜닝 가능.

## 미결 확인 사항 (구현 시)
- Naver `/api/news/integration/{code}` 가 KOSDAQ 소형주에도 종목 전용 뉴스를 정상 반환하는지 실측 확인.
  - 만약 Naver도 부실하면 → 종목명 기반 검색 보강(Fix 3 확장)을 별도 과제로 승격.
- Yahoo `.KQ` 커버리지 한계 확인(여전히 부실하면 한국 종목은 Naver 단독 운영 검토).

## 추가 수정안 — extractTicker 업종어 오매칭 (Fix 4)

### 배경
Fix 1·2 적용 후 뉴스 품질은 개선됐으나, **질문에 없던 종목(SFA반도체)** 이 분석에 등장하는 현상이 발견됐다.
질문이 삼성전자/SOX 디커플링에 관한 것인데, 프리패치 컨텍스트가 SFA반도체로 잡혔다.

### 진단 (코드 추적)
1. 채팅 컨텍스트엔 종목이 없어 `extractTicker(message)`로 질문에서 종목 추출.
2. 질문에 **"반도체"(업종 일반명사)** 포함 → 3순위 검색어로 사용.
3. `searchByQuery`는 이름 **부분일치**(`name.includes(q)`) → 이름에 "반도체"가 든 종목(SFA반도체 등) 매칭.
   ```js
   // stockMasterDb.js searchByQuery
   .filter(r => r.name.toLowerCase().includes(q) || ...)
   ```
4. 그중 하나(SFA반도체 036540)를 "질문 종목"으로 확정 → 뉴스·감성 프리패치.

근본 원인(RC4): **업종 일반명사("반도체·자동차·바이오·전자…")가 회사명에 부분일치해 특정 종목으로 오확정.**
"오늘"→오늘이엔엠과 동일 계열(일반어 prefix/부분 매칭).

### 수정안
1. **업종어 스톱워드 추가** — `반도체/자동차/바이오/전자/화학/금융/건설/제약/에너지/조선/방산…` 을 extractTicker 검색에서 제외.
2. **모호성 가드** — 한 단어가 여러 종목 이름에 부분일치하면(일반어) 특정 종목으로 확정하지 않고 건너뜀.
   단, **종목명이 검색어와 정확히 일치**하면 그 종목으로 인정(예: "삼성전자", "한미반도체"는 그대로 동작).

```js
// 모호성 가드 (extractTicker 검색 루프)
const rows = await searchByQuery(word, { limit: 10 })
const exactRows = rows.filter(r => r.name.toLowerCase() === word.toLowerCase())
// 정확 일치 없고 다수 매칭이면 일반어로 보고 건너뜀
if (exactRows.length === 0 && rows.length >= AMBIGUOUS_LIMIT) continue
const candidates = exactRows.length > 0 ? exactRows : rows
```

장점: 업종어("반도체")는 정확 일치 없음 + 다수 매칭 → 건너뜀. 회사명("삼성전자"·"한미반도체")은 정확 일치 → 인정.
스톱워드는 빠른 1차 차단, 가드는 목록에 없는 일반어까지 포괄.

### 검증
| 검증 | 입력 | 기대 |
|------|------|------|
| 오매칭 차단 | "미국 반도체는 폭등하는데 왜 디커플링?" | 특정 종목 미확정(종목 경로로 안 빠짐) |
| 회사명 정상 | "삼성전자 왜 올랐어?" | 삼성전자(005930) |
| 업종어 포함 회사명 | "한미반도체 어때?" | 한미반도체(정확 일치 인정) |
| 회귀 | "오늘 카카오 왜 빠졌어?" | 카카오 |

## 권장 진행
Fix 1 + Fix 2를 1차로 적용(저위험·고효과). Fix 3는 실측 후 필요 시. Fix 4는 업종어 오매칭 해소.

## 구현 상태
- [x] **Fix 1 + Fix 2 구현 완료** (2026-06-16, `src/api/newsApi.js`)
  - `NAVER_SUFFICIENT = 3` 상수 추가
  - 한국 종목: Naver 우선, 3건 이상이면 Yahoo 미병합 / 부족 시 Yahoo 보강(KOSDAQ `.KQ`)
  - 라이브 확인: SFA반도체 감성 뉴스가 반도체 관련(+0.8 등)으로 정상화
- [x] **Fix 4 구현 완료** (2026-06-16, `src/agents/researchAgent.js` extractTicker)
  - 업종 일반명사 STOPWORDS 추가(반도체·자동차·바이오 등)
  - 모호성 가드: `AMBIGUOUS_LIMIT = 3` — 정확 일치 없이 다수 매칭이면 종목 확정 건너뜀
  - 전체 테스트 90건 통과, 빌드 성공
- [ ] ✋ 배포 후 "반도체 디커플링" 류 질문에 엉뚱한 종목(SFA반도체) 미등장 확인
- [ ] ✋ 회귀: "삼성전자 왜 올랐어?" / "한미반도체 어때?" 정상 매칭 확인
- [ ] Fix 3 (옵션) — 실측 후 필요 시
- [ ] 미결: Naver KOSDAQ 소형주 응답 실측
