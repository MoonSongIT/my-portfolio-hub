# 14.04.03 AI 분석 기능 고도화 체크리스트

## 작성일
2026-06-15

## 근거 문서
- [14.04.01 AI 분석 기능고도화 조사](./14.04.01%20AI%20분석%20기능고도화%20조사.md)
- [14.04.02 AI 분석 기능고도화 구현계획](./14.04.02%20AI%20분석%20기능고도화%20구현계획.md)

> 진행하며 `[ ]` → `[x]` 로 갱신한다. 수작업 확인 항목은 ✋ 표시.

---

## 사전 결정 (착수 전)

- [x] 웹 검색 토글 기본값 결정 → OFF(기본), 설정에서 ON
- [x] 동적 필터링 버전(`web_search_20260209`) 1차 포함 여부 결정 → 미포함(기본형 20250305)
- [x] Opus 적용 에이전트 범위 확정 → analysis·research·report
- [x] ✋ Anthropic 콘솔에서 웹 검색 사용 허용 설정 확인

---

## Phase 1 — P1: 프록시 모델 파라미터화

### api/claude.js
- [x] 요청 본문에서 `model` 수용
- [x] 허용 모델 화이트리스트 상수 추가 (`ALLOWED_MODELS`)
- [x] 미허용·미지정 `model` → 기본값 보정 (`DEFAULT_MODEL`)
- [x] 검증 통과 `model`만 Anthropic 요청에 전달
- [x] 기존 호출(하위호환) 정상 동작 확인 (build 통과)

### src/api/claudeApi.js
- [x] `AGENT_MODELS` 맵 추가 (analysis·research·report → Opus 4.8)
- [x] `sendToAgent` payload에 `model` 포함
- [x] `runAgentPipeline` 경로에 `model` 반영 (sendToAgent 경유)
- [x] `sendWithHistory` 경로 → 기본 모델(Sonnet) 사용 확인 (journal=기본값, 변경 불요)

### P1 검증
- [x] ✋ analysis 호출 시 네트워크 응답 `model` = `claude-opus-4-8` 확인
- [x] 미허용 model 문자열 → 기본값 보정 (프록시 화이트리스트 로직)

---

## Phase 1 — P2: web_search 도구 결합

### 응답 파서 (선행 — TDD)
- [x] 멀티 `text` 블록 결합 함수 단위 테스트 작성
- [x] 인용(`citations`) 추출 로직 단위 테스트 작성
- [x] 파서 구현 → 테스트 통과 (5건, `claudeApiParser.test.js`)

### api/claude.js
- [x] 요청 본문에서 `tools` 배열 수용 → Anthropic 요청 passthrough
- [x] `pause_turn` 등 발생 시 루프 없이 존재하는 text 블록 반환 (단순화)
- [x] web_search 응답을 `{ content }` 그대로 클라이언트에 전달 (블록 보존)

### src/api/claudeApi.js
- [x] `AGENT_TOOLS` 맵 추가 (analysis·research·alert 활성)
- [x] web_search 도구 설정 적용 (max_uses=3, user_location=KR)
  - [x] 핫픽스: `allowed_domains` 제거 — 조직(Console) 도메인 설정 충돌로 400 발생(런타임 로그 확인), user_location 으로만 한국 편향 처리
- [x] 응답 파서로 `{ text, citations }` 정규화
- [x] citations → 응답 하단 `[참고 출처]` 렌더링
- [x] 기존 Yahoo/Naver/DART 프리패치와 병행 동작 유지

### 비용 가드 (settingsStore + UI)
- [x] `settingsStore`에 `webSearchEnabled` 토글 추가 (기본 OFF)
- [x] 토글 OFF 시 `AGENT_TOOLS` 비활성 → 프리패치 폴백
- [x] 설정 화면에 웹 검색 토글 UI 추가 (AI 및 API 키 섹션)
- [x] 동일 질의 `apiCache` 재사용 (기존 캐시 그대로 유지)

### P2 검증
- [x] ✋ "오늘 삼성전자 왜 움직였어?" 질의 → 검색 수행 + 출처 인용 응답
- [x] ✋ web_search 멀티 블록 응답에서 텍스트 잘림 없음 확인
- [x] ✋ 설정 웹 검색 OFF → 기존 프리패치 방식 정상 응답
- [x] ✋ max_uses 초과(`max_uses_exceeded`) 무해 처리 확인
- [x] ✋ journal/portfolio/report → 기존과 동일 동작(도구 미적용)

---

## 완료 처리 (Phase 1)

- [x] `npx vitest run` — 신규 파서 5건 포함 78건 통과 (htsParser 4건은 사전 존재 실패, 무관)
- [x] `npm run build` 성공
- [x] 커밋 분리 (docs / feat / 도메인 핫픽스)
- [x] master 머지·푸시 (단일 브랜치 전략)
- [x] ✋ 배포 후 실제 AI 분석 화면에서 최종 동작 확인

---

## ✅ Phase 1 완료 (2026-06-15)

최고 사양 모델(Opus 4.8) 적용 + 실시간 웹 검색·출처 인용 라이브 검증 완료.
후속은 Phase 2(P3 뉴스 감성 점수 + P4 LLM 라우팅) / Phase 3(P5 추론 강화 + P6 거시 뉴스).

---

## Phase 2 — 구현 완료 (상세: 14.04.02 §4)

### 사전 결정 (착수 전)
- [x] P3 적용 범위 결정 → movement(종목 급등락)만
- [x] P3 동작 방식 결정 → 항상 ON (Haiku, 실패 시 폴백)
- [x] P4 LLM 보정 트리거 결정 → 정적 폴백 시에만

### P3 — 뉴스 감성 점수 구조화
#### 감성 모듈 (선행 — TDD)
- [x] 감성 JSON 파싱 단위 테스트 작성 (정상/코드펜스/클램프/깨진 JSON 폴백)
- [x] `src/api/sentimentApi.js` — `scoreNewsSentiment(news, {name, ticker})` 구현
- [x] Haiku 호출 (claude-haiku-4-5, max_tokens 512, 타임아웃 5s, 실패 시 빈 배열)
- [x] `apiCache` 연동 (`sentiment:${ticker}:${market}`, 3분)

#### analysisAgent 주입
- [x] `buildMovementContext` 뉴스별 감성(점수/강도/근거) 렌더
- [x] 종목별 종합 감성 라벨 한 줄 추가
- [x] ANALYSIS_PROMPT에 감성 활용 지침 추가
- [~] portfolio/market brief 경로 → 범위 결정(movement만)으로 제외

#### claudeApi 결합
- [x] `sendToAgent` analysis movement 경로에서 `fetchNews` 후 `mergeSentiment` 병합
- [x] 항상 ON — 별도 토글 없이 실패 시 폴백
- [x] 후속 보완: 채팅 free-text에서 `extractTicker`로 종목 추출 → 급등락(감성) 경로 연결
- [x] 후속 보완: `AGENT_MAX_TOKENS.analysis` 1024 → 2048 (응답 잘림 방지)

#### P3 검증
- [x] 감성 파싱/병합 단위 테스트 통과 (7건)
- [x] ✋ 채팅 종목 질의 → Haiku 감성 호출 발생 + JSON 배열 응답 확인 (라이브)
- [x] ✋ 정확 종목 매칭 → 의미 있는 감성 점수(호재/악재 구분) 확인 (라이브, 평균 +0.24)
- [x] ✋ 종목 오매칭("오늘"→오늘이엔엠) 수정 후 정상 종목 분석 확인
- [ ] ✋ Haiku 실패 시 감성 없이 기존 분석 정상 (자연 발생 시 확인 예정)

### P4 — LLM 기반 라우팅 (하이브리드)
- [x] `orchestrator.classifyIntentLLM(message)` — Haiku 분류 (max_tokens 16, 타임아웃 2s)
- [x] `orchestrator.routeToAgentSmart(message)` — 정적 우선 → 폴백 시에만 LLM 보정
- [x] `parseIntentResponse` 순수 파서 분리 (테스트 대상)
- [x] `claudeApi.sendToAgent`의 라우팅을 `routeToAgentSmart`로 교체
- [x] 정적 `routeToAgent` 유지 (폴백·테스트 기준)

#### P4 검증
- [x] 의도 파싱 + 정적 라우팅 단위 테스트 통과 (5건)
- [x] ✋ "무슨일 있었길래?" → 정적 폴백 후 Haiku 보정("analysis")로 원인분석 라우팅 (라이브)
- [x] ✋ 명확 키워드("삼성전자 분석해줘" 등) → 라우팅 Haiku 미호출 (간접 확인)
- [x] ✋ LLM 실패/타임아웃 시 정적 결과 폴백 (catch→portfolio, 단위테스트로 폴백 경로 검증)

### 완료 처리 (Phase 2)
- [x] `npx vitest run` — 신규 12건 포함 90건 통과 (htsParser 4건 사전 실패, 무관)
- [x] `npm run build` 성공
- [x] 커밋 분리 (P3 / P4 / 후속 보완 / 오매칭 fix)
- [x] master 머지·푸시
- [x] ✋ 배포 후 P3 분석·감성 라이브 확인
- [x] ✋ P4 라우팅 라이브 확인 (모호 질문 → Haiku 보정 동작 확인)

> **Phase 2 상태**: P3(뉴스 감성)·P4(LLM 라우팅) 모두 라이브 검증 완료.

---

## Phase 3 — 구현 완료 (상세: 14.04.02 §4-9)

### 사전 결정 (착수 전)
- [x] P5 확장적 사고 적용 에이전트 → analysis + research
- [x] P5 thinking 방식 → adaptive (Opus 4.8은 manual 미지원) + max_tokens 4096
- [x] P5 동작 → 항상 ON
- [x] P6 거시 뉴스 소스 → web_search 활용
- [x] P6 적용 범위 → market brief만

### P5 — 추론 강화 (확장적 사고)
- [x] (선반영) `AGENT_MAX_TOKENS.analysis` 1024 → 2048 → 4096 (잘림 방지 + 사고 여유)
- [x] `api/claude.js` — `thinking` passthrough (adaptive / enabled+budget 검증)
- [x] `claudeApi.js` — `AGENT_THINKING` 맵 (analysis·research → `{type:'adaptive'}`)
- [x] thinking 활성 에이전트 max_tokens 4096 동반 상향
- [x] 응답 파서 thinking 블록 무시 (extractTextAndCitations는 text만 취함 → 호환)
- [x] 핫픽스: Opus 4.8은 `type:'enabled'` 미지원(400) → `adaptive` 사용 (문서 확인)

#### P5 검증
- [ ] ✋ analysis 응답이 다요인(뉴스·공시·감성 교차) 추론으로 깊어졌는지
- [ ] ✋ thinking 블록이 본문에 새지 않고 텍스트만 정상 출력
- [ ] ✋ 잘림 없이 완결

### P6 — 거시·테마 뉴스 결합 (P2 web_search 종속)
- [x] market brief 경로(`MARKET_BRIEF_PROMPT`)에 거시 웹 검색 지침 추가
- [~] alert 일일 브리핑 → 범위 결정(market brief만)으로 제외
- [x] `webSearchEnabled` ON일 때만 거시 검색 (analysis web_search 도구 게이팅 재사용) — OFF면 폴백
- [x] 거시 질의 지침("금리·환율·통화정책·지정학·유가 등") 프롬프트 반영

#### P6 검증
- [ ] ✋ 시장 브리핑에 금리·환율·정책 등 거시 이슈 반영
- [ ] ✋ web_search OFF 시 기존 브리핑 정상

### 완료 처리 (Phase 3)
- [x] `npx vitest run` — 90건 통과 (htsParser 4건 사전 실패, 무관)
- [x] `npm run build` 성공
- [ ] 커밋 분리 (P5 / P6)
- [ ] master 머지·푸시
- [ ] ✋ 배포 후 실제 동작 확인
- [ ] ✋ 배포 후 실제 동작 확인
