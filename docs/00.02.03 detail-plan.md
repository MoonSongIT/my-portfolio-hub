# Phase 3 상세 구현 계획 — 실시간 주가 API 연동

> **기간**: 5~7일
> **목표**: 샘플 데이터 → 실제 주가 데이터 전환, TanStack Query 캐싱 시스템 구축, Research 페이지 완성
> **산출물**: stockApi.js 완성, 커스텀 훅 4종, Dashboard/Portfolio 실데이터 연동, Research 종목 검색+상세

---

## Phase 3 아키텍처 개요

### 데이터 흐름: API → Cache → Store → UI

```
┌──────────────────────────────────────────────────────────────────┐
│                        외부 데이터 소스                            │
│  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐  │
│  │ Yahoo Finance    │  │ KRX 데이터포털    │  │ 환율 API      │  │
│  │ (글로벌 주가)    │  │ (한국 시장)       │  │ (USD/KRW)     │  │
│  │ - 현재가/시세    │  │ - 일별 시세       │  │ - 실시간 환율 │  │
│  │ - 히스토리       │  │ - 종목 정보       │  │               │  │
│  │ - 기업 프로필    │  │ - 업종 데이터     │  │               │  │
│  └────────┬─────────┘  └────────┬─────────┘  └───────┬───────┘  │
└───────────┼──────────────────────┼─────────────────────┼─────────┘
            │ CORS 프록시          │                     │
            ▼                      ▼                     ▼
┌──────────────────────────────────────────────────────────────────┐
│                   Vite Dev Proxy / API 서비스                     │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │  src/api/stockApi.js                                      │    │
│  │  ┌─────────────────┐ ┌────────────────┐ ┌─────────────┐ │    │
│  │  │ fetchQuote()    │ │ fetchHistory() │ │ fetchRate()  │ │    │
│  │  │ fetchSearch()   │ │ fetchProfile() │ │              │ │    │
│  │  │ fetchBatchQuotes│ │ fetchFinancials│ │              │ │    │
│  │  └─────────────────┘ └────────────────┘ └─────────────┘ │    │
│  └──────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────┘
            │
            ▼
┌──────────────────────────────────────────────────────────────────┐
│              TanStack Query v5 (캐싱 + 폴링 + 에러 처리)         │
│  ┌──────────────┐ ┌────────────────┐ ┌──────────────────────┐   │
│  │ QueryClient  │ │ Smart Polling  │ │ 에러/로딩 상태 관리   │   │
│  │ staleTime:30s│ │ 장중: 30초     │ │ retry: 2회            │   │
│  │ gcTime: 5min │ │ 장외: 5분      │ │ error boundary 연동   │   │
│  └──────────────┘ └────────────────┘ └──────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
            │
            ▼
┌──────────────────────────────────────────────────────────────────┐
│                     커스텀 훅 (src/hooks/)                        │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐     │
│  │useStockPrice │ │useStockSearch│ │ useMarketStatus      │     │
│  │  단일 종목   │ │  검색 결과   │ │  장중/장외 판단      │     │
│  │  실시간 시세 │ │  자동완성    │ │  KRX/NYSE/NASDAQ     │     │
│  └──────────────┘ └──────────────┘ └──────────────────────┘     │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐     │
│  │useBatchQuotes│ │useStockDetail│ │ useExchangeRate      │     │
│  │  포트폴리오  │ │  종목 상세   │ │  USD/KRW 환율        │     │
│  │  일괄 조회   │ │  프로필+재무 │ │  자동 업데이트       │     │
│  └──────────────┘ └──────────────┘ └──────────────────────┘     │
└──────────────────────────────────────────────────────────────────┘
            │
            ▼
┌──────────────────────────────────────────────────────────────────┐
│        Zustand Store (기존 portfolioStore 확장)                   │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │ portfolioStore.js (변경 사항)                              │    │
│  │ - samplePortfolio 의존 제거                                │    │
│  │ - updatePrice() ← API 데이터 반영                          │    │
│  │ - EXCHANGE_RATE → 동적 환율로 전환                         │    │
│  │ - lastUpdated 타임스탬프 추가                              │    │
│  └──────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────┘
            │
            ▼
┌──────────────────────────────────────────────────────────────────┐
│                         UI 페이지                                 │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐     │
│  │ Dashboard    │ │ Portfolio    │ │ Research (신규 완성)  │     │
│  │ KPI 실데이터 │ │ 실시간 수익률│ │ 종목 검색            │     │
│  │ 차트 실데이터│ │ 현재가 갱신  │ │ 종목 상세 분석       │     │
│  │ 업데이트시간 │ │ 환율 실데이터│ │ 가격 차트            │     │
│  └──────────────┘ └──────────────┘ └──────────────────────┘     │
└──────────────────────────────────────────────────────────────────┘
```

### Yahoo Finance API 엔드포인트 정리

| 기능 | 엔드포인트 | 설명 |
|------|-----------|------|
| 실시간 시세 | `/v8/finance/chart/{ticker}?interval=1d&range=1d` | 현재가, 전일종가, 거래량 |
| 가격 히스토리 | `/v8/finance/chart/{ticker}?interval=1d&range=6mo` | 기간별 OHLCV 데이터 |
| 종목 검색 | `/v1/finance/search?q={keyword}` | 티커/종목명 자동완성 |
| 기업 요약 | `/v10/finance/quoteSummary/{ticker}?modules=...` | PER, PBR, ROE 등 재무지표 |

### 한국 종목 티커 변환 규칙

```
KRX 종목코드 → Yahoo Finance 티커:
  005930 → 005930.KS  (KOSPI)
  035420 → 035420.KS  (KOSPI)
  263750 → 263750.KQ  (KOSDAQ)

규칙: {6자리코드}.KS (코스피) / {6자리코드}.KQ (코스닥)
```

---

## 10단계 상세 실행 가이드

---

### Step 1: TanStack Query 초기 설정

**목표**: QueryClient 생성 + Provider 연동 + DevTools 설치

**수정 파일**: `src/main.jsx`

```jsx
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { ReactQueryDevtools } from '@tanstack/react-query-devtools'

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 30 * 1000,        // 30초 — 장중 폴링 주기와 동기화
      gcTime: 5 * 60 * 1000,       // 5분 — 가비지 컬렉션
      retry: 2,                    // 실패 시 2회 재시도
      retryDelay: (attempt) => Math.min(1000 * 2 ** attempt, 10000),
      refetchOnWindowFocus: true,  // 탭 전환 시 자동 리페치
      refetchOnReconnect: true,    // 네트워크 복구 시 자동 리페치
    },
  },
})

// App 래핑
<QueryClientProvider client={queryClient}>
  <App />
  <ReactQueryDevtools initialIsOpen={false} />
</QueryClientProvider>
```

**설치 패키지**:

```bash
npm install @tanstack/react-query-devtools
```

> `@tanstack/react-query`는 이미 설치됨 (package.json 확인)

**검증 포인트**:
- 브라우저 우하단에 React Query DevTools 아이콘 표시
- 콘솔에 QueryClient 관련 에러 없음

---

### Step 2: Vite 프록시 설정 (CORS 우회)

**목표**: Yahoo Finance API CORS 문제 해결을 위한 개발 서버 프록시 설정

**수정 파일**: `vite.config.js`

```javascript
export default defineConfig({
  plugins: [react()],
  resolve: { alias: { '@': path.resolve(__dirname, './src') } },
  server: {
    proxy: {
      // Yahoo Finance API 프록시
      '/api/yahoo': {
        target: 'https://query1.finance.yahoo.com',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api\/yahoo/, ''),
        headers: {
          'User-Agent': 'Mozilla/5.0',
        },
      },
      // Yahoo Finance v10 API 프록시 (quoteSummary용)
      '/api/yahoo-v10': {
        target: 'https://query2.finance.yahoo.com',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api\/yahoo-v10/, ''),
        headers: {
          'User-Agent': 'Mozilla/5.0',
        },
      },
    },
  },
})
```

**검증 포인트**:
- `npm run dev` 후 `http://localhost:5173/api/yahoo/v8/finance/chart/AAPL` 접근 시 JSON 반환
- 브라우저 콘솔에 CORS 에러 없음

---

### Step 3: stockApi.js — 핵심 API 서비스 구현

**목표**: Yahoo Finance API 래퍼 함수 전체 구현

**수정 파일**: `src/api/stockApi.js`

```javascript
import axios from 'axios'

const yahooApi = axios.create({
  baseURL: '/api/yahoo',
  timeout: 10000,
})

const yahooV10Api = axios.create({
  baseURL: '/api/yahoo-v10',
  timeout: 10000,
})

// ─── 한국 종목 티커 변환 ───
export const toYahooTicker = (ticker, market) => {
  if (market === 'KRX') {
    // KOSPI: .KS, KOSDAQ: .KQ (기본은 .KS)
    return `${ticker}.KS`
  }
  return ticker // 미국 종목은 그대로
}

// ─── 1. 실시간 시세 (단일 종목) ───
export const fetchQuote = async (ticker, market = 'NASDAQ') => {
  const yahooTicker = toYahooTicker(ticker, market)
  const { data } = await yahooApi.get(`/v8/finance/chart/${yahooTicker}`, {
    params: { interval: '1d', range: '1d' },
  })

  const result = data.chart.result?.[0]
  if (!result) throw new Error(`종목을 찾을 수 없습니다: ${ticker}`)

  const meta = result.meta
  return {
    ticker,
    yahooTicker,
    name: meta.shortName || meta.longName || ticker,
    currentPrice: meta.regularMarketPrice,
    previousClose: meta.chartPreviousClose || meta.previousClose,
    change: meta.regularMarketPrice - (meta.chartPreviousClose || meta.previousClose),
    changePercent: ((meta.regularMarketPrice - (meta.chartPreviousClose || meta.previousClose)) / (meta.chartPreviousClose || meta.previousClose)) * 100,
    volume: meta.regularMarketVolume,
    currency: meta.currency,
    exchangeName: meta.exchangeName,
    marketState: meta.marketState, // 'REGULAR', 'PRE', 'POST', 'CLOSED'
    timestamp: meta.regularMarketTime * 1000,
  }
}

// ─── 2. 일괄 시세 조회 (포트폴리오용) ───
export const fetchBatchQuotes = async (holdings) => {
  // 병렬 요청 (최대 동시 5개)
  const batchSize = 5
  const results = []

  for (let i = 0; i < holdings.length; i += batchSize) {
    const batch = holdings.slice(i, i + batchSize)
    const batchResults = await Promise.allSettled(
      batch.map(h => fetchQuote(h.ticker, h.market))
    )
    results.push(...batchResults)
  }

  return results.map((r, idx) => ({
    ticker: holdings[idx].ticker,
    success: r.status === 'fulfilled',
    data: r.status === 'fulfilled' ? r.value : null,
    error: r.status === 'rejected' ? r.reason.message : null,
  }))
}

// ─── 3. 가격 히스토리 ───
export const fetchHistory = async (ticker, market = 'NASDAQ', range = '6mo', interval = '1d') => {
  const yahooTicker = toYahooTicker(ticker, market)
  const { data } = await yahooApi.get(`/v8/finance/chart/${yahooTicker}`, {
    params: { interval, range },
  })

  const result = data.chart.result?.[0]
  if (!result) throw new Error(`히스토리 데이터 없음: ${ticker}`)

  const timestamps = result.timestamp || []
  const quotes = result.indicators.quote?.[0] || {}

  return timestamps.map((ts, i) => ({
    date: new Date(ts * 1000).toISOString().split('T')[0],
    open: quotes.open?.[i],
    high: quotes.high?.[i],
    low: quotes.low?.[i],
    close: quotes.close?.[i],
    volume: quotes.volume?.[i],
  })).filter(d => d.close != null) // null 데이터 제거
}

// ─── 4. 종목 검색 ───
export const fetchSearch = async (query) => {
  if (!query || query.length < 1) return []

  const { data } = await yahooApi.get('/v1/finance/search', {
    params: {
      q: query,
      quotesCount: 10,
      newsCount: 0,
      listsCount: 0,
    },
  })

  return (data.quotes || []).map(q => ({
    ticker: q.symbol,
    name: q.shortname || q.longname || q.symbol,
    type: q.quoteType,       // 'EQUITY', 'ETF', 'INDEX'
    exchange: q.exchange,     // 'KSC', 'NMS', 'NYQ'
    market: q.exchange === 'KSC' || q.exchange === 'KOE' ? 'KRX'
          : q.exchange === 'NMS' ? 'NASDAQ'
          : q.exchange === 'NYQ' ? 'NYSE'
          : q.exchange,
  }))
}

// ─── 5. 기업 상세 + 재무 지표 ───
export const fetchProfile = async (ticker, market = 'NASDAQ') => {
  const yahooTicker = toYahooTicker(ticker, market)
  const modules = 'summaryProfile,summaryDetail,defaultKeyStatistics,financialData'

  const { data } = await yahooV10Api.get(`/v10/finance/quoteSummary/${yahooTicker}`, {
    params: { modules },
  })

  const result = data.quoteSummary?.result?.[0]
  if (!result) throw new Error(`기업 정보를 찾을 수 없습니다: ${ticker}`)

  const profile = result.summaryProfile || {}
  const detail = result.summaryDetail || {}
  const stats = result.defaultKeyStatistics || {}
  const financial = result.financialData || {}

  return {
    // 기업 프로필
    sector: profile.sector,
    industry: profile.industry,
    website: profile.website,
    description: profile.longBusinessSummary,
    country: profile.country,

    // 시세 요약
    marketCap: detail.marketCap?.raw,
    fiftyTwoWeekHigh: detail.fiftyTwoWeekHigh?.raw,
    fiftyTwoWeekLow: detail.fiftyTwoWeekLow?.raw,
    dividendYield: detail.dividendYield?.raw,
    averageVolume: detail.averageVolume?.raw,

    // 재무 지표
    trailingPE: detail.trailingPE?.raw || stats.trailingEps?.raw,
    forwardPE: stats.forwardPE?.raw,
    priceToBook: stats.priceToBook?.raw,
    returnOnEquity: financial.returnOnEquity?.raw,
    debtToEquity: financial.debtToEquity?.raw,
    revenueGrowth: financial.revenueGrowth?.raw,
    earningsGrowth: financial.earningsGrowth?.raw,
    currentRatio: financial.currentRatio?.raw,
    targetMeanPrice: financial.targetMeanPrice?.raw,
    recommendationKey: financial.recommendationKey, // 'buy', 'hold', 'sell'
    numberOfAnalystOpinions: financial.numberOfAnalystOpinions?.raw,
  }
}

// ─── 6. 환율 조회 (USD/KRW) ───
export const fetchExchangeRate = async () => {
  const { data } = await yahooApi.get('/v8/finance/chart/USDKRW=X', {
    params: { interval: '1d', range: '1d' },
  })

  const meta = data.chart.result?.[0]?.meta
  return {
    rate: meta?.regularMarketPrice || 1350, // fallback
    timestamp: (meta?.regularMarketTime || 0) * 1000,
  }
}
```

**검증 포인트**:
- `fetchQuote('AAPL')` → 정상 JSON 반환 (currentPrice 포함)
- `fetchQuote('005930', 'KRX')` → 삼성전자 시세 반환
- `fetchSearch('삼성')` → 검색 결과 배열 반환
- `fetchExchangeRate()` → rate 필드에 실시간 환율

---

### Step 4: 커스텀 훅 구현 (src/hooks/)

**목표**: TanStack Query 기반 커스텀 훅 6종 구현

**생성 파일**: `src/hooks/useStockData.js`

```javascript
import { useQuery } from '@tanstack/react-query'
import { fetchQuote, fetchBatchQuotes, fetchHistory, fetchSearch, fetchProfile, fetchExchangeRate } from '../api/stockApi'

// ─── 장중 여부 판단 ───
const isKRXOpen = () => {
  const now = new Date()
  const kst = new Date(now.toLocaleString('en-US', { timeZone: 'Asia/Seoul' }))
  const day = kst.getDay()
  const hour = kst.getHours()
  const min = kst.getMinutes()
  const time = hour * 60 + min
  return day >= 1 && day <= 5 && time >= 540 && time <= 930 // 09:00~15:30
}

const isUSMarketOpen = () => {
  const now = new Date()
  const est = new Date(now.toLocaleString('en-US', { timeZone: 'America/New_York' }))
  const day = est.getDay()
  const hour = est.getHours()
  const min = est.getMinutes()
  const time = hour * 60 + min
  return day >= 1 && day <= 5 && time >= 570 && time <= 960 // 09:30~16:00
}

export const isAnyMarketOpen = () => isKRXOpen() || isUSMarketOpen()

// ─── 1. 단일 종목 실시간 시세 ───
export function useStockPrice(ticker, market, options = {}) {
  return useQuery({
    queryKey: ['stockPrice', ticker, market],
    queryFn: () => fetchQuote(ticker, market),
    enabled: !!ticker,
    refetchInterval: isAnyMarketOpen() ? 30_000 : 300_000, // 장중 30초, 장외 5분
    ...options,
  })
}

// ─── 2. 포트폴리오 일괄 시세 조회 ───
export function useBatchQuotes(holdings, options = {}) {
  return useQuery({
    queryKey: ['batchQuotes', holdings.map(h => h.ticker).join(',')],
    queryFn: () => fetchBatchQuotes(holdings),
    enabled: holdings.length > 0,
    refetchInterval: isAnyMarketOpen() ? 60_000 : 300_000, // 장중 1분, 장외 5분
    ...options,
  })
}

// ─── 3. 종목 검색 (디바운스 연동) ───
export function useStockSearch(query, options = {}) {
  return useQuery({
    queryKey: ['stockSearch', query],
    queryFn: () => fetchSearch(query),
    enabled: !!query && query.length >= 1,
    staleTime: 60 * 1000, // 검색 결과는 1분 캐시
    ...options,
  })
}

// ─── 4. 가격 히스토리 ───
export function useStockHistory(ticker, market, range = '6mo', options = {}) {
  return useQuery({
    queryKey: ['stockHistory', ticker, market, range],
    queryFn: () => fetchHistory(ticker, market, range),
    enabled: !!ticker,
    staleTime: 5 * 60 * 1000, // 히스토리는 5분 캐시
    ...options,
  })
}

// ─── 5. 종목 상세 프로필 + 재무 ───
export function useStockDetail(ticker, market, options = {}) {
  return useQuery({
    queryKey: ['stockDetail', ticker, market],
    queryFn: () => fetchProfile(ticker, market),
    enabled: !!ticker,
    staleTime: 10 * 60 * 1000, // 재무 데이터는 10분 캐시
    ...options,
  })
}

// ─── 6. 환율 (USD/KRW) ───
export function useExchangeRate(options = {}) {
  return useQuery({
    queryKey: ['exchangeRate'],
    queryFn: fetchExchangeRate,
    staleTime: 60 * 1000,      // 1분 캐시
    refetchInterval: 5 * 60_000, // 5분마다 갱신
    ...options,
  })
}
```

**생성 파일**: `src/hooks/useDebounce.js`

```javascript
import { useState, useEffect } from 'react'

export function useDebounce(value, delay = 300) {
  const [debouncedValue, setDebouncedValue] = useState(value)

  useEffect(() => {
    const handler = setTimeout(() => setDebouncedValue(value), delay)
    return () => clearTimeout(handler)
  }, [value, delay])

  return debouncedValue
}
```

**검증 포인트**:
- `useStockPrice('AAPL', 'NASDAQ')` → `{ data, isLoading, isError }` 반환
- DevTools에서 `['stockPrice', 'AAPL', 'NASDAQ']` 쿼리 키 확인
- 장중에는 30초마다 자동 리페치 동작

---

### Step 5: portfolioStore 확장 — 실데이터 연동

**목표**: 샘플 데이터 하드코딩 제거, API 데이터 반영 로직 추가

**수정 파일**: `src/store/portfolioStore.js`

```javascript
// 변경 사항:
// 1. EXCHANGE_RATE 상수 → 동적 환율 state 추가
// 2. updateAllPrices() 액션 — API 일괄 조회 결과 반영
// 3. lastUpdated 타임스탬프 추가
// 4. 기존 samplePortfolio 의존은 loadUserAccounts 초기값으로만 유지
//    (사용자가 아직 API 조회 전에는 샘플 가격 표시)

export const usePortfolioStore = create(
  persist(
    immer((set, get) => ({
      accounts: [],
      selectedAccountId: 'all',
      exchangeRate: 1350,      // ← 신규: 동적 환율
      lastUpdated: null,       // ← 신규: 마지막 업데이트 시각

      // ... 기존 액션 유지 ...

      // ─── 신규 액션: 전체 종목 가격 일괄 업데이트 ───
      updateAllPrices: (priceMap) => set((state) => {
        // priceMap: { ticker: currentPrice, ... }
        state.accounts.forEach(acc => {
          acc.holdings.forEach(h => {
            if (priceMap[h.ticker] !== undefined) {
              h.currentPrice = priceMap[h.ticker]
            }
          })
        })
        state.lastUpdated = new Date().toISOString()
      }),

      // ─── 신규 액션: 환율 업데이트 ───
      updateExchangeRate: (rate) => set((state) => {
        state.exchangeRate = rate
      }),

      // ─── 기존 getter 수정: 동적 환율 사용 ───
      getTotalValue: () => {
        const { getSelectedHoldings, getSelectedCash, exchangeRate } = get()
        const holdings = getSelectedHoldings()
        const { krw, usd } = getSelectedCash()
        return calculateTotalValue(holdings, krw, usd, exchangeRate)
      },
    })),
    { name: 'portfolio-storage', version: 4 }
  )
)
```

**검증 포인트**:
- `updateAllPrices({ 'AAPL': 180.50 })` 호출 시 해당 종목 currentPrice 변경
- `lastUpdated`에 타임스탬프 저장
- `exchangeRate` state가 persist로 저장/복원

---

### Step 6: Dashboard 실데이터 연동

**목표**: KPI 카드 + 차트를 실제 API 데이터로 전환

**수정 파일**: `src/pages/Dashboard.jsx`

```
변경 사항:
1. useBatchQuotes() 훅으로 보유 종목 일괄 시세 조회
2. useExchangeRate() 훅으로 실시간 환율 조회
3. API 응답 → portfolioStore.updateAllPrices() 연동
4. samplePriceHistory 의존 제거 → useStockHistory() 기반 히스토리
5. 로딩/에러 상태 UI 추가 (LoadingSpinner, 에러 배너)
6. "마지막 업데이트: HH:mm:ss" 표시
7. "새로고침" 버튼 (queryClient.invalidateQueries 호출)
```

**UI 변경 요소**:

```
┌─────────────────────────────────────────────────┐
│ Dashboard                    마지막 업데이트 14:32:15 [🔄] │
├──────────┬──────────┬──────────┬──────────┤
│ 총 자산  │ 오늘 손익 │ 총 수익률 │ 보유종목 │  ← 실데이터
│ ₩xx,xxx  │ +₩xx,xxx │ +12.3%   │ 8종목    │
├──────────┴──────────┴──────────┴──────────┤
│ 포트폴리오 추이 (6개월 실데이터 라인차트)          │
├──────────────────────┬────────────────────┤
│ 종목별 비중 파이차트  │ 섹터별 비중 파이차트│  ← 실데이터
├──────────────────────┴────────────────────┤
│ 보유 종목 요약 테이블 (실시간 현재가+수익률)       │
└─────────────────────────────────────────────────┘
```

**검증 포인트**:
- 페이지 로드 시 로딩 스피너 → 데이터 로드 후 KPI 표시
- "마지막 업데이트" 시각이 실제 API 호출 시각
- 새로고침 버튼 클릭 시 데이터 갱신
- API 실패 시 에러 메시지 표시 (이전 캐시 데이터 유지)

---

### Step 7: Portfolio 페이지 실데이터 연동

**목표**: 보유 종목 현재가/수익률을 실시간 API 데이터로 표시

**수정 파일**: `src/pages/Portfolio.jsx`, `src/components/portfolio/PortfolioTable.jsx`

```
변경 사항:
1. useBatchQuotes() 훅 연동
2. PortfolioTable에 "현재가" 컬럼 실시간 업데이트
3. 가격 변동 시 색상 애니메이션 (상승: 초록 깜빡, 하락: 빨강 깜빡)
4. 환율 실데이터 연동 (USD 종목 원화 환산)
5. "전체 새로고침" 버튼 + 개별 종목 "시세 조회" 버튼
6. AddStockModal — 종목 추가 시 실시간 검색 API 연동
```

**AddStockModal 개선**:

```
기존: 티커 직접 입력
변경: 검색어 입력 → useStockSearch() → 자동완성 드롭다운 → 선택 → 현재가 자동 반영
```

**검증 포인트**:
- 포트폴리오 테이블의 현재가가 API에서 가져온 실제 가격
- USD 종목의 원화 환산이 실시간 환율 기준
- 종목 추가 시 검색 자동완성 동작
- 가격 변동 시 시각적 피드백

---

### Step 8: Research 페이지 완성 — 종목 검색 + 상세

**목표**: 종목 검색 → 결과 목록 → 상세 분석 페이지 완성

**수정 파일**: `src/pages/Research.jsx`
**생성 파일**: `src/pages/StockDetail.jsx` (종목 상세)
**수정 파일**: `src/App.jsx` (라우트 추가)

```
Research 페이지 레이아웃:

┌─────────────────────────────────────────────────┐
│ 종목 탐색                                         │
├─────────────────────────────────────────────────┤
│ 🔍 [검색어 입력...              ] [검색]          │
│    ├─ 자동완성 드롭다운                            │
│    │  AAPL — Apple Inc. (NASDAQ)                  │
│    │  005930 — 삼성전자 (KRX)                     │
│    │  AMZN — Amazon.com (NASDAQ)                  │
├─────────────────────────────────────────────────┤
│ 검색 결과 (카드 그리드)                             │
│ ┌──────────────┐ ┌──────────────┐ ┌───────────┐ │
│ │ AAPL         │ │ AMZN         │ │ MSFT      │ │
│ │ Apple Inc.   │ │ Amazon       │ │ Microsoft │ │
│ │ $178.50      │ │ $185.30      │ │ $375.00   │ │
│ │ +1.2% ▲     │ │ -0.5% ▼     │ │ +0.8% ▲  │ │
│ │ [상세보기]   │ │ [상세보기]   │ │ [상세보기]│ │
│ └──────────────┘ └──────────────┘ └───────────┘ │
└─────────────────────────────────────────────────┘
```

**StockDetail 페이지 레이아웃**:

```
라우트: /research/:ticker

┌─────────────────────────────────────────────────┐
│ ← 뒤로   AAPL — Apple Inc.        [관심종목 추가] │
├──────────┬──────────┬──────────┬──────────┤
│ 현재가   │ 전일대비  │ 52주 고가│ 52주 저가│
│ $178.50  │ +$2.30   │ $199.62  │ $124.17  │
├──────────┴──────────┴──────────┴──────────┤
│ 6개월 가격 차트 (Recharts AreaChart)             │
│ [1주] [1개월] [3개월] [6개월] [1년] 탭          │
├──────────────────────┬────────────────────┤
│ 기업 개요             │ 재무 지표           │
│ 섹터: Technology     │ PER: 28.5          │
│ 업종: Consumer Elec. │ PBR: 45.2          │
│ 시총: $2.8T          │ ROE: 160.1%        │
│ 거래량: 54.2M        │ 부채비율: 176.3%   │
│ 배당수익률: 0.55%    │ 매출성장률: 8.1%   │
│                      │ 애널리스트: Buy     │
│                      │ 목표가: $195.00    │
├──────────────────────┴────────────────────┤
│ 기업 설명                                       │
│ Apple Inc. designs, manufactures, and markets   │
│ smartphones, personal computers...              │
└─────────────────────────────────────────────────┘
```

**검증 포인트**:
- 검색어 입력 시 300ms 디바운스 후 자동완성 표시
- 검색 결과 카드에 실시간 시세 표시
- 상세 페이지에서 가격 차트 렌더링 (기간 탭 전환)
- 재무 지표 테이블에 실제 API 데이터 표시
- "관심종목 추가" 클릭 시 watchlistStore 연동

---

### Step 9: Watchlist 페이지 실데이터 연동

**목표**: 관심종목의 현재가/변동률을 실시간 API 데이터로 전환

**수정 파일**: `src/pages/Watchlist.jsx`

```
변경 사항:
1. useBatchQuotes() 훅으로 관심종목 일괄 시세 조회
2. 관심종목 추가 시 useStockSearch() 기반 검색 연동
3. 각 종목 카드에 실시간 가격/변동률 표시
4. 가격 알림 기준 표시 (±5% 이상: 🟡, ±10% 이상: 🔴)
5. "종목 상세" 링크 → /research/:ticker 연동
```

**검증 포인트**:
- 관심종목의 현재가가 실제 API 데이터
- 종목 추가 시 검색 자동완성 동작
- 급등락 종목에 시각적 표시

---

### Step 10: 에러 처리 + 폴백 + 최종 통합 테스트

**목표**: 네트워크 에러, API 장애 시 안정적 동작 보장

**생성 파일**: `src/components/common/ErrorBoundary.jsx`
**생성 파일**: `src/components/common/ApiStatusBanner.jsx`

```
에러 처리 전략:

1. API 호출 실패 시:
   - TanStack Query retry 2회 → 실패 시 에러 상태
   - 이전 캐시 데이터가 있으면 "오래된 데이터" 라벨과 함께 표시
   - 캐시도 없으면 샘플 데이터로 폴백 + "오프라인 모드" 배너

2. 개별 종목 실패 시:
   - 실패한 종목만 "시세 조회 실패" 표시
   - 나머지 종목은 정상 표시

3. 환율 API 실패 시:
   - localStorage에 저장된 마지막 환율 사용
   - 기본값: 1350 KRW/USD

4. 네트워크 완전 차단 시:
   - navigator.onLine 감지
   - "오프라인입니다" 배너 표시
   - 마지막 캐시 데이터로 UI 유지
```

**ApiStatusBanner 컴포넌트**:

```
┌─────────────────────────────────────────────────┐
│ ⚠️ 일부 시세 데이터를 가져오지 못했습니다.          │
│    마지막 정상 조회: 14:28:30 | [다시 시도]        │
└─────────────────────────────────────────────────┘
```

**최종 통합 테스트 시나리오**:

| # | 시나리오 | 기대 결과 |
|---|---------|----------|
| 1 | 로그인 → Dashboard | 실시간 KPI + 차트 표시 |
| 2 | Portfolio → 종목 추가 | 검색 자동완성 → 현재가 자동반영 |
| 3 | Research → 검색 → 상세 | 재무지표 + 차트 정상 |
| 4 | Watchlist → 관심종목 | 실시간 시세 업데이트 |
| 5 | 네트워크 끊기 | 오프라인 배너 + 캐시 데이터 유지 |
| 6 | API 장애 | 에러 배너 + 이전 데이터 표시 |
| 7 | 30초 대기 | 자동 리페치 동작 (DevTools 확인) |

**검증 포인트**:
- 전체 페이지에서 콘솔 에러 없음
- API 실패 시 앱 크래시 없음 (ErrorBoundary 동작)
- 네트워크 복구 시 자동 리페치 동작

---

## 신규/수정 파일 목록

| 구분 | 파일 | 설명 |
|------|------|------|
| 신규 | `src/hooks/useStockData.js` | TanStack Query 커스텀 훅 6종 |
| 신규 | `src/hooks/useDebounce.js` | 검색 디바운스 훅 |
| 신규 | `src/pages/StockDetail.jsx` | 종목 상세 분석 페이지 |
| 신규 | `src/components/common/ErrorBoundary.jsx` | React 에러 바운더리 |
| 신규 | `src/components/common/ApiStatusBanner.jsx` | API 상태 알림 배너 |
| 수정 | `src/main.jsx` | QueryClientProvider 래핑 |
| 수정 | `vite.config.js` | Yahoo Finance 프록시 설정 |
| 수정 | `src/api/stockApi.js` | API 래퍼 함수 전체 구현 |
| 수정 | `src/store/portfolioStore.js` | 동적 환율, updateAllPrices 추가 |
| 수정 | `src/pages/Dashboard.jsx` | 실데이터 연동 |
| 수정 | `src/pages/Portfolio.jsx` | 실데이터 연동 |
| 수정 | `src/pages/Research.jsx` | 검색 기능 완성 |
| 수정 | `src/pages/Watchlist.jsx` | 실데이터 연동 |
| 수정 | `src/components/portfolio/PortfolioTable.jsx` | 실시간 가격 표시 |
| 수정 | `src/components/portfolio/AddStockModal.jsx` | 검색 자동완성 |
| 수정 | `src/App.jsx` | StockDetail 라우트 추가 |
| 수정 | `package.json` | devtools 패키지 추가 |

---

## Phase 3 완료 기준

| # | 기준 | 충족 조건 |
|---|------|----------|
| 1 | API 연동 | Yahoo Finance API를 통해 실시간 시세 조회 성공 |
| 2 | 캐싱 | TanStack Query DevTools에서 캐시 히트/미스 확인 |
| 3 | 자동 갱신 | 장중 30~60초, 장외 5분 간격 자동 폴링 동작 |
| 4 | 환율 | USD/KRW 환율이 실시간으로 반영 |
| 5 | Dashboard | 모든 KPI가 실제 API 데이터 기반 |
| 6 | Portfolio | 보유 종목 현재가가 실시간 갱신 |
| 7 | Research | 종목 검색 + 상세 분석 페이지 완성 |
| 8 | Watchlist | 관심종목 실시간 시세 표시 |
| 9 | 에러 처리 | API 실패 시 앱 크래시 없음 |
| 10 | 오프라인 | 네트워크 끊김 시 캐시 데이터로 UI 유지 |

---

*작성일: 2026-04-02*
*참조: [00.02 master-plan.md](./00.02%20master-plan.md), [00.02.02 detail-plan.md](./00.02.02%20detail-plan.md)*
