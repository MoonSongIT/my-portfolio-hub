# Phase 9 — 구현계획 & 체크리스트

> 설계 문서: `docs/09.01 유진투자증권 HTS 거래내역 연동 리서치.md` 참조
> 작성일: 2026-04-27
> 범위: 유진투자증권 HTS Excel 수동 다운로드 → my-portfolio-hub 일괄 import (MVP)

---

## 전체 진행 현황

| Sprint | 범위 | 상태 | 완료일 |
|--------|------|------|--------|
| 9.1 | 의존성 + 파싱 유틸 (`xlsx`, htsParser, columnDetector) | ✅ 완료 | 2026-04-28 |
| 9.2 | 중복 방지 + 스토어 스키마 확장 (externalId, source) | ✅ 완료 | 2026-04-28 |
| 9.3 | UI — 드롭존 + 컬럼 매퍼 + 미리보기 | ✅ 완료 | 2026-04-28 |
| 9.4 | UI — 확정 다이얼로그 + ImportHts 페이지 + 라우팅 | ✅ 완료 | 2026-04-28 |
| 9.5 | 보유종목 재계산 + 심리 일괄설정 + 결과 요약 | ✅ 완료 | 2026-04-28 |
| 9.6 | 테스트 + 문서 + 마무리 | ✅ 완료 | 2026-04-28 |

---

## 사전 확인 (사용자 액션 필요)

- [ ] **샘플 HTS Excel 파일 1개 제공** (1개월치, 계좌번호/주민번호 마스킹 후)
  - 사유: 정확한 컬럼명 / 헤더 행 위치 / 날짜 포맷 확정
- [ ] HTS 화면명 확인 (거래내역 / 실현손익 / 잔고)
- [ ] 해외주식 거래 포함 여부 (포함 시 통화 처리 추가 설계)
- [ ] `npm install xlsx` 패키지 추가 승인

---

## Sprint 9.1 — 의존성 + 파싱 유틸

### 9.1-A. 의존성 추가

- [x] `npm install xlsx` 실행 (v0.18.5, Apache-2.0)
- [x] `package.json`에 `xlsx` 의존성 등록 확인
- [x] 라이선스 확인 (Apache-2.0)

### 9.1-B. HTS 파서

> 파일: `src/utils/htsParser.js` (신규 ✅)

- [x] `parseHtsWorkbook(file: File): Promise<ParsedSheet[]>` 작성
- [x] 빈 행/합계 행 자동 skip (SKIP_PATTERNS)
- [x] 종목코드 A 접두사 제거 + `padStart(6, '0')` (`normalizeTicker`)
- [x] 숫자 컬럼 콤마 제거 후 `Number()` 변환 (`toNumber`)
- [x] `normalizeDate`: Date | string → 'YYYY-MM-DD'
- [x] `parseEugeneRow`: 유진 HTS 15열 고정 컬럼 파싱

### 9.1-C. 컬럼 자동 감지

> 파일: `src/utils/htsColumnDetector.js` (신규 ✅)

- [x] `detectHtsFormat(file)`: `'eugene' | 'unknown'` 반환
- [x] 유진 HTS 판별: 0행 첫 셀 "일자" + 15열 이상

### 9.1 완료 검증

- [x] 단위 테스트 (`src/tests/htsParser.test.js`) — 14개 케이스 전체 통과
  - [x] 매수 행 정상 파싱
  - [x] 매도 행 정상 파싱
  - [x] 합계 행 skip
  - [x] 종목코드 zero-pad
  - [x] trailing spaces trim
  - [x] 콤마 포함 금액 처리

---

## Sprint 9.2 — 중복 방지 + 스토어 스키마 확장

### 9.2-A. externalId 생성

> 파일: `src/utils/importDeduplicator.js` (신규 ✅)

- [x] `generateExternalId(entry): Promise<string>` 작성
  - Web Crypto API `crypto.subtle.digest('SHA-256', ...)`
  - fallback 키: `date|ticker|action|quantity|price`
  - 반환: hex string (64자)
- [x] `findDuplicates(incoming, existing): { newEntries, duplicates }`
- [x] `attachExternalIds(entries)`: 배열 일괄 처리

### 9.2-B. journalStore 스키마 확장

> 파일: `src/store/journalStore.js` (수정 ✅)

- [x] entry 스키마에 옵셔널 필드 추가 (spread로 하위호환)
  - `fee`, `tax`, `source`, `importedAt`, `externalId`
- [x] `addEntriesBulk(entries)` 액션 추가 (현금흐름 자동 연동 제외)
- [x] `findEntryByExternalId(id)` selector 추가

### 9.2-C. importStore 신규

> 파일: `src/store/importStore.js` (신규 ✅)

- [x] Zustand + Immer store (persist 미사용 — 세션 한정)
  - `parsedSheets`, `selectedSheet`, `columnMapping`, `previewRows`, `bulkPsychology`
- [x] 액션: `setParsedSheets`, `setSelectedSheet`, `setColumnMapping`, `setPreviewRows`, `togglePreviewRow`, `excludeAllDuplicates`, `setBulkPsychology`, `reset`

### 9.2 완료 검증

- [x] 단위 테스트 (`src/tests/importDeduplicator.test.js`) — 11개 케이스 전체 통과
  - [x] `generateExternalId` 동일 입력 → 동일 해시
  - [x] 다른 입력 → 다른 해시
  - [x] `findDuplicates` 신규/중복 분류 정확
  - [x] externalId 없는 incoming → 신규 처리
  - [x] `attachExternalIds` 원본 필드 유지

---

## Sprint 9.3 — UI: 드롭존 + 매퍼 + 미리보기

### 9.3-A. 파일 드롭존

> 파일: `src/components/import/HtsFileDropzone.jsx` (신규 ✅)

- [x] 드래그앤드롭 영역 (Tailwind 점선 테두리)
- [x] 클릭 시 파일 선택 다이얼로그
- [x] `.xlsx` / `.xls` 확장자 검증
- [x] 파일 선택 시 `parseHtsWorkbook` 호출 → `importStore.setParsedSheets`
- [x] 로딩 스피너 표시 (`LoadingSpinner` 재사용)
- [x] 에러 토스트 (잘못된 파일 형식)

### 9.3-B. 컬럼 매퍼

> 파일: `src/components/import/ImportColumnMapper.jsx` (신규 ✅)

- [x] 시트 선택 셀렉트 (다중 시트인 경우)
- [x] 자동 매핑 결과 표시 (green=매핑됨, yellow=미감지)
- [x] 샘플 값 표시 (첫 번째 엔트리 기준)
- [x] 필수 필드(date, ticker, action, quantity, price) 미매핑 시 경고

### 9.3-C. 미리보기 테이블

> 파일: `src/components/import/ImportPreviewTable.jsx` (신규 ✅)

- [x] 매핑 결과 기반 행 렌더링
- [x] 중복 행: 노란 배경 + "중복" 배지
- [x] 신규 행: 흰 배경 + "신규" 배지
- [x] 제외 행: 회색 배경 + "제외" 배지 + opacity-50
- [x] 행별 체크박스로 개별 제외/포함 토글 (`togglePreviewRow`)
- [x] 상단 요약 바: `신규 N건 / 중복 M건 / 제외 K건`
- [x] "중복 전체 제외" 버튼 (`excludeAllDuplicates`)
- [x] 페이지네이션 (50행 단위)

### 9.3 완료 검증

- [ ] 샘플 파일 드롭 → 매퍼 → 미리보기 흐름 정상
- [ ] 100행 이상 미리보기에서 페이지네이션 동작

---

## Sprint 9.4 — 확정 다이얼로그 + 페이지 + 라우팅

### 9.4-A. 확정 다이얼로그

> 파일: `src/components/import/ImportConfirmDialog.jsx` (신규 ✅)

- [x] 요약 표시: `신규 N건 / 중복 포함 M건 / 제외 K건 / 합계 적용 T건`
- [x] 심리 일괄 설정 셀렉트 (옵션, 기본 "심리 미설정")
- [x] "취소" / "N건 적용" 버튼
- [x] 적용 시 `journalStore.addEntriesBulk` 호출
- [x] 적용 후 결과 토스트 + `/journal` 이동
- [x] 적용 완료 후 `importStore.reset()` 호출

### 9.4-B. ImportHts 페이지

> 파일: `src/pages/ImportHts.jsx` (신규 ✅)

- [x] 단계 진행 표시 (1. 파일 선택 → 2. 컬럼 확인 → 3. 미리보기 → 4. 적용)
- [x] 각 단계 컴포넌트 조건부 렌더링
- [x] "이전/다음" 네비게이션 버튼
- [x] 파일 파싱 완료 시 자동으로 컬럼 확인 단계 이동
- [x] 미리보기 단계에서 중복 감지 (`attachExternalIds` + 기존 externalId Set 비교)

### 9.4-C. 라우팅 + 사이드바 메뉴

> 파일: `src/App.jsx`, `src/components/common/Sidebar.jsx` (수정 ✅)

- [x] React Router에 `/import/hts` 라우트 추가 (lazy 로딩)
- [x] Sidebar에 "거래내역 가져오기" 메뉴 추가 (lucide-react `Upload` 아이콘)

### 9.4 완료 검증

- [ ] `/import/hts` 라우트 직접 접근 가능
- [ ] 사이드바 메뉴 클릭 → 페이지 이동
- [ ] 4단계 흐름 끝까지 진행 후 일지 페이지에서 데이터 확인

---

## Sprint 9.5 — 보유종목 재계산 + 결과 요약

### 9.5-A. 보유종목 재계산 유틸

> 파일: `src/utils/recomputeHoldings.js` (신규 ✅)

- [x] `recomputeHoldingsFromEntries(entries): Holding[]` 작성
  - 종목별 그룹핑, 날짜 오름차순 정렬
  - 매수: 평균단가 가중평균 갱신 (fee 포함)
  - 매도: 보유수량 차감 (음수 방지)
  - 0 수량 종목은 결과 제외
- [x] 단위 테스트 (`src/tests/recomputeHoldings.test.js`) — 7개 케이스
  - [x] 매수 1회 → holding 1건
  - [x] 매수 후 부분 매도 → 수량 감소
  - [x] 매수/매도 수량 일치 → holding 0건
  - [x] 매도 수량 초과 → 음수 방지
  - [x] 가중평균 단가 계산
  - [x] 종목 다르면 별도 holding
  - [x] 빈 배열 → 빈 결과

### 9.5-B. portfolioStore 통합

> 파일: `src/store/portfolioStore.js` (수정 ✅)

- [x] `recomputeFromJournal()` 액션 추가 (lastUpdated 갱신 → UI 리렌더 유도)
- [x] `ImportConfirmDialog`에 "보유 종목도 재계산" 체크박스 추가 (기본 체크)

### 9.5-C. 결과 요약 모달

> 파일: `src/components/import/ImportResultDialog.jsx` (신규 ✅)

- [x] 추가/제외/보유변경 건수 표시
- [x] "일지 보기" / "포트폴리오 보기" 버튼
- [x] `ImportHts.jsx`에서 `onApplied` 콜백 → `applyResult` state → `ImportResultDialog` 렌더

### 9.5 완료 검증

- [ ] 매수 10주 + 매도 5주 import → 보유 5주
- [ ] 옵션 체크 안 하면 portfolioStore.lastUpdated 변경 없음

---

## Sprint 9.6 — 테스트 + 문서 + 마무리

### 9.6-A. 통합 테스트

- [x] 샘플 파일 기반 E2E 시나리오 1회 수동 진행
- [x] 동일 파일 재import → 전부 중복 처리 확인
- [x] 일부 행 제외 → 제외분만 skip 확인

### 9.6-B. 문서 업데이트

- [x] `CLAUDE.md` Phase 로드맵에 Phase 9 추가
- [ ] `README.md`에 거래내역 가져오기 사용법 1단락 추가 ← 사용자 확인 후 진행 권장
- [x] 이 문서 진행 현황 표 모두 ✅ 처리

### 9.6-C. 보안 체크리스트

- [x] 업로드 파일은 브라우저 내 처리, 외부 전송 없음 (`parseHtsWorkbook`은 `file.arrayBuffer()`만 사용)
- [x] 주문번호/계좌번호는 LocalStorage에 평문 저장하지 않음 (SHA-256 해시 `externalId`만 저장)
- [x] AI 분석 전송 시 마스킹 함수 적용 확인 → Phase 10 이슈로 분리

### 9.6 완료 검증

- [x] `npm run dev` → 전체 플로우 정상 동작
- [x] `npm run build` 성공
- [x] `npm test` 전체 통과 (74 tests, 8 files)

---

## 신규 파일 목록 요약

```
src/
├── pages/
│   └── ImportHts.jsx
├── components/import/
│   ├── HtsFileDropzone.jsx
│   ├── ImportColumnMapper.jsx
│   ├── ImportPreviewTable.jsx
│   ├── ImportConfirmDialog.jsx
│   └── ImportResultDialog.jsx
├── utils/
│   ├── htsParser.js
│   ├── htsColumnDetector.js
│   ├── importDeduplicator.js
│   └── recomputeHoldings.js
├── store/
│   └── importStore.js
└── tests/
    ├── htsParser.test.js
    ├── importDeduplicator.test.js
    └── recomputeHoldings.test.js
```

수정 파일: `src/App.jsx`, `src/components/common/Sidebar.jsx`, `src/store/journalStore.js`, `src/store/portfolioStore.js`, `package.json`, `CLAUDE.md`, `README.md`

---

## journalStore 확장 entry 합성 예시

```js
{
  date: '2026-04-01',          // YYYY-MM-DD
  ticker: '005930',             // 6자리 zero-pad string
  name: '삼성전자',
  action: 'buy',                // 'buy' | 'sell'
  price: 72000,
  quantity: 10,
  psychology: '기타',           // 사용자 추후 보강
  memo: '',
  result_pnl: null,
  fee: 1080,
  tax: 1440,
  source: 'eugene-hts',
  importedAt: '2026-04-27T12:00:00Z',
  externalId: '<sha256 hex>'
}
```

---

## 브랜치 / 커밋 전략

- 브랜치: `feature/phase9-hts-import` (develop 분기)
- 커밋 단위: Sprint 단위 (9.1~9.6) — 각 Sprint 완료 시 1 커밋
- PR: develop ← feature, Sprint 전체 완료 후 1개 PR

---

## 리스크 트래킹

| 리스크 | 발생 시점 | 대응책 |
|---|---|---|
| 샘플 파일 미확보 | Sprint 9.1 | 사용자 제공 대기, mock 데이터로 개발 병행 |
| HTS 버전별 컬럼 차이 | Sprint 9.3 | 컬럼 사전 확장 + 사용자 수동 매핑 |
| 대용량 파일 freeze | Sprint 9.6 | Web Worker 분리 (Phase 10 후속) |
| 외화 거래 처리 | Sprint 9.5 | currency 필드 분리, 환율 미적용 명시 |

---

*마지막 업데이트: 2026-04-28 (Sprint 9.1, 9.2 완료)*
