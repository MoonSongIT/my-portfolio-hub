# 14.02.02 관심종목 기능개선 체크리스트

## 작성일
2026-06-05

## 대상 파일
- `src/components/watchlist/TargetPriceBar.jsx`

---

## Step 1 — % 초기값 역산 + 설정 기본값 placeholder 반영

### 코드 수정

- [x] A. `useState` 초기값에서 `targetPct` 역산 추가
  - 조건: `stock.entryPrice != null && stock.targetPrice != null`
  - 값: `pctFromPrices(stock.targetPrice, stock.entryPrice)`
- [x] B. `useState` 초기값에서 `stopLossPct` 역산 추가
  - 조건: `stock.entryPrice != null && stock.stopLoss != null`
  - 값: `pctFromPrices(stock.stopLoss, stock.entryPrice)`
- [x] C. `targetPct` 입력란 placeholder 동적화
  - 변경 전: `"+20"` (하드코딩)
  - 변경 후: `` `+${watchlistDefaults.targetPct}` ``
- [x] D. `stopLossPct` 입력란 placeholder 동적화
  - 변경 전: `"-10"` (하드코딩)
  - 변경 후: `` `-${watchlistDefaults.stopLossPct}` ``

### 수작업 검증 (개발서버 실행 후)

- [ ] E. 목표가+매입가가 저장된 종목 카드 → 목표가 설정 펼치기 → targetPct 란에 역산 % 표시 확인
- [ ] F. 손절가+매입가가 저장된 종목 카드 → 목표가 설정 펼치기 → stopLossPct 란에 역산 % 표시 확인
- [ ] G. Settings → 관심종목 기본값 → 목표가 %를 30으로 변경 → 관심종목 카드의 목표가 % placeholder가 "+30"으로 변경 확인
- [ ] H. Settings → 관심종목 기본값 → 손절가 %를 15로 변경 → 관심종목 카드의 손절가 % placeholder가 "-15"으로 변경 확인

---

## Step 2 — 보유 종목 평균매입가 가져오기 버튼

### 코드 수정

- [x] A. `useJournalStore` import 추가
- [x] B. `useMemo`로 포트폴리오 보유 현황에서 해당 ticker의 `avgPrice` 조회
  - `journalStore.computeAllHoldings()`에서 `stock.ticker` 매칭
  - 없으면 `null`
- [x] C. 매입가 입력란 오른쪽에 아이콘 버튼 조건부 렌더링
  - 표시 조건: `portfolioAvgPrice != null`
  - 아이콘: lucide-react `RefreshCw` (또는 `Download`)
  - 버튼 크기: 아이콘 `w-3.5 h-3.5`, 패딩 최소화 (기존 입력란 높이 유지)
  - 툴팁(title 속성): `포트폴리오 평균매입가 ${formatCurrency(portfolioAvgPrice, currency)} 가져오기`
- [x] D. 버튼 클릭 시 `handleEntryChange(String(portfolioAvgPrice))` 호출

### 수작업 검증 (개발서버 실행 후)

- [ ] E. 포트폴리오에 보유 중인 종목 관심종목 카드 → 목표가 설정 펼치기 → 매입가 입력란 오른쪽에 아이콘 버튼 표시 확인
- [ ] F. 포트폴리오에 없는 종목 관심종목 카드 → 목표가 설정 펼치기 → 아이콘 버튼 미표시 확인
- [ ] G. 아이콘 버튼 클릭 → 매입가 입력란에 평균매입가 자동 입력 확인
- [ ] H. 매입가 자동 입력 후 → 목표가·손절가가 설정 기본값 %로 자동 계산되어 표시 확인
- [ ] I. 기존 목표가%가 있는 경우 → 버튼 클릭 후 기존 % 유지하며 가격만 재계산 확인
- [ ] J. 저장 버튼 클릭 → IDB에 매입가·목표가·손절가 저장 확인 (카드 재렌더 후 값 유지)

---

## 전체 흐름 최종 확인

- [ ] K. 보유 종목에서 전체 흐름 1회 테스트
  - 아이콘 버튼 클릭 → 매입가 자동 입력 → 목표가/손절가 자동 계산 → 저장 → 카드에 목표가·달성률 표시 확인
- [ ] L. 비보유 종목에서 수동 매입가 입력 → 목표가/손절가 자동 계산 → 저장 확인 (기존 동작 회귀 없음)

---

## 수작업 검증 환경

```
npm run dev  →  http://localhost:5173
관심종목 탭 (좌측 사이드바 또는 상단 네비게이션)
각 종목 카드 하단 "목표가 설정" 텍스트 클릭 → 패널 펼침
```

---

*작성: Claude Code / 수작업 검증 대기*
