# Phase 5 — 고도화 & PWA 구현 체크리스트

> **작성일**: 2026-04-03
> **Phase**: Phase 5 (최종 마무리)
> **대상**: 모든 Step 완료 여부 확인
> **참조**: [00.02.07 detail-plan.md](./00.02.07%20detail-plan.md)

---

## 📋 실행 체크리스트

### ✅ Step 1: lightweight-charts 캔들스틱 차트

#### 1.1 Package 설치

- [X] `npm install lightweight-charts` 실행 완료
- [X] `package.json`에 `"lightweight-charts": "^5.1.0"` 기록됨 (v5 최신)
- [X] `node_modules/lightweight-charts` 폴더 생성됨

#### 1.2 CandlestickChart 컴포넌트 구현

- [X] `src/components/charts/CandlestickChart.jsx` 파일 생성
- [X] `createChart()` 기본 차트 생성 구현
- [X] **캔들스틱 시리즈** 추가 (upColor, downColor 설정)
  - [X] 양봉 색상: #26a69a (초록)
  - [X] 음봉 색상: #ef5350 (빨강)
- [X] **이동평균선** 3개 추가
  - [X] SMA 20일선 (주황 #FF9800)
  - [X] SMA 60일선 (파랑 #2196F3)
  - [X] SMA 120일선 (보라 #9C27B0)
- [X] **거래량 히스토그램** 추가 (별도 패널 — `chart.addPane()`)
  - [X] 거래량 색상 양/음봉에 맞춤 (투명도 70%)
- [X] **시간축 자동 스케일** (`chart.timeScale().fitContent()`)
- [X] **창 리사이징** 이벤트 리스너 추가
  - [X] 컴포넌트 언마운트 시 이벤트 리스너 제거
  - [X] 메모리 누수 방지 (`chart.remove()`)
- [X] 다크모드 대응 (`useSettingsStore` 연동)
- [X] SMA 범례 표시 (컴포넌트 상단)

#### 1.3 Helper 함수 구현

- [X] `calculateSMA(data, period)` 함수 구현
  - [X] 입력 데이터 배열 처리
  - [X] 이동평균 계산 로직 검증
  - [X] null 값 필터링 (`.filter(Boolean)`)

#### 1.4 API 연동 (stockApi.js)

- [X] 기존 `fetchHistory()` 함수가 OHLCV 포맷 반환 (재사용)
- [X] 응답 데이터 포맷 검증 완료 (date, open, high, low, close, volume)

#### 1.5 StockDetail 페이지 통합 (Research → StockDetail 구조에 맞게 변경)

- [X] `src/pages/StockDetail.jsx` 수정
- [X] `CandlestickChart` 컴포넌트 import
- [X] 라인/캔들 토글 버튼 추가 (`chartType` state)
- [X] 기존 AreaChart와 CandlestickChart 조건부 렌더링

#### 1.6 브라우저 테스트

- [X] `npm run dev` 실행
- [X] 종목 검색 후 상세 페이지(`/research/:ticker`) 이동
- [X] "캔들" 버튼 클릭 → 캔들스틱 차트 렌더링 확인
- [X] 이동평균선 3개 표시 확인
- [X] 거래량 히스토그램 표시 확인
- [X] 창 리사이징 시 차트 크기 조정 확인
- [X] 기간 변경 시 데이터 업데이트 확인
- [X] "라인" 버튼 클릭 → AreaChart로 전환 확인
- [X] 다크모드에서 차트 색상 확인
- [X] 모바일 반응형 확인 (375px 너비)

---

### ✅ Step 2: IndexedDB (Dexie.js) 구현

#### 2.1 Package 설치

- [X] `npm install dexie` 실행 완료
- [X] `package.json`에 `"dexie": "^4.4.2"` 기록됨

#### 2.2 Database 스키마 정의

- [X] `src/utils/db.js` 파일 생성
- [X] `Dexie` 인스턴스 생성 (`PortfolioHub`)
- [X] **테이블 스키마** 정의
  - [X] `transactions`: `&id, ticker, action, date, accountId` (UUID primary key)
  - [X] `priceHistory`: `++id, ticker, date`
  - [X] `reports`: `++id, type, createdAt`
- [X] 버전 관리 (`db.version(1).stores(...)`)

#### 2.3 CRUD 헬퍼 함수 구현

- [X] `addTransaction(entry)` 구현 (put = upsert)
- [X] `updateTransaction(id, updates)` 구현
- [X] `deleteTransaction(id)` 구현
- [X] `getAllTransactions()` 구현
- [X] `getTransactionsByTicker(ticker)` 구현
- [X] `getTransactionsByDate(startDate, endDate)` 구현
- [X] `getPriceHistory(ticker, days)` 구현
- [X] `savePriceHistory(ticker, historyData)` 구현 (bulkPut)
- [X] `saveReport(reportData)` 구현
- [X] `getReportsByType(type)` 구현
- [X] `isCacheValid(ticker, hours)` 구현 (캐시 유효성 검사)

#### 2.4 Zustand Store 연동

- [X] `src/store/journalStore.js` 수정
- [X] `addEntry` — IndexedDB fire-and-forget 추가 (localStorage 백업 유지)
- [X] `updateEntry` — IndexedDB 동기화
- [X] `deleteEntry` — IndexedDB 동기화
- [X] `clearEntries` — IndexedDB clear 동기화
- [X] `loadFromDB()` 함수 구현 (IndexedDB → store 로드)
- [X] `persist` 미들웨어 유지 (LocalStorage 이중 백업)

#### 2.5 App.jsx에서 초기화

- [X] `App.jsx` 수정
- [X] `useEffect` 훅에서 `loadFromDB()` 호출
  - [X] 앱 시작 시 IndexedDB에서 데이터 로드
  - [X] 의존성 배열: `[]` (최초 1회만)

#### 2.6 DevTools 설정 (선택)

- [X] 브라우저 DevTools에서 IndexedDB 탭 확인
  - [X] PortfolioHub 데이터베이스 생성됨
  - [X] 3개 테이블 생성됨

#### 2.7 브라우저 테스트

- [X] 매매 일지 추가 시 IndexedDB에 저장 확인
- [X] 브라우저 개발자 도구 → Application → IndexedDB 확인
  - [X] PortfolioHub 데이터베이스 존재
  - [X] transactions 테이블에 데이터 저장됨
- [X] 페이지 새로고침 후 데이터 복구 확인
- [X] 로컬스토리지와 IndexedDB 동시 저장 확인

---

### ✅ Step 3: Reports 페이지 완성

#### 3.1 Reports 페이지 레이아웃

- [X] `src/pages/Reports.jsx` 전면 개편
- [X] 페이지 구조 구성
  - [X] 제목 + 기간 선택 버튼 (오늘/1주/1개월/1년)
  - [X] KPI 카드 4개 (총 수익률, KOSPI 대비, 거래 횟수, 승률)
  - [X] Tabs 컴포넌트 (성과 추이 / 거래 내역 / AI 인사이트)
- [X] shadcn/ui `Card`, `Tabs`, `Button` import
- [X] 기간 선택 버튼 클릭 시 `dateRange` 상태 변경

#### 3.2 PerformanceChart (Reports.jsx 내 인라인)

- [X] Recharts `LineChart` 구성
- [X] 내 포트폴리오 (파랑) / KOSPI (초록) / S&P500 (노랑)
- [X] Tooltip 포맷 `+x.xx%`
- [X] Legend 표시

#### 3.3 TradeHistoryTable 컴포넌트

- [X] `src/components/reports/TradeHistoryTable.jsx` 생성
- [X] 컬럼: 날짜, 종목, 구분(매수/매도), 가격, 수량, 심리, 손익
- [X] 매수/매도 색상 구분 (파랑/빨강 뱃지)
- [X] 손익 색상 구분 (초록/빨강)
- [X] 요약 카드 4개 (매수, 매도, 승률, 실현손익)
- [X] `calculateSummary()` 헬퍼 함수 구현

#### 3.4 InsightsCard 컴포넌트 (ReportAgent 연동)

- [X] `src/components/reports/InsightsCard.jsx` 생성
- [X] "AI 인사이트 생성" 버튼
- [X] 클릭 시 `sendToAgent(msg, context, 'report')` 호출
- [X] 로딩 상태 (`RefreshCw` 스피너)
- [X] 응답 마크다운 렌더링 (굵게/줄바꿈)
- [X] 면책 문구 포함

#### 3.5 계산 헬퍼 함수 (utils/calculator.js)

- [X] `getStartDate(dateRange)` 구현
- [X] `filterByDateRange(entries, dateRange)` 구현
- [X] `calculateWinRate(entries)` 구현

#### 3.6 브라우저 테스트

- [X] `/reports` 페이지 이동
- [X] KPI 카드 4개 데이터 표시 확인
- [X] 기간 버튼 클릭 시 거래 횟수·승률 갱신 확인
- [X] "성과 추이" 탭 라인 차트 표시 확인
- [X] "거래 내역" 탭 테이블 표시 확인
- [X] "AI 인사이트" 탭 → 버튼 클릭 → 응답 표시 확인
- [X] 모바일 반응형 확인

---

### ✅ Step 4: Watchlist 완성 + AlertAgent

#### 4.1 Watchlist 페이지 UI

- [X] `src/pages/Watchlist.jsx` 수정
- [X] 페이지 구조
  - [X] 제목 + 관심종목 추가 버튼
  - [X] Dialog 모달 (종목 추가)
- [X] 관심종목 카드/테이블 (뷰 전환 지원)
  - [X] 컬럼: 종목명, 현재가, 변동률, 액션(상세보기, 알림 설정, 삭제)
  - [X] 변동률 색상: 양수(초록), 음수(빨강)
  - [X] "알림 설정" 버튼 → AlarmDialog 오픈
  - [X] "삭제" 버튼 → watchlist에서 제거
  - [X] 알림 설정된 종목에 Bell + 개수 배지 표시

#### 4.2 실시간 시세 연동

- [X] TanStack Query로 관심종목 실시간 가격 조회 (`useBatchQuotes`)
- [X] 현재가 표시
- [X] 변동률(%) 표시
- [X] ±5% 이상: 주의 배지, ±10% 이상: 긴급 배지

#### 4.3 Alert 스토어 구현

- [X] `src/store/watchlistStore.js` 수정
- [X] `alerts[]` 상태 추가
- [X] `addAlert(alert)` 구현 (uuid, createdAt, triggered 자동 부여)
- [X] `removeAlert(alertId)` 구현
- [X] `checkAlerts(priceMap)` 구현 (조건 충족 알림 배열 반환)
- [X] `removeFromWatchlist` — 연결 알림 동시 삭제
- [X] `clearWatchlist` — 알림 함께 초기화
- [X] persist version 4 + migration

#### 4.4 AlarmDialog 컴포넌트

- [X] `src/components/watchlist/AlarmDialog.jsx` 생성
- [X] 이상/이하 조건 버튼 토글 (색상 구분)
- [X] 목표가 Input (숫자 입력, Enter 지원)
- [X] "알림 추가" 버튼 → store에 추가
- [X] 등록된 알림 목록 (삭제 버튼 포함)

#### 4.5 활성 알림 요약 배너

- [X] Watchlist 페이지 하단에 "활성 가격 알림 (N개)" 배너
- [X] 각 알림을 pill 형태로 표시 (종목 + 조건 + 목표가)
- [X] 알림 없으면 배너 미표시

#### 4.6 오늘 브리핑 (AlertAgent)

- [X] "오늘 브리핑" 버튼 → ChatPanel 오픈 (forceAgent="alert")
- [X] 초기 메시지: "오늘 관심종목 시장 브리핑해줘" 자동 전송

#### 4.7 브라우저 테스트

- [X] `/watchlist` 페이지 이동
- [X] 관심종목 추가 동작 확인
- [X] 카드/테이블 뷰 전환 확인
- [X] 실시간 가격 업데이트 확인
- [X] "알림 설정" 버튼 → AlarmDialog 열림 확인
- [X] 목표가 입력 후 "알림 추가" → 목록 표시 확인
- [X] 알림 삭제 버튼 동작 확인
- [X] 알림 배지(Bell + 개수) 카드/테이블에 표시 확인
- [X] 하단 활성 알림 배너 표시 확인
- [X] "오늘 브리핑" 버튼 → ChatPanel 열림 확인
- [X] 삭제 버튼 클릭 시 목록에서 제거 확인

---

### ✅ Step 5: PWA 설정 & Offline Support

#### 5.1 vite-plugin-pwa 설치

- [X] `npm install -D vite-plugin-pwa workbox-window` 실행
- [X] `package.json`에 기록됨

#### 5.2 vite.config.js 설정

- [X] `import { VitePWA } from 'vite-plugin-pwa'` 추가
- [X] `VitePWA` 플러그인 추가
  - [X] `registerType: 'autoUpdate'`
  - [X] `workbox` 설정:
    - [X] `globPatterns`: js, css, html, 이미지 파일 캐싱
    - [X] `runtimeCaching`:
      - [X] Yahoo Finance API: StaleWhileRevalidate (5분 캐시)
      - [X] 로컬 API 프록시: NetworkFirst (5초 타임아웃)
  - [X] `manifest` 설정:
    - [X] name, short_name, description
    - [X] theme_color (#1d4ed8), background_color (#0f172a)
    - [X] display: 'standalone'
    - [X] start_url: '/'
    - [X] icons (64/192/512 + maskable)

#### 5.3 앱 아이콘 준비

- [X] `@vite-pwa/assets-generator`로 자동 생성
  - [X] `public/pwa-64x64.png`
  - [X] `public/pwa-192x192.png`
  - [X] `public/pwa-512x512.png`
  - [X] `public/maskable-icon-512x512.png`
  - [X] `public/apple-touch-icon-180x180.png`
  - [X] `public/favicon.ico`

#### 5.4 App.jsx에서 PWA 초기화

- [X] `import { useRegisterSW } from 'virtual:pwa-register/react'` 추가
- [X] `useRegisterSW()` 훅 사용
- [X] `needRefresh` 상태 시 업데이트 배너 렌더링
- [X] `onOfflineReady()` 콜백: 콘솔 로그

#### 5.5 서비스 워커 생성 확인

- [X] `npm run build` 실행 완료
- [X] `dist/sw.js` 생성됨 (24 entries, 1583 KiB precache)
- [X] `dist/manifest.webmanifest` 생성됨
- [X] `dist/workbox-*.js` 생성됨

#### 5.6 웹 앱 매니페스트 검증

- [X] 브라우저 DevTools → Application → Manifest
  - [ ] name, short_name 표시됨
  - [ ] icons 배열 표시됨
  - [ ] display: 'standalone' 확인

#### 5.7 PWA 설치 테스트

- [X] `npm run preview` 로컬 서빙
- [X] Chrome 주소창에 설치 버튼 확인
- [X] 설치 후 앱 아이콘으로 실행 확인

#### 5.8 오프라인 동작 테스트

- [X] Chrome DevTools → Network → Offline
- [X] 캐시된 페이지 표시 확인
- [X] 로컬 데이터 표시 확인

#### 5.9 브라우저 테스트

- [X] `npm run preview` 실행 후 브라우저 확인
- [X] Application 탭 → Manifest / Service Workers 확인
- [X] Offline 체크 후 앱 동작 확인

---

### ✅ Step 6: 성능 최적화 & 마무리

#### 6.1 코드 스플리팅 (React.lazy + Suspense)

- [X] `src/App.jsx` 수정
- [X] 각 페이지 컴포넌트를 `lazy()` 감싸기
  - [X] Dashboard, Portfolio, Research, Journal, Watchlist, Reports, StockDetail, AIChat, Login
- [X] 라우트에 `<Suspense fallback={<LoadingSpinner />}>` 적용 (2중 — 앱 전체 + 내부 라우트)
- [X] `LoadingSpinner` 컴포넌트 확인

#### 6.2 차트 라이브러리 분리

- [X] `vite.config.js` `manualChunks` 설정
  - [X] `vendor-react`: react, react-dom, react-router-dom
  - [X] `vendor-query`: @tanstack/react-query
  - [X] `vendor-chart-recharts`: recharts (422 kB / gzip 112 kB)
  - [X] `vendor-chart-lw`: lightweight-charts (165 kB / gzip 52 kB)
  - [X] `vendor-zustand`: zustand, immer
- [X] 메인 번들 863 kB → **451 kB (gzip 151 kB)** — 45% 감소

#### 6.3 Lighthouse 성능 점수 확인

- [X] `npm run preview` 실행
- [X] Chrome DevTools → Lighthouse
- [X] 4개 카테고리 점수 검사:
  - [X] **Performance** > 90 → **99** ✅ (FCP 0.7s, LCP 0.8s, TBT 0ms, CLS 0.001)
  - [X] **Accessibility** > 90 → **93** ✅ (button-name 3건 미적용: 사이드바 토글, 햄버거 메뉴, 테마 토글)
  - [X] **Best Practices** > 90 → **100** ✅
  - [X] **SEO** > 90 → **91** ✅ (robots.txt 미존재 — 로컬 개발 환경, 배포 시 추가 예정)
- [X] PWA 항목 확인
  - [X] Service Worker 등록됨
  - [X] Web app manifest 설정됨

#### 6.4 번들 분석 (선택)

- [ ] `npm install -D rollup-plugin-visualizer` 설치
- [ ] `vite.config.js`에 visualizer 플러그인 추가
- [ ] `npm run build` 실행 후 `dist/stats.html` 확인
  - [ ] 가장 큰 청크 확인
  - [ ] 불필요한 의존성 식별

#### 6.5 반응형 디자인 점검

- [ ] 모바일 (375px)
  - [ ] 사이드바 → 바텀 네비게이션 전환 확인
  - [ ] 터치 타겟 크기: 최소 44×44px
  - [ ] 텍스트 가독성 (폰트 크기 >= 12px)
  - [ ] 수평 스크롤 없음
  - [ ] 모든 페이지 테스트 (Dashboard, Portfolio, Reports 등)
- [ ] 태블릿 (768px)
  - [ ] 2열 레이아웃 적용 확인
  - [ ] 차트 가독성 확인
  - [ ] 테이블 가로 스크롤 가능 여부
- [ ] 데스크톱 (1920px)
  - [ ] 사이드바 + 메인 콘텐츠 3열 구성 확인
  - [ ] 여백 적절성 확인

#### 6.6 다크모드 전체 점검

- [ ] Settings에서 다크모드 활성화
- [ ] 모든 페이지에서 다크모드 확인:
  - [ ] 텍스트 색상 (흰색 or 밝은 회색)
  - [ ] 배경색 (어두운 회색)
  - [ ] 입력 필드 색상
  - [ ] 차트 배경색 및 텍스트 색상
  - [ ] 버튼 색상
- [ ] WCAG AA 대비도 기준 확인
  - [ ] 일반 텍스트: 최소 4.5:1
  - [ ] 큰 텍스트: 최소 3:1

#### 6.7 성능 메트릭 최종 확인

- [ ] Core Web Vitals 측정:
  - [ ] **LCP (Largest Contentful Paint)** < 2.5s
  - [ ] **FID (First Input Delay)** < 100ms (또는 INP < 200ms)
  - [ ] **CLS (Cumulative Layout Shift)** < 0.1
- [ ] 번들 크기 확인:
  - [ ] Main bundle < 200KB (gzip)
  - [ ] 총 용량 < 1MB (gzip)

#### 6.8 최종 테스트 및 문서화

- [ ] 모든 기능 동작 확인
  - [ ] 포트폴리오 CRUD
  - [ ] 매매 일지 기록 및 조회
  - [ ] AI 채팅 응답
  - [ ] 보고서 생성
  - [ ] 관심종목 알림
  - [ ] PWA 설치 및 오프라인 동작
- [ ] 오류 로그 확인
  - [ ] 콘솔 에러 없음
  - [ ] 네트워크 오류 없음 (대기 중인 요청 제외)
- [ ] 버그 리포트 및 수정

#### 6.9 Git 최종 커밋

- [X] 모든 파일 스테이징
  ```bash
  git add .
  ```
- [X] 커밋 메시지 작성
  ```bash
  git commit -m "feat: Phase 5 고도화 완성 — 캔들스틱 차트, IndexedDB, PWA, 성능 최적화"
  ```
- [X] develop 브랜치에 푸시
  ```bash
  git push origin develop
  ```
- [X] GitHub에서 PR 생성 (develop → main)
  - [ ] PR 설명 작성
  - [ ] 리뷰 요청

#### 6.10 배포 (선택)

- [ ] Vercel에 연동된 GitHub 저장소 확인
- [ ] main 브랜치에 머지 후 자동 배포 확인
- [ ] 프로덕션 URL 접속:
  - [ ] `https://my-portfolio-hub-xxx.vercel.app/`
- [ ] 프로덕션 Lighthouse 점수 확인
  - [ ] 모든 지표 > 90 확인

---

## 📊 진행도 추적

| Step                   | 상태                    | 완료 날짜  | 담당자 | 비고                                    |
| ---------------------- | ----------------------- | ---------- | ------ | --------------------------------------- |
| 1: lightweight-charts  | 🔄 브라우저 테스트 대기 | 2026-04-03 | Claude | v5.1.0 설치, StockDetail 통합 완료      |
| 2: IndexedDB           | 🔄 브라우저 테스트 대기 | 2026-04-03 | Claude | Dexie 4.4.2, dual-write 구현 완료       |
| 3: Reports 페이지      | 🔄 브라우저 테스트 대기 | 2026-04-03 | Claude | Tabs + KPI 4개 + AI 인사이트 완성       |
| 4: Watchlist + Alert   | 🔄 브라우저 테스트 대기 | 2026-04-03 | Claude | AlarmDialog + 알림 배너 + 브리핑 완성   |
| 5: PWA 설정            | 🔄 브라우저 테스트 대기 | 2026-04-03 | Claude | vite-plugin-pwa + workbox + 아이콘 완성 |
| 6: 성능 최적화         | 🔄 Lighthouse 대기      | 2026-04-03 | Claude | lazy 코드스플리팅 + manualChunks 완성   |
| **Phase 5 완료** | ⬜                      | -          | -      | -                                       |

---

## 🎯 최종 검증 항목

### 기능 검증

- [ ] 캔들스틱 차트 렌더링 + 이동평균선 표시
- [ ] IndexedDB 데이터 저장 및 복구
- [ ] Reports 페이지 성과 분석 차트 및 테이블
- [ ] Watchlist 실시간 시세 + 알림 설정
- [ ] PWA 설치 가능 + 오프라인 동작
- [ ] AI 채팅 응답 (ReportAgent, AlertAgent)

### 성능 검증

- [ ] Lighthouse Performance > 90
- [ ] Lighthouse Accessibility > 90
- [ ] Lighthouse Best Practices > 90
- [ ] Lighthouse SEO > 90
- [ ] LCP < 2.5s
- [ ] 번들 크기 < 200KB (gzip)

### 호환성 검증

- [ ] Chrome (최신)
- [ ] Firefox (최신)
- [ ] Safari (최신)
- [ ] iOS Safari (PWA)
- [ ] Android Chrome (PWA)
- [ ] 모바일 반응형 (375px, 768px, 1920px)
- [ ] 다크모드

### 보안 검증

- [ ] HTTPS 사용 (프로덕션)
- [ ] XSS 대책 (React 기본 + 사용자 입력 검증)
- [ ] CSRF 대책 (API 호출)
- [ ] 민감 정보 로컬 저장 (API 키 하드코딩 없음)
- [ ] 사용자 데이터 로컬 또는 IndexedDB (프라이빗)

---

## 📝 주의사항

⚠️ **각 Step별 테스트 순서 준수**

- Step 1 → 2 → 3 → 4 → 5 → 6 순서로 진행
- 이전 Step 완료 후 다음 Step 시작

⚠️ **브라우저 캐시 클리어**

- 개발 중 CSS/JS 변경 후 DevTools에서 "Disable cache" 체크
- 또는 하드 새로고침 (Ctrl+Shift+R / Cmd+Shift+R)

⚠️ **PWA 캐시 갱신**

- Service Worker 변경 후 DevTools → Application → Service Workers
- "Unregister" 버튼으로 기존 SW 제거 후 새로고침

⚠️ **성능 측정 환경**

- Lighthouse는 Incognito 모드에서 실행 (더 정확한 결과)
- 로컬 개발 환경이 아닌 프로덕션 빌드에서 측정

---

## 🔗 참고 문서

- [Phase 5 상세 계획: 00.02.07 detail-plan.md](./00.02.07%20detail-plan.md)
- [마스터 계획: 00.02 master-plan.md](./00.02%20master-plan.md)
- [프로젝트 지침: CLAUDE.md](../CLAUDE.md)

---

*작성: Claude Code | 프로젝트: My Portfolio Hub*
