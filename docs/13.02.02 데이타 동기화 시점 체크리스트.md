# 13.02.02 데이타 동기화 시점 체크리스트

> 작성일: 2026-06-01
> 버전: 1.0
> 설계 참조: `docs/13.02.01 데이타 동기화 시점 설계.md`

---

## S-1: 앱 종료/이탈 시 미동기화 업로드

### S-1-A: syncService 확장

- [ ] **A-1** `src/services/syncService.js` 에 `countPendingRecords()` 추가
  - [ ] `SYNC_TABLE_MAP` 전체 테이블 순회
  - [ ] 각 테이블에서 `syncedAt === null` 건수 합산
  - [ ] 반환값: `number`

- [ ] **A-2** `uploadAll()` 완료 후 `syncStore.resetPending()` 호출 확인
  - [ ] 성공 시 호출됨
  - [ ] 실패 시 호출되지 않음 (대기 상태 유지)

### S-1-B: 커스텀 모달 구현

- [ ] **B-1** `src/components/sync/PendingUploadModal.jsx` 신규 작성
  - [ ] props: `pendingCount`, `onUploadAndLeave`, `onLeave`, `onCancel`
  - [ ] 표시 문구: "서버에 저장되지 않은 변경사항이 {N}개 있습니다."
  - [ ] 버튼 3개: [업로드 후 이동] / [그냥 이동] / [취소]
  - [ ] 업로드 중: 버튼 비활성화 + 스피너 + "업로드 중..." 텍스트
  - [ ] 업로드 성공: 모달 닫힘 + `toast.success("N개 항목이 서버에 저장되었습니다.")`
  - [ ] 업로드 실패: 인라인 오류 메시지 + 버튼 재활성화
    - 오류 메시지에 HTTP 상태코드 또는 원인 포함
    - "변경사항은 로컬 저장소에 보존되어 있습니다." 문구 포함

- [ ] **B-2** 빌드 확인: `npm run build` 에러 없음

### S-1-C: usePendingSync 훅 구현

- [ ] **C-1** `src/hooks/usePendingSync.js` 신규 작성
  - [ ] React Router `useBlocker` 활용 (v6.4+)
  - [ ] 이동 시도 감지 → `countPendingRecords()` 실행
  - [ ] pendingCount > 0 이면 `PendingUploadModal` 표시
  - [ ] pendingCount === 0 이면 이동 허용

- [ ] **C-2** `src/App.jsx` 에서 `usePendingSync` 훅 등록

### S-1-D: useBeforeUnloadSync 훅 구현

- [ ] **D-1** `src/hooks/useBeforeUnloadSync.js` 신규 작성
  - [ ] `beforeunload` 이벤트: `pendingChanges > 0` 이면 `event.preventDefault()` + `event.returnValue = ''`
  - [ ] `pagehide` 이벤트: `navigator.sendBeacon('/api/sync/upload-pending', ...)` 호출 (토큰 포함)

- [ ] **D-2** `src/App.jsx` 에서 `useBeforeUnloadSync` 훅 등록

### S-1-E: syncStore pendingChanges 초기화

- [ ] **E-1** `src/store/syncStore.js` 에 `setPendingChanges(n)` 액션 추가

- [ ] **E-2** `src/App.jsx` 로그인 후 초기화 블록에 `countPendingRecords()` 스캔 + `setPendingChanges()` 호출 추가

### S-1 테스트

- [ ] **S1-T1** 미동기화 레코드 있는 상태에서 페이지 이동
  ```
  1. 앱에서 매매 1건 입력 (서버 업로드 전)
  2. 좌측 메뉴에서 다른 페이지 클릭
  3. PendingUploadModal 표시 확인
     - "변경사항이 1개 있습니다." 문구 표시
     - 버튼 3개 표시
  ```

- [ ] **S1-T2** [업로드 후 이동] 버튼 — 성공 케이스
  ```
  1. PendingUploadModal에서 [업로드 후 이동] 클릭
  2. 버튼 비활성화 + 스피너 표시 확인
  3. 업로드 완료 후:
     - 성공 토스트 표시
     - 모달 닫힘
     - 목적지 페이지로 이동
  4. Supabase 테이블에서 해당 레코드 확인
  ```

- [ ] **S1-T3** [업로드 후 이동] 버튼 — 실패 케이스
  ```
  1. DevTools → Network → Offline 설정 (네트워크 차단)
  2. PendingUploadModal에서 [업로드 후 이동] 클릭
  3. 인라인 오류 메시지 표시 확인
     - HTTP 오류 또는 네트워크 오류 원인 표시
     - "로컬 저장소에 보존" 문구 표시
  4. [그냥 이동] / [재시도] 버튼 활성화 확인
  ```

- [ ] **S1-T4** [그냥 이동] 버튼
  ```
  1. PendingUploadModal에서 [그냥 이동] 클릭
  2. 업로드 없이 목적지 페이지로 이동
  3. DevTools IDB 확인 — syncedAt=null 레코드 유지됨
  ```

- [ ] **S1-T5** [취소] 버튼
  ```
  1. PendingUploadModal에서 [취소] 클릭
  2. 모달 닫힘
  3. 현재 페이지 유지 (이동 없음)
  ```

- [ ] **S1-T6** 미동기화 레코드 없는 경우 모달 미표시
  ```
  1. 전체 업로드 완료 후 (syncedAt 모두 설정됨)
  2. 페이지 이동 시도
  3. 모달 표시되지 않고 즉시 이동
  ```

- [ ] **S1-T7** 브라우저 탭 닫기 — 기본 확인창 표시
  ```
  1. 미동기화 레코드 있는 상태에서 브라우저 탭 닫기 시도 (X 버튼)
  2. 브라우저 기본 "페이지에서 나가시겠습니까?" 확인창 표시 확인
  ```

- [ ] **S1-T8** 앱 초기화 시 pendingChanges 정확성
  ```
  1. 미동기화 레코드 N건 있는 상태에서 앱 새로고침
  2. 콘솔: useSyncStore.getState().pendingChanges
  3. N과 일치하는지 확인
  ```

---

## S-2: 로그인 후 증분 다운로드

### S-2-A: syncService 증분 다운로드 함수 추가

- [ ] **A-1** `downloadTable(localTable, since)` 에 `since` 파라미터 추가
  - [ ] `since` 있으면 `?table=xxx&since=ISO_STRING` 으로 URL 구성
  - [ ] `since` 없으면 기존 `?table=xxx` 유지 (하위 호환)

- [ ] **A-2** `downloadAllDelta(since)` 함수 신규 추가
  - [ ] `SYNC_TABLE_MAP` 전체 테이블 순회
  - [ ] 각 테이블에 `downloadTable(local, since)` 호출
  - [ ] 총 downloaded 건수 집계
  - [ ] 완료 후 `setLastSyncedAt(now)` 호출
  - [ ] 반환값: `{ total: number }`

- [ ] **A-3** `downloadDeltaAndReload(userId, since)` 함수 신규 추가
  - [ ] `downloadAllDelta(since)` 호출
  - [ ] total > 0 이면 5개 스토어 `loadFromDB(userId)` 재로드
  - [ ] 반환값: `{ total: number }`

### S-2-B: App.jsx 로그인 후 처리 변경

- [ ] **B-1** 기존 `txCount === 0` 조건 블록 제거

- [ ] **B-2** 아래 로직으로 교체
  ```javascript
  const { lastSyncedAt } = useSyncStore.getState()
  const since = lastSyncedAt
    ? new Date(new Date(lastSyncedAt).getTime() - 5 * 60 * 1000).toISOString()
    : null

  if (!since) {
    syncService.downloadAndReload(session.user.id)
      .then(({ total }) => {
        if (total > 0) toast.success(`서버에서 ${total}개 항목을 받아왔습니다.`)
      })
      .catch(err => console.warn('[Sync] 자동 다운로드 실패:', err))
  } else {
    syncService.downloadDeltaAndReload(session.user.id, since)
      .then(({ total }) => {
        if (total > 0) toast.info(`서버에서 ${total}개 변경사항을 받아왔습니다.`)
      })
      .catch(err => console.warn('[Sync] 증분 다운로드 실패:', err))
  }
  ```

- [ ] **B-3** `downloadAndReload` 반환값에 `{ total }` 추가
  - 현재 반환값 없음 → `return { total }` 추가

- [ ] **B-4** 빌드 확인: `npm run build` 에러 없음

### S-2 테스트

- [ ] **S2-T1** 최초 기기 — 전체 다운로드 + 토스트
  ```
  1. 시크릿 탭(새 기기) → 로그인 (서버에 데이터가 있는 계정)
  2. 로그인 직후 "서버에서 N개 항목을 받아왔습니다." success 토스트 표시 확인
  3. DevTools IDB — 각 테이블에 데이터 존재 확인
  ```

- [ ] **S2-T2** 기존 기기 재로그인 — 증분 다운로드
  ```
  1. 기기A에서 로그인 + 서버 업로드 완료 (lastSyncedAt 설정됨)
  2. 기기B에서 새 매매 1건 추가 + 서버 업로드
  3. 기기A에서 로그아웃 후 재로그인
  4. "서버에서 1개 변경사항을 받아왔습니다." info 토스트 표시 확인
  5. Journal 페이지에서 기기B에서 추가한 매매 표시 확인
  ```

- [ ] **S2-T3** 재로그인 — 변경사항 없을 때 토스트 없음
  ```
  1. 기기A에서 로그인 + 서버 업로드 완료
  2. 다른 기기에서 아무 변경 없음
  3. 기기A에서 로그아웃 후 재로그인
  4. 토스트 표시되지 않음 (조용히 완료)
  ```

- [ ] **S2-T4** 다운로드 실패 — warning 토스트
  ```
  1. DevTools → Network → Offline 설정
  2. 앱 로그인
  3. "서버 동기화에 실패했습니다. 로컬 데이터로 계속합니다." warning 토스트 표시
  4. 앱은 정상 작동 (로컬 데이터 표시)
  ```

- [ ] **S2-T5** 증분 다운로드 — 기존 데이터 덮어쓰기 없음
  ```
  1. 기기A에서 데이터 100건, lastSyncedAt 설정됨
  2. 기기B에서 신규 5건 추가 + 업로드
  3. 기기A 재로그인 후 증분 다운로드
  4. 기기A IDB 전체 건수: 105건 (기존 100건 + 신규 5건, 기존 데이터 유지)
  5. 기존 레코드의 syncedAt, userId 값 변경되지 않음
  ```

- [ ] **S2-T6** `since` 5분 마진 동작 확인
  ```
  1. 기기A의 lastSyncedAt = T1 시각
  2. 서버에 synced_at = T1 - 2분 인 레코드 존재 (경계 케이스)
  3. 기기A 증분 다운로드 → since = T1 - 5분
  4. 해당 레코드 다운로드됨 (마진으로 포함)
  ```

---

## 통합 검증

- [ ] **I-1** 전체 플로우 시나리오
  ```
  1. 기기A: 매매 5건 입력
  2. 기기A: [업로드 후 이동] 모달 경유 업로드 완료
  3. 기기B: 동일 계정 로그인 → 증분 다운로드 → "5개 변경사항" 토스트
  4. 기기B: 매매 3건 추가
  5. 기기B: 다른 페이지 이동 시도 → 모달 표시 → 업로드
  6. 기기A: 로그아웃 재로그인 → "3개 변경사항" 토스트
  ```

- [ ] **I-2** 콘솔 에러 없음
  ```
  Chrome DevTools → Console
  각 테스트 시 빨간 에러 없음
  ```

- [ ] **I-3** 빌드 정상
  ```
  npm run build → 에러 없음
  ```

---

## 수작업 검증 포인트

- [ ] PendingUploadModal UI 확인 — 버튼 3개, 스피너, 오류 메시지 표시
- [ ] 증분 다운로드 토스트 — info/success/warning 색상 구분
- [ ] IDB 건수 일치 — 다운로드 전후 DevTools IndexedDB 수동 확인
- [ ] Supabase 테이블 확인 — 업로드 후 Table Editor에서 데이터 존재 확인

---

*각 항목 완료 시 `[ ]` → `[x]` 체크.*
*수작업 검증 필요 항목은 검증 후 다음 단계 진행.*
