# 종목 관리 (Master DB) — 구현계획 & 체크리스트

> 설계 문서: `docs/07.01 종목 관리 기능구현 설계.md` 참조
> 작성일: 2026-04-21
> 목표: 국내(KOSPI/KOSDAQ/NXT/ETF) + 해외(NYSE/NASDAQ/US_ETF) 종목 마스터를
> IndexedDB(Dexie)에 일괄 다운로드 & 증분 upsert 하고, 앱 전역 단일 소스로 사용

---

## 전체 진행 현황

| Sprint | 범위                                                      | 상태    | 완료일     |
| ------ | --------------------------------------------------------- | ------- | ---------- |
| 7.1    | Dexie 스키마 + 기존 LocalStorage 마이그레이션             | ✅ 완료 | 2026-04-22 |
| 7.2    | 서버 수집기 모듈화 (dart / krxEtf / yahoo / normalize)    | ✅ 완료 | 2026-04-22 |
| 7.3    | **NXT 수집기** + NASDAQ Trader 파서 (커버리지 확장) | ⏳ 대기 | —         |
| 7.4    | 클라이언트 API +`upsertSync()` diff 엔진                | ⏳ 대기 | —         |
| 7.5    | `<StockMasterPanel/>` UI (국내/해외 분리, 진행바)       | ⏳ 대기 | —         |
| 7.6    | 소비자 연결 (fetchSearch / watchlist / ResearchAgent)     | ✅ 완료 | 2026-04-23 |
| 7.7    | 테스트 + 오프라인 검증 + 문서 마무리                      | ⏳ 대기 | —         |

---

## Sprint 7.1 — Dexie 스키마 + 마이그레이션

### 7.1-A. 의존성 설치 및 설정

- [X] `npm install dexie` — `dexie ^4.4.2` 이미 설치됨 확인
- [X] `package.json` 의존성에 `dexie: ^4.x` 확인
- [X] 기존 `src/utils/db.js` (PortfolioHub DB)와 **별도 DB 인스턴스**로 분리 유지

### 7.1-B. `stockMasterDb.js` 신규 생성

> 파일: `src/utils/stockMasterDb.js` (신규 ✅)

- [X] `StockMasterDB` Dexie 인스턴스 선언
- [X] `version(1).stores({...})` 정의
  - `stocks` 테이블: PK=`id`, 인덱스 `[category+exchange]`, `[category+ticker]`, `ticker`, `name`, `nameEn`
  - `syncLogs` 테이블: `++id, syncedAt, exchange, status`
- [X] `categoryOf(exchange)` 헬퍼 — exchange → 'DOMESTIC'/'OVERSEAS' 매핑
- [X] `makeId({ category, exchange, ticker })` 헬퍼 — 복합 PK 생성
- [X] 타입 정의 JSDoc 주석 (`StockMasterRow` 14개 필드 완전 기술)

### 7.1-C. CRUD & 검색 헬퍼 구현

- [X] `upsertMany(rows)` — `bulkPut` 래퍼
- [X] `deactivateMissing(exchange, keepIds)` — `isActive=false` 일괄 갱신
- [X] `getByExchange(exchange, { includeInactive })` — 인덱스 기반 조회
- [X] `getCountsByExchange()` — exchange 별 카운트 집계 (Promise.all)
- [X] `searchByQuery(query, { limit, includeInactive })`
  - ticker `startsWithIgnoreCase` + name/nameEn 부분 일치
  - `dedupeById()` 로 결과 병합
- [X] `getById(id)` / `getByTicker(ticker, category?)` 단건 조회
- [X] `appendSyncLog({ exchange, status, stats, durationMs, error? })` — 관측 로그
- [X] `upsertSync(exchange, remoteRows)` — diff 엔진 (Sprint 7.4 선행 구현)
- [X] `addCustom(row)` / `removeCustom(id)` — 커스텀 종목 CRUD

### 7.1-D. 기존 데이터 마이그레이션

> 파일: `src/utils/stockMasterMigrate.js` (신규 ✅)

- [X] `migrateFromLegacy()` 함수 작성
  - `localStorage.getItem('stock-db-v1')` 존재 여부 확인
  - IndexedDB 가 비어있고 & legacy 존재 시에만 실행
  - `stocksByMarket` 배열 + `customStocks` → `StockMasterRow` 변환
  - `bulkPut` 후 `localStorage['stock-db-v1-migrated'] = ISO` 플래그 저장
- [X] `App.jsx` 에서 앱 기동 시 1회 호출 (`useEffect([], [])`)
- [X] 마이그레이션 실패 시 sonner warning 토스트 + 설정 페이지 유도

### 7.1 완료 검증

- [X] 브라우저 DevTools Application → IndexedDB → `StockMasterDB` 테이블 확인
- [X] 기존 사용자 업그레이드 시 LocalStorage 데이터가 IDB 로 이전되었는지 확인
- [X] 새 사용자: IDB 비어있음 & 에러 없음 확인

---

## Sprint 7.2 — 서버 수집기 모듈화 + 정규화

> 기존 `server/stockUpdateHandler.js` 의 로직을 파일별로 분리 + StockMasterRow 로 통일

### 7.2-A. 폴더 구조 생성

```
server/stockMaster/
├── index.js        ✅
├── dart.js         ✅
├── krxEtf.js       ✅
├── nxt.js             ← Sprint 7.3
├── nasdaqTrader.js    ← Sprint 7.3
├── yahooUs.js      ✅
└── normalize.js    ✅
```

- [X] `server/stockMaster/` 디렉터리 생성
- [X] 각 모듈 파일 완전 구현 (skeleton → 완성)

### 7.2-B. `normalize.js` — 공통 매핑

- [X] `fromDart(item, corpCls)` — DART → StockMasterRow
- [X] `fromNaverEtf(item)` — 네이버 ETF → StockMasterRow (exchange=`KRX_ETF`)
- [X] `fromYahoo(quote, targetExchange)` — Yahoo quote → StockMasterRow
- [X] `fromNasdaqTrader(row, exchange)` — NASDAQ Trader 파이프 구분 텍스트 → Row (시그니처)
- [X] 공통 필드 기본값: `isCustom: false`, `isActive: true`, `firstSeenAt/updatedAt: now`

### 7.2-C. `dart.js` — KOSPI/KOSDAQ 수집기

- [X] 기존 `fetchDartListByCls()` 로직을 `dart.js` 로 이관
- [X] 89일 × 4윈도우 페이징 유지
- [X] 출력: `StockMasterRow[]` (normalize.fromDart 경유)
- [X] `corpCode` 필드 포함 확인

### 7.2-D. `krxEtf.js` — KRX ETF 수집기

- [X] 기존 `fetchNaverEtfList()` 로직 이관 → `fetchKrxEtfList()`
- [X] 네이버 PC `etfItemList.nhn` + 모바일 API 폴백 유지
- [X] exchange=`KRX_ETF`, type=`ETF`, category=`DOMESTIC`

### 7.2-E. `yahooUs.js` — NYSE/NASDAQ + US_ETF 수집기

- [X] 기존 `fetchYahooStocks()` 이관 + `sector` 필드 추출
- [X] NASDAQ_ETF / NYSE_ETF → `US_ETF` 통합 (`fetchUsEtf()`)
- [X] FALLBACK 티커 상수를 모듈 내 export 상수로 분리

### 7.2-F. `index.js` — 라우팅 + 엔드포인트

- [X] `collectExchange(exchange, dartKey)` 라우터 함수
- [X] `GET /api/stock-master?exchange=...` 핸들러
- [X] `GET /api/stock-master?category=DOMESTIC|OVERSEAS|ALL` 핸들러
- [X] `GET /api/stock-master/manifest` — 카운트 / last-modified 요약만
- [X] 응답 포맷: `{ exchange, category, rows, count, errors, collectedAt, durationMs }`
- [X] 에러 시 200 + `errors` 배열 (5xx 남발 방지)

### 7.2-G. `vite.config.js` 미들웨어 연결

- [X] 기존 `/api/stock-update` 라우트와 **병행 등록** (하위호환 유지)
- [X] 새 `/api/stock-master/*` 핸들러 import 및 `configureServer()` 등록
- [X] 에러 catch → JSON 200 응답 (writableEnded 체크 포함)

### 7.2 완료 검증

- [X] `curl http://localhost:5173/api/stock-master?exchange=KOSPI` → rows ≥ 800
- [X] `curl http://localhost:5173/api/stock-master?category=DOMESTIC` → 4개 exchange 응답
- [X] `curl http://localhost:5173/api/stock-master/manifest` → 카운트만 반환 (rows 없음)

---

## Sprint 7.3 — NXT + NASDAQ Trader 커버리지 확장 ✅ 완료 (2026-04-22)

### 7.3-A. NXT 수집기 (`nxt.js`)

> NXT = Nextrade ATS. "KOSPI/KOSDAQ 종목 중 NXT 동시 거래 가능 목록"

- [x] Nextrade 거래소 공식 소스 조사
  - 1차: KRX 정보데이터시스템 ATS 종목 조회 (`MDCSTAT01901`)
  - 2차: 하드코딩 시드 리스트 (KOSPI/KOSDAQ 주요 대형주 47개)
- [x] `fetchNxtTradableSet()` 함수 — KRX API 성공 시 우선, 실패 시 시드 폴백
- [x] `mergeNxtFlags(dartRows, nxtSet)` — DART rows에 NXT tradableOn 병합 (불변 처리)
- [x] DART 수집 결과와 **병합** 단계 구현
  - KOSPI/KOSDAQ row 중 NXT 대상 ticker 에 대해 `tradableOn: ['KOSPI', 'NXT']`
- [x] 파싱 실패 시 시드 폴백 + 경고 로그 (전체 수집은 계속)

### 7.3-B. NASDAQ Trader 공식 파일 파서 (`nasdaqTrader.js`)

> 기존 Yahoo screener 한계(~150종) → 공식 파일(~8,000종) 보강

- [x] `fetchNasdaqListed()` — `nasdaqlisted.txt` (파이프 구분 파싱)
  - 헤더 / "Test Issue=Y" / 마지막 "File Creation Time" 라인 제외
  - 워런트/파생 종목 필터 (`+`, `=`, `$` 접미사)
  - exchange=`NASDAQ`, ETF=Y → `US_ETF`
- [x] `fetchOtherListed()` — `otherlisted.txt`
  - Exchange 코드 매핑: A=AMEX, N=NYSE, P/Z/V=NYSE
  - ETF=Y → `US_ETF` 재분류
- [x] `fetchAllFromNasdaqTrader()` — 두 파일 병렬 수집, exchange별 분리 반환
- [x] 요청 타임아웃 20초, `Promise.allSettled` 기반 부분 실패 허용

### 7.3-C. `yahooUs.js` 와의 병합 전략

- [x] NASDAQ Trader → 기본 수집 (커버리지 우선)
- [x] Yahoo screener/quote → 부스트 수집 (NASDAQ Trader 누락분 보완)
- [x] 병합 규칙: NASDAQ Trader 기본, Yahoo 중 미등재 ticker만 추가
- [x] NASDAQ Trader 완전 실패 시 Yahoo 전체 사용 (폴백)

### 7.3-D. `index.js` 라우팅 갱신

- [x] `exchange=KOSPI/KOSDAQ` → DART + NXT tradableOn 병합
- [x] `exchange=NXT` → KOSPI+KOSDAQ 전체에서 NXT 플래그 종목만 반환
- [x] `exchange=NYSE` → NASDAQ Trader otherlisted 필터 + Yahoo 부스트
- [x] `exchange=NASDAQ` → NASDAQ Trader nasdaqlisted + Yahoo 부스트
- [x] `exchange=US_ETF` → NASDAQ Trader ETF=Y + Yahoo FALLBACK_US_ETF 보완
- [x] 30분 인메모리 캐시 추가 (개발 서버 중복 요청 방지)

### 7.3 완료 검증

- [x] `/api/stock-master?exchange=NASDAQ` → rows ≥ 3,000 (기존 ~150 → 대폭 확장)
- [x] `/api/stock-master?exchange=NYSE` → rows ≥ 2,000
- [x] `/api/stock-master?exchange=US_ETF` → rows ≥ 500
- [x] KOSPI 결과 중 삼성전자(005930) → `tradableOn: ['KOSPI', 'NXT']` 포함
- [x] NASDAQ Trader 차단 시 Yahoo 폴백으로 최소 150 rows 반환

---

## Sprint 7.4 — 클라이언트 API + Diff/Upsert 엔진 ✅ 완료 (2026-04-22)

### 7.4-A. `stockMasterApi.js` 신규 생성

> 파일: `src/api/stockMasterApi.js` (신규)

- [x] `EXCHANGES` 상수 export — `{ DOMESTIC: [...], OVERSEAS: [...] }`
- [x] `EXCHANGE_LABELS` — 한글 표시명 매핑
- [x] `fetchManifest()` — manifest(카운트 요약) 조회
- [x] `syncExchange(exchange, { onProgress, signal })` — 단일 시장 fetch+upsertSync
- [x] `syncCategory(category, { onProgress, signal })` — 카테고리 단위 순차 sync
- [x] `syncAll({ onProgress, signal })` — DOMESTIC → OVERSEAS 순서
- [x] `syncIncremental(localCounts, opts)` — manifest 비교 후 변동 exchange만 sync
- [x] AbortController 지원 — anySignal + 60초 타임아웃

### 7.4-B. `upsertSync()` 구현

> 파일: `src/utils/stockMasterDb.js` (Sprint 7.1에서 구현 완료)

- [x] `upsertSync(exchange, remoteRows)` 함수
  - 1단계: 기존 local rows 조회 (`[category+exchange]` 인덱스)
  - 2단계: `Set` 기반 diff — added / removed / common
  - 3단계: common 중 isDirty 판별 (name/nameEn/sector/corpCode/type/tradableOn 비교)
  - 4단계: `mergeRow` — firstSeenAt/isCustom 보존
  - 5단계: Dexie 트랜잭션 안에서 `bulkPut(added+changed)` + `modify(removed → isActive=false)`
  - 6단계: `appendSyncLog()`
- [x] 반환 형식: `{ added: N, changed: N, removed: N, durationMs }`
- [x] `isCustom=true` row는 removed 후보에서 제외 (사용자 데이터 보호)

### 7.4-C. `stockMasterStore.js` (Zustand)

> 파일: `src/store/stockMasterStore.js` (신규)

- [x] `persist` 미들웨어, name=`stock-master-meta-v1`
- [x] 상태: `counts`, `lastSync`, `lastStats`, `progress`
- [x] 액션: `setProgress`, `setSyncResult`, `refreshCounts`, `setExchangeCount`, `reset`
- [x] 셀렉터: `isSyncing()`, `getTotalCount()`, `getCategoryCounts()`
- [x] **rows 자체는 저장하지 않음** (IDB에 보관, Zustand는 메타만)
- [x] `partialize` 로 progress 제외 (재기동 시 초기화)
- [x] `App.jsx` — 마이그레이션 완료 후 `refreshCounts()` 1회 호출

### 7.4-D. 커스텀 종목 로직 이전

- [x] `stockMasterDb.addCustom(row)` — Sprint 7.1에서 구현 완료
  - 중복 검증: id(category:exchange:ticker) 기준
  - `isCustom: true`, `source: 'MANUAL'`
- [x] `stockMasterDb.removeCustom(id)` — isCustom인 row만 허용
- [ ] Settings.jsx UI 연결 → Sprint 7.5에서 처리

### 7.4 완료 검증

- [x] 빈 DB → syncAll 실행 → added ≈ 전체 개수, removed=0
- [x] 동일 상태로 재실행 → added=0, changed≤5, removed=0
- [x] 임의 row 삭제 후 sync → added=1 로 복원
- [x] Dexie 트랜잭션 중 throw → 롤백 확인 (DB 일관성 유지)
- [x] `isCustom` row가 sync로 사라지지 않음 확인

---

## Sprint 7.5 — 설정 UI (`<StockMasterPanel/>`) ✅ 완료 (2026-04-22)

### 7.5-A. 컴포넌트 신규 생성

> 파일: `src/components/settings/StockMasterPanel.jsx` (신규)

- [x] 기존 `Settings.jsx` 의 "종목 DB 관리" 섹션 추출 & 교체
- [x] **국내/해외 섹션 분리** — 접이식(ChevronUp/Down) 카드 그룹
- [x] 각 exchange 별 카드 — 카운트 + 수집 중 파란 테두리 하이라이트
- [x] 진행바 — `stockMasterStore.progress` 구독, exchange + phase 표시
- [x] 최근 결과 요약 배지 — `+N 신규 / 변경 N / 해제 N`

### 7.5-B. 액션 버튼

- [x] `[전체 업데이트]` — `syncAll({onProgress})`
- [x] `[증분 동기화]` — `syncIncremental(counts)` (manifest 비교 후 변동 시장만)
- [x] `[국내 업데이트]` — `syncCategory('DOMESTIC', {onProgress})`
- [x] `[해외 업데이트]` — `syncCategory('OVERSEAS', {onProgress})`
- [x] `[취소]` — AbortController로 진행 중 fetch 중단
- [x] `[초기화]` — 확인 모달 후 `stockMasterDb.stocks.clear()` + store.reset()
- [x] 실행 중 버튼 `disabled`, RefreshCw `animate-spin`

### 7.5-C. 커스텀 종목 UI 이전

> 파일: `src/components/settings/CustomStockForm.jsx` (신규 분리)

- [x] `category`(국내/해외) 선택 → `exchange` 옵션 동적 갱신
- [x] 유형(주식/ETF/ETN/REIT) 선택 지원
- [x] 추가 시 `stockMasterDb.addCustom()` 호출 (IDB 직접 쓰기)
- [x] 목록 표시: `stockMasterDb.stocks.filter(r => r.isCustom)` (IDB 필터)
- [x] 삭제 버튼 + `sonner` 토스트 피드백
- [x] `onChanged` 콜백으로 부모(StockMasterPanel)의 `refreshCounts()` 트리거

### 7.5-D. 증분 동기화 로직

- [x] `syncIncremental(localCounts)` — manifest 서버 count vs 로컬 count 비교
- [x] 카운트 차이 > 0 인 exchange만 `syncExchange()` 실행
- [x] 모두 동일하면 "변경된 시장이 없습니다." 토스트 + skip

### 7.5-E. Settings.jsx 리팩토링

> 파일: `src/pages/Settings.jsx` (수정)

- [x] 기존 `종목 DB 관리` / `커스텀 종목 추가` 섹션 전체 삭제
- [x] `<StockMasterPanel/>` 1줄로 교체
- [x] `useStockDbStore`, `fetchAllMarketsSequential`, `MARKET_LABELS` import 제거
- [x] 빌드 확인 ✅ (에러 없음)

### 7.5 완료 검증

- [x] 설정 페이지 → "전체 업데이트" → 진행바 정상 동작
- [x] 국내/해외 카드 분리 UI 확인
- [x] 커스텀 종목 추가/삭제 — IDB 직접 CRUD (구 localStorage 무관)
- [x] 증분 동기화: 변경 없으면 skip, 있으면 해당 exchange만 sync
- [x] 다크모드 Tailwind 클래스 적용

---

## Sprint 7.6 — 소비자 연결 (앱 전역 단일 소스)

### 7.6-A. `fetchSearch()` 마스터 DB 우선 조회

> 파일: `src/api/stockApi.js` (수정)

- [x] `fetchSearch(query)` 1순위를 `stockMasterDb.searchByQuery()` 로 교체
- [x] 결과 부족 시(< 5건) 기존 Yahoo/Naver search 로 원격 보강
- [x] 결과 매핑: `StockMasterRow` → 기존 `SearchResult` 형식 유지 (하위호환) — `masterRowToSearchResult()` 헬퍼
- [x] `exchangeToMarket()` 헬퍼로 exchange → market 변환 (fetchQuote 파라미터 호환)
- [x] **성능**: 로컬 우선 → 평균 검색 지연 < 50ms 목표

### 7.6-B. 관심종목 검증

> 파일: `src/store/watchlistStore.js` (수정)

- [x] `addToWatchlistValidated({ ticker, market })` async 액션 추가
  - `stockMasterDb.getByTicker()` 로 존재 확인
  - 없으면 `{ added: true, warn: true, message }` 반환 → 컴포넌트에서 토스트 처리
  - 중복이면 `{ added: false, warn: false, message }` 반환
- [x] IDB 미준비(최초 설치) 시 검증 스킵, 경고 없이 추가

### 7.6-C. ResearchAgent 티커 해석

> 파일: `src/agents/researchAgent.js` (수정)

- [x] `extractTicker(userMessage)` export 함수 추가
  - 1순위: 정규식 (6자리 숫자 / 대문자 1~5자 티커)
  - 2순위: `stockMasterDb.searchByQuery()` — 단어별 분리 검색
  - 최상위 결과의 `{ ticker, exchange, market }` 반환
- [x] 다중 일치 시 KOSPI > KOSDAQ > NASDAQ > NYSE 우선순위 정렬

### 7.6-D. useStockSearch 훅 — 마스터 DB 기반으로 전환

> 파일: `src/hooks/useStockData.js` (수정)

- [x] `fetchSearch()` 내부가 마스터 DB 우선이므로 훅은 자동 적용
- [x] `staleTime` 을 5분으로 조정 (IDB 기반으로 네트워크 불필요)
- [x] Research / AddStockModal / Watchlist 등 기존 `useStockSearch` 사용처 전체 자동 혜택

### 7.6-E. 포트폴리오 추가 모달

> 파일: `src/components/portfolio/AddStockModal.jsx` (수정)

- [x] 기존 자동완성은 `useStockSearch` 사용 → 마스터 DB 자동 적용
- [x] 티커 직접 입력 후 blur 시 `getByTicker()` 검증 → `masterWarn` 상태
- [x] 마스터 DB 에 없는 ticker → `AlertTriangle` 경고 배너 표시

### 7.6 완료 검증

- [ ] "삼성전자" 검색 → 마스터 DB 에서 즉시 표시 (네트워크 요청 0회)
- [ ] "AAPL" 검색 → NASDAQ row 표시
- [ ] AI 채팅 "삼성전자 분석해줘" → 티커 005930 자동 해석 → 분석 정상 생성
- [ ] 관심종목 추가 시 마스터 DB 검증 동작 (`addToWatchlistValidated`)
- [ ] 포트폴리오 추가: 알 수 없는 티커 직접 입력 → 노란 경고 배너 표시

---

## Sprint 7.7 — 테스트 + 문서 마무리

### 7.7-A. 단위 테스트

- [ ] `normalize.js` 각 함수 — 고정 입력 → StockMasterRow 스냅샷
- [ ] `upsertSync()` — 4가지 경계 케이스
  - 빈 DB → 전량 added
  - 전량 동일 → added=0 changed=0 removed=0
  - 일부 삭제 → removed만 발생
  - `isCustom` row 존재 시 removed 제외
- [ ] `searchByQuery()` — 한/영/혼합/빈 쿼리/특수문자

### 7.7-B. 통합 테스트

- [ ] 로컬 서버 `/api/stock-master?exchange=KOSPI` → rows ≥ 800
- [ ] 2회 연속 syncAll → 2회차 변경 최소 (변동 종목 5개 미만)
- [ ] 트랜잭션 중 throw 주입 → DB 상태 롤백 확인
- [ ] DART 키 미설정 → 500 대신 200 + `errors` 배열 + UI 에러 뱃지

### 7.7-C. E2E (Playwright)

- [ ] 설정 → [전체 업데이트] → 진행바 이동 → 완료 토스트 → 카운트 > 0
- [ ] Research 페이지 "삼성전자" 입력 → 자동완성 (네트워크 mock off 상태에서도)
- [ ] DB 초기화 → syncCategory('DOMESTIC') → 4개 시장 정상 복원
- [ ] 오프라인 모드: 검색 동작 확인 (IDB 기반)

### 7.7-D. 마이그레이션 시나리오 검증

- [ ] 기존 사용자(LocalStorage 에 stock-db-v1 존재) → 첫 실행 시 IDB 로 이전
- [ ] 마이그레이션 완료 플래그 설정 후 재실행 → 재마이그레이션 발생 안 함
- [ ] 이전 실패 케이스 → 사용자 알림 + 설정 페이지 유도

### 7.7-E. 문서 업데이트

- [ ] `CLAUDE.md` 로드맵에 Phase 7 항목 추가
- [ ] `docs/07.03 종목 관리 테스트 시나리오.md` 작성 (E2E 시나리오 정리)
- [ ] 본 문서 `전체 진행 현황` 표 상태 업데이트 (✅ 완료)

---

## 보안 · 운영 체크리스트 (전 Sprint 공통)

- [ ] `DART_API_KEY` 서버 전용 (VITE_ 접두사 금지) 재확인
- [ ] NASDAQ Trader ftp — `User-Agent` 헤더 포함 (차단 방지)
- [ ] Naver etfItemList — `Referer: https://finance.naver.com` 헤더 유지
- [ ] 마스터 DB 는 **공개 정보**만 — PII 없음 확인
- [ ] `isCustom=true` 사용자 데이터 보호 — 서버 sync 로 덮어쓰지 않음
- [ ] Rate limit 대응: DART 일일 40,000건 / Yahoo 비공식 1초 간격
- [ ] 에러 응답 정규화: 5xx 대신 200 + `errors` 배열로 UI 복원력 확보

---

## E2E 테스트 시나리오

| #   | 시나리오                         | 기대 결과                                        | Sprint |
| --- | -------------------------------- | ------------------------------------------------ | ------ |
| M1  | 신규 사용자 → [전체 업데이트]   | IDB 에 5,000+ rows 저장, 진행바 정상             | 7.5    |
| M2  | 기존 LocalStorage 사용자 첫 실행 | IDB 로 자동 마이그레이션, 카운트 유지            | 7.1    |
| M3  | 동일 상태 재 sync                | added=0, changed≤5, removed=0                   | 7.4    |
| M4  | 삼성전자 검색 (한글)             | 005930 KOSPI 즉시 표시                           | 7.6    |
| M5  | AAPL 검색 (영문)                 | NASDAQ row 즉시 표시                             | 7.6    |
| M6  | 오프라인 모드 검색               | 마스터 DB 기반 검색 정상                         | 7.6    |
| M7  | DART 키 미설정 상태 sync         | KOSPI/KOSDAQ errors 배지, 나머지 정상 수집       | 7.2    |
| M8  | NXT 병합 확인 (삼성전자)         | `tradableOn: ['KOSPI','NXT']` 포함             | 7.3    |
| M9  | 커스텀 종목 추가 → sync 실행    | 커스텀 종목 isActive 유지, removed 제외          | 7.4    |
| M10 | 상장폐지 종목 sync               | `isActive=false`, 기본 검색 숨김, 토글 시 노출 | 7.4    |
| M11 | AI 채팅 "삼성전자 분석"          | 마스터 DB 에서 티커 해석 → 분석 정상            | 7.6    |
| M12 | 증분 동기화 (카운트 변경 없음)   | "변경 없음" 토스트, API 호출 skip                | 7.5    |

---

## 파일별 변경 요약

| 파일                                             | 변경 유형                         | Sprint |
| ------------------------------------------------ | --------------------------------- | ------ |
| `src/utils/stockMasterDb.js`                   | 신규 생성                         | 7.1    |
| `src/utils/stockMasterMigrate.js`              | 신규 생성                         | 7.1    |
| `server/stockMaster/index.js`                  | 신규 생성                         | 7.2    |
| `server/stockMaster/dart.js`                   | 신규 생성                         | 7.2    |
| `server/stockMaster/krxEtf.js`                 | 신규 생성                         | 7.2    |
| `server/stockMaster/yahooUs.js`                | 신규 생성                         | 7.2    |
| `server/stockMaster/normalize.js`              | 신규 생성                         | 7.2    |
| `server/stockMaster/nxt.js`                    | 신규 생성                         | 7.3    |
| `server/stockMaster/nasdaqTrader.js`           | 신규 생성                         | 7.3    |
| `src/api/stockMasterApi.js`                    | 신규 생성                         | 7.4    |
| `src/store/stockMasterStore.js`                | 신규 생성                         | 7.4    |
| `src/components/settings/StockMasterPanel.jsx` | 신규 생성                         | 7.5    |
| `src/components/settings/CustomStockForm.jsx`  | 신규 생성                         | 7.5    |
| `vite.config.js`                               | 미들웨어 등록                     | 7.2    |
| `src/pages/Settings.jsx`                       | 섹션 교체                         | 7.5    |
| `src/api/stockApi.js`                          | fetchSearch 변경                  | 7.6    |
| `src/store/watchlistStore.js`                  | 검증 추가                         | 7.6    |
| `src/agents/researchAgent.js`                  | 티커 해석 확장                    | 7.6    |
| `src/components/portfolio/AddStockModal.jsx`   | 자동완성 교체                     | 7.6    |
| `src/App.jsx` or `src/main.jsx`              | 마이그레이션 호출                 | 7.1    |
| `src/store/stockDbStore.js`                    | ⚠ deprecated (Phase 7.7 후 제거) | 7.7    |
| `CLAUDE.md`                                    | 로드맵 갱신                       | 7.7    |
| `docs/07.03 종목 관리 테스트 시나리오.md`      | 신규 생성                         | 7.7    |

---

## 의존성 그래프 (Sprint 간)

```
7.1 (Dexie 스키마) ──┬──> 7.2 (서버 수집기) ──> 7.3 (NXT/NASDAQ Trader)
                     │                                │
                     └──> 7.4 (upsertSync) <──────────┘
                                  │
                                  ▼
                           7.5 (UI 패널)
                                  │
                                  ▼
                           7.6 (소비자 연결)
                                  │
                                  ▼
                           7.7 (테스트 + 문서)
```

- **7.1 → 7.4** : 스키마 없으면 upsert 불가
- **7.2 → 7.4** : 서버 API 없으면 클라이언트 sync 불가
- **7.3 → 7.4** : NXT 병합 로직이 normalize 에 포함됨 (7.2 확장)
- **7.5 → 7.6** : UI 로 데이터 확보 후에야 소비자 연결 검증 가능
- **병렬 가능**: 7.2 와 7.1 은 동시 진행 가능 (독립)

---

## 리스크 & 대응

| 리스크                               | 영향                    | 대응                                            |
| ------------------------------------ | ----------------------- | ----------------------------------------------- |
| NASDAQ Trader ftp 403/차단           | 해외 커버리지 ↓        | Yahoo screener 폴백 유지 (Sprint 7.3-D)         |
| Nextrade 공식 데이터 소스 부재       | NXT 플래그 부정확       | 초기 하드코딩 시드 + 추후 수동 갱신 스크립트    |
| DART 89일 제한으로 신규 상장 누락    | KOSPI/KOSDAQ 누락       | KRX 상장법인 XLS 2차 소스 Sprint 7.2 옵션 추가  |
| Dexie 마이그레이션 실패              | 기존 사용자 데이터 손실 | LocalStorage 원본 1주일 보관 + 수동 복원 가이드 |
| 대용량 수집 중 서버 타임아웃         | UX 나빠짐               | exchange 단위 순차 + 진행바 + AbortController   |
| 오프라인 사용자가 전체 업데이트 시도 | 에러 발생               | 온라인 감지 후 버튼 disable + 안내 문구         |

---

## 예상 작업량 (참고용)

| Sprint | 작업량 추정                             | 비고                                     |
| ------ | --------------------------------------- | ---------------------------------------- |
| 7.1    | 신규 2파일 (~400 lines)                 | Dexie 경험 있으면 단축                   |
| 7.2    | 신규 6파일 (~700 lines, 기존 로직 이관) | 기존 `stockUpdateHandler.js` 재사용    |
| 7.3    | 신규 2파일 (~300 lines)                 | NXT 소스 조사 시간 별도 필요             |
| 7.4    | 신규 2파일 + 확장 1파일 (~400 lines)    | diff 알고리즘 테스트 커버리지 중요       |
| 7.5    | 신규 2파일 + 수정 1파일 (~500 lines)    | UI 이므로 디자인 리뷰 1회 권장           |
| 7.6    | 수정 4~5파일 (~200 lines)               | 회귀 테스트 주의 (기존 검색 깨지지 않게) |
| 7.7    | 테스트 + 문서 (~작업량 분산)            | 전 Sprint 병행 권장                      |

---

*작성: 2026-04-21*
*대상 Phase: 7 (종목 마스터 관리)*
*선행 문서: `docs/07.01 종목 관리 기능구현 설계.md`*
*후속 문서 (예정): `docs/07.03 종목 관리 테스트 시나리오.md`*
