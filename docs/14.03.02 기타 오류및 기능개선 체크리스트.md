# 14.03.02 기타 오류 및 기능 개선 체크리스트

## 작성일
2026-06-06

---

## [FEAT-01] 수익률 추이 차트 — 일간 증감치 오버레이

### 코드 수정

- [x] `ProfitLineChart.jsx` — AreaChart → ComposedChart 교체
- [x] `ProfitLineChart.jsx` — 우측 YAxis (yAxisId="right") 추가
- [x] `ProfitLineChart.jsx` — Bar (dataKey="dailyReturn", yAxisId="right") 추가
- [x] `ProfitLineChart.jsx` — Bar 색상 분기: dailyReturn > 0 → #EF4444, ≤ 0 → #3B82F6
- [x] `ProfitLineChart.jsx` — Tooltip에 일간 증감치 항목 추가
- [x] `HoldingPnlTimeline.jsx` — 날짜별 포트폴리오 합산 일간 수익률 계산 로직 추가
- [x] `HoldingPnlTimeline.jsx` — ComposedChart 교체 + 포트폴리오 합산 Bar 오버레이

### 수작업 검증

- [ ] 대시보드 > 수익률 추이 차트에서 Bar가 Area 위에 표시되는지 확인
- [ ] Bar 색상이 상승(빨강) / 하락(파랑)으로 올바르게 구분되는지 확인
- [ ] 우측 Y축 레이블이 정상 표시되는지 확인
- [ ] Tooltip hover 시 누적 수익률 + 일간 증감치 모두 표시되는지 확인
- [ ] 포트폴리오 > 꺾은선 차트에서 포트폴리오 합산 Bar가 종목별 Line 위에 오버레이되는지 확인
- [ ] 기간 선택(7일 / 30일 / 90일) 변경 시 Bar도 함께 갱신되는지 확인

---

<!-- 새 항목은 아래에 ## [FEAT-NN] 형식으로 추가 -->
