# 14.04.03 AI 분석 기능 고도화 체크리스트

## 작성일
2026-06-15

## 근거 문서
- [14.04.01 AI 분석 기능고도화 조사](./14.04.01%20AI%20분석%20기능고도화%20조사.md)
- [14.04.02 AI 분석 기능고도화 구현계획](./14.04.02%20AI%20분석%20기능고도화%20구현계획.md)

> 진행하며 `[ ]` → `[x]` 로 갱신한다. 수작업 확인 항목은 ✋ 표시.

---

## 사전 결정 (착수 전)

- [ ] 웹 검색 토글 기본값 결정 (ON / OFF)
- [x] 동적 필터링 버전(`web_search_20260209`) 1차 포함 여부 결정 → 미포함(기본형 20250305)
- [x] Opus 적용 에이전트 범위 확정 → analysis·research·report
- [ ] ✋ Anthropic 콘솔에서 웹 검색 사용 허용 설정 확인

---

## Phase 1 — P1: 프록시 모델 파라미터화

### api/claude.js
- [x] 요청 본문에서 `model` 수용
- [x] 허용 모델 화이트리스트 상수 추가 (`ALLOWED_MODELS`)
- [x] 미허용·미지정 `model` → 기본값 보정 (`DEFAULT_MODEL`)
- [x] 검증 통과 `model`만 Anthropic 요청에 전달
- [x] 기존 호출(하위호환) 정상 동작 확인 (build 통과)

### src/api/claudeApi.js
- [x] `AGENT_MODELS` 맵 추가 (analysis·research·report → Opus 4.8)
- [x] `sendToAgent` payload에 `model` 포함
- [x] `runAgentPipeline` 경로에 `model` 반영 (sendToAgent 경유)
- [x] `sendWithHistory` 경로 → 기본 모델(Sonnet) 사용 확인 (journal=기본값, 변경 불요)

### P1 검증
- [ ] ✋ analysis 호출 시 네트워크 응답 `model` = `claude-opus-4-8` 확인
- [x] 미허용 model 문자열 → 기본값 보정 (프록시 화이트리스트 로직)

---

## Phase 1 — P2: web_search 도구 결합

### 응답 파서 (선행 — TDD)
- [x] 멀티 `text` 블록 결합 함수 단위 테스트 작성
- [x] 인용(`citations`) 추출 로직 단위 테스트 작성
- [x] 파서 구현 → 테스트 통과 (5건, `claudeApiParser.test.js`)

### api/claude.js
- [x] 요청 본문에서 `tools` 배열 수용 → Anthropic 요청 passthrough
- [x] `pause_turn` 등 발생 시 루프 없이 존재하는 text 블록 반환 (단순화)
- [x] web_search 응답을 `{ content }` 그대로 클라이언트에 전달 (블록 보존)

### src/api/claudeApi.js
- [x] `AGENT_TOOLS` 맵 추가 (analysis·research·alert 활성)
- [x] web_search 도구 설정 적용 (max_uses=3, user_location=KR, allowed_domains)
- [x] 응답 파서로 `{ text, citations }` 정규화
- [x] citations → 응답 하단 `[참고 출처]` 렌더링
- [x] 기존 Yahoo/Naver/DART 프리패치와 병행 동작 유지

### 비용 가드 (settingsStore + UI)
- [x] `settingsStore`에 `webSearchEnabled` 토글 추가 (기본 OFF)
- [x] 토글 OFF 시 `AGENT_TOOLS` 비활성 → 프리패치 폴백
- [x] 설정 화면에 웹 검색 토글 UI 추가 (AI 및 API 키 섹션)
- [x] 동일 질의 `apiCache` 재사용 (기존 캐시 그대로 유지)

### P2 검증
- [ ] ✋ "오늘 삼성전자 왜 움직였어?" 질의 → 검색 수행 + 출처 인용 응답
- [ ] ✋ web_search 멀티 블록 응답에서 텍스트 잘림 없음 확인
- [ ] ✋ 설정 웹 검색 OFF → 기존 프리패치 방식 정상 응답
- [ ] ✋ max_uses 초과(`max_uses_exceeded`) 무해 처리 확인
- [ ] ✋ journal/portfolio/report → 기존과 동일 동작(도구 미적용)

---

## 완료 처리 (Phase 1)

- [x] `npx vitest run` — 신규 파서 5건 포함 78건 통과 (htsParser 4건은 사전 존재 실패, 무관)
- [x] `npm run build` 성공
- [ ] 커밋 분리 (P1 / P2 / 토글 단위)
- [ ] master 머지·푸시 (단일 브랜치 전략)
- [ ] ✋ 배포 후 실제 AI 분석 화면에서 최종 동작 확인

---

## Phase 2 (후속 — 미착수)

- [ ] P3 뉴스 감성 점수 구조화 (analysisAgent)
- [ ] P4 LLM 기반 라우팅 (orchestrator, Haiku)

## Phase 3 (후속 — 미착수)

- [ ] P5 analysis max_tokens 상향 + 확장적 사고 옵션
- [ ] P6 거시·테마 뉴스 탐색을 alert 일일 브리핑에 결합
