# 10.08 리포트 기능 개선 — 구현계획 및 체크리스트

> 설계 문서: [10.08.01 리포트 기능 개선 계획.md](./10.08.01%20리포트%20기능%20개선%20계획.md)  
> 브랜치: `worktree-feature+report-improvement`  
> 시작일: 2026-05-18

---

## Phase A — P1 핵심 기능

### A-1. 커스텀 날짜 피커 (계획 2-2)

**대상 파일:** `src/pages/Reports.jsx`

- [x] `customRange` 상태 추가 `{ from: '', to: '' }`
- [x] DATE_RANGES 배열에 `{ key: 'custom', label: '직접 설정' }` 항목 추가
- [x] 상단 날짜 필터 바에 "직접 설정" 버튼 추가
- [x] 버튼 클릭 시 두 개의 `<input type="date">` 인라인 표시 (Popover 미설치로 input 방식 채택)
- [x] `dateRange === 'custom'` 일 때 `customRange.from ~ customRange.to` 로 직접 필터링
- [ ] ~~커스텀 기간 선택 시 버튼에 "MM.DD ~ MM.DD" 형식으로 선택 기간 표시~~ (input 방식으로 대체)
- [x] 기존 1d / 1w / 1m / 1y 버튼 동작 영향 없는지 확인 (수동 검증 필요)
- [x] 검증: 임의 기간 선택 → KPI·차트·테이블 모두 해당 기간 데이터 필터링 확인 (수동 검증 필요)

---

### A-2. 심리 패턴 분석 탭 신설 (계획 2-1)

#### A-2-1. calculator.js 유틸 확장

**대상 파일:** `src/utils/calculator.js`

- [x] `groupByPsychology(entries)` 함수 추가 — psychology 별 거래 그룹핑
- [x] `calcPsychologyStats(entries)` 함수 추가 — 심리 유형별 `{ avgPnl, winRate, count }` 반환
- [x] `findRepeatedMistakes(entries)` 함수 추가 — 동일 심리로 3회 이상 손실 항목 탐지
- [x] `findBestPatterns(entries)` 함수 추가 — 수익률 상위 30% 거래의 공통 심리 도출
- [x] 검증: 데이터 없을 때 빈 배열 반환 확인 (수동 검증 필요)

#### A-2-2. PsychologyAnalysis.jsx 신규

**대상 파일:** `src/components/reports/PsychologyAnalysis.jsx`  
**참고:** `src/components/charts/PsychologyProfitChart.jsx` (재활용)

- [x] 파일 첫 줄 한국어 역할 주석 작성
- [x] Props: `entries` (필터링된 거래 일지 배열)
- [x] **심리 유형별 평균 손익 차트**: recharts `BarChart` (수평) — 수익 빨강 / 손실 파랑
- [x] **심리 유형별 승률 바**: 프로그레스 바 형태 — 승률 % + 거래수 표시
- [x] **반복 실수 패턴 카드**: `findRepeatedMistakes()` 결과 → 경고 아이콘과 함께 카드 표시
- [x] **잘된 결정 강화 카드**: `findBestPatterns()` 결과 → Star 아이콘과 함께 카드 표시
- [x] 데이터 없을 때 빈 상태 메시지 표시
- [x] 검증: 다양한 심리 유형 데이터로 모든 섹션 렌더링 확인 (수동 검증 필요)

#### A-2-3. Reports.jsx — 심리분석 탭 연결

**대상 파일:** `src/pages/Reports.jsx`

- [ ] `PsychologyAnalysis` 컴포넌트 import 추가
- [ ] 탭 목록에 `심리분석` 탭 추가 (7번째, 기존 탭 순서 유지)
- [ ] 탭 콘텐츠에 `<PsychologyAnalysis entries={filteredEntries} />` 렌더링
- [ ] 검증: 탭 전환 시 정상 렌더링 확인

---

### A-3. AI 리포트 히스토리 뷰 (계획 2-3)

#### A-3-1. db.js 함수 추가

**대상 파일:** `src/utils/db.js`

- [ ] `getAllReports(type?)` 함수 추가 — type 미지정 시 전체, 지정 시 해당 type 필터링, 날짜 역순 정렬
- [ ] `deleteReport(id)` 함수 추가 — IndexedDB에서 해당 리포트 삭제
- [ ] 검증: 저장 후 `getAllReports()` 호출 시 목록 반환 확인

#### A-3-2. ReportHistoryDrawer.jsx 신규

**대상 파일:** `src/components/reports/ReportHistoryDrawer.jsx`

- [x] 파일 첫 줄 한국어 역할 주석 작성
- [x] Props: `open`, `onClose`, `userId` (userId 추가)
- [x] shadcn/ui `Sheet` 기반 슬라이드 패널
- [x] 과거 리포트 목록 표시: 날짜 · 타입(주간/월간) · 제목
- [x] 목록 항목 클릭 시 리포트 전문 텍스트 렌더링 (whitespace-pre-wrap)
- [x] 각 항목 삭제 버튼 → `deleteReport(id)` 호출 후 목록 갱신
- [x] 목록 비어있을 때 빈 상태 메시지 표시
- [x] 검증: 리포트 저장 후 목록에 표시, 삭제 후 제거 확인 (수동 검증 필요)

#### A-3-3. Reports.jsx — 히스토리 뷰 연결

**대상 파일:** `src/pages/Reports.jsx`

- [x] `ReportHistoryDrawer` 컴포넌트 import 추가
- [x] `historyOpen` 상태 추가
- [x] AI 인사이트 탭에 "히스토리" 버튼 추가 → `historyOpen = true`
- [x] `<ReportHistoryDrawer open={historyOpen} onClose={() => setHistoryOpen(false)} userId={currentUser?.id} />` 렌더링
- [x] 검증: 버튼 클릭 → Drawer 열림·닫힘 정상 동작 확인 (수동 검증 필요)

---

## Phase B — P2 중요 기능

### B-1. 리스크 지표 KPI 카드 (계획 2-4)

#### B-1-1. calculator.js 유틸 추가

**대상 파일:** `src/utils/calculator.js`

- [x] `calculateMDD(cumulativeReturnSeries)` 함수 추가
  - 입력: `[{ date, value }]` 형태의 누적 수익률 시계열
  - 출력: 최대낙폭 (%) — 음수값
- [x] `calculateVolatility(dailyReturnSeries)` 함수 추가
  - 입력: `[dailyReturn]` 형태의 일간 수익률 배열
  - 출력: 연환산 변동성 (%) — `σ × √252`
- [x] 데이터 2개 미만일 때 `null` 반환 처리
- [ ] 검증: 알려진 시계열로 수동 계산값과 비교 (수동 검증 필요)

#### B-1-2. RiskKpiCards.jsx 신규

**대상 파일:** `src/components/reports/RiskKpiCards.jsx`

- [x] 파일 첫 줄 한국어 역할 주석 작성
- [x] Props: `snapshots`, `dateRange`
- [x] `dailyPnlStore` 스냅샷으로 기간 내 누적 수익률 시계열 구성
- [x] MDD 카드: 값 없을 때 "데이터 부족" 표시
- [x] 변동성 카드: 값 없을 때 "데이터 부족" 표시
- [ ] 검증: 스냅샷 충분할 때 수치 표시, 부족할 때 fallback 표시 (수동 검증 필요)

#### B-1-3. Reports.jsx — 리스크 KPI 연결

**대상 파일:** `src/pages/Reports.jsx`

- [x] `RiskKpiCards` import 추가
- [x] `useDailyPnlStore` import 및 `snapshots` 전달
- [x] 기존 KPI 4종 카드 그리드 아래에 `<RiskKpiCards />` 배치
- [ ] 검증: KPI 카드 레이아웃 깨짐 없는지 확인 (수동 검증 필요)

---

### B-2. 수익/손실 히트맵 캘린더 (계획 2-5)

#### B-2-1. PnlHeatmapCalendar.jsx 신규

**대상 파일:** `src/components/reports/PnlHeatmapCalendar.jsx`

- [x] 파일 첫 줄 한국어 역할 주석 작성
- [x] Props: `entries`, `year`, `month`
- [x] 날짜별 실현손익 집계 — `entries` groupBy date → sum pnl
- [x] CSS Grid 7열로 월 캘린더 렌더링 (일~토)
- [x] 수익 날짜: 빨강 계열 (`bg-red-100 ~ bg-red-500`) — 손익 크기에 비례
- [x] 손실 날짜: 파랑 계열 (`bg-blue-100 ~ bg-blue-500`) — 손익 크기에 비례
- [x] 거래 없는 날: `bg-gray-100 dark:bg-gray-800`
- [x] 날짜 셀 hover 시 tooltip으로 손익 금액 표시
- [x] 월 이동 버튼 (이전/다음 달)
- [ ] 검증: 수익 빨강·손실 파랑 한국 색상 컨벤션 준수 확인 (수동 검증 필요)

#### B-2-2. Reports.jsx — 히트맵 연결

**대상 파일:** `src/pages/Reports.jsx`

- [x] `PnlHeatmapCalendar` import 추가
- [x] 심리분석 탭 상단에 `<PnlHeatmapCalendar entries={filteredEntries} />` 배치
- [x] `year`, `month` 상태 추가 (기본: 현재 연월)
- [ ] 검증: 데이터 없는 달 빈 캘린더 표시 확인 (수동 검증 필요)

---

### B-3. 거래 패턴 분석 (계획 2-6)

#### B-3-1. calculator.js 유틸 추가

**대상 파일:** `src/utils/calculator.js`

- [x] `calcDayOfWeekStats(entries)` 함수 추가 — 요일(1=월~5=금)별 평균 손익·건수 반환
- [x] `calcHoldingPeriodDistribution(entries)` 함수 추가 — 매수→매도 보유일 수 계산, 구간(`≤1일 / 2~7일 / 8~30일 / 30일+`) 분포 반환
- [x] `calcSectorStats(entries, tickerSectorMap)` 함수 추가 — 업종별 평균 손익 반환

#### B-3-2. Reports.jsx — 거래 패턴 섹션 추가

**대상 파일:** `src/components/reports/PsychologyAnalysis.jsx` 하단

- [x] 요일별 평균 수익률 BarChart 추가 (토·일 데이터 없으면 숨김)
- [x] 보유기간 분포 BarChart 추가
- [x] 업종별 평균 수익률 HorizontalBarChart 추가 (entries의 sector 필드 활용)
- [ ] 검증: entries 없을 때 빈 상태 처리 확인 (수동 검증 필요)

---

### B-4. 연간 목표 달성률 게이지 (계획 2-7)

#### B-4-1. settingsStore.js 필드 추가

**대상 파일:** `src/store/settingsStore.js`

- [x] `annualTargetReturn: 10` 필드 추가 (기본값 10%)
- [x] `setAnnualTargetReturn(value)` 액션 추가
- [ ] persist 버전 업 및 migrate 처리 (zustand persist 자동 병합으로 생략)
- [ ] 검증: 설정 저장 후 reload 시 유지 확인 (수동 검증 필요)

#### B-4-2. Reports.jsx — 목표 달성률 게이지 추가

**대상 파일:** `src/pages/Reports.jsx`

- [x] `useSettingsStore`에서 `annualTargetReturn` 조회
- [x] 달성률 계산: `Math.min((totalReturn / annualTargetReturn) * 100, 100)`
- [x] Tailwind 커스텀 게이지 바 표시 (Progress 미설치로 직접 구현)
- [x] 목표 수치 인라인 편집 (클릭 → input 전환 → Enter/blur 시 저장)
- [x] 연말 예상 수익률 계산: `totalReturn / 경과일 × 365`
- [ ] 검증: 목표 수정 → 게이지 즉시 갱신 확인 (수동 검증 필요)

---

## Phase C — P3 개선 (여유 시)

### C-1. 월간 리포트 자동 트리거 (계획 2-8)

**대상 파일:** `src/pages/Reports.jsx`

- [x] `useEffect`에서 `shouldGenerateMonthlyReport(lastMonthlyReport?.createdAt)` 체크
- [x] 새 달 진입 시 상단에 "월간 리포트를 생성할까요?" 배너 표시
- [x] 배너에서 생성 / 건너뜀 선택 가능
- [ ] 검증: 저장된 월간 리포트 날짜 조작 후 배너 표시 확인 (수동 검증 필요)

---

### C-2. 벤치마크 추가 (계획 2-9)

**대상 파일:** `src/pages/Reports.jsx`, `src/api/stockApi.js`

- [x] 벤치마크 선택 상태 추가 (`['KOSPI']` 기본)
- [x] KOSDAQ, NASDAQ 벤치마크 데이터 fetch 함수 추가 (`stockApi.js`)
- [x] 성과추이 탭에 벤치마크 선택 토글 버튼 추가
- [x] 선택된 벤치마크만 차트에 렌더링
- [ ] 검증: 벤치마크 토글 시 차트 라인 즉시 추가·제거 확인 (수동 검증 필요)

---

### C-3. 리포트 PDF 구조 개선 (계획 2-10)

**대상 파일:** `src/utils/exportReport.js`

- [x] 현재 html2canvas 전체 캡처 방식 구조 파악
- [x] 섹션별 캡처 후 PDF 합치는 방식으로 개선
- [x] 표지 (날짜·수익률 요약) 추가
- [ ] 검증: PDF 생성 후 내용 깨짐 없는지 확인 (수동 검증 필요)

---

## 완료 기준

| Phase | 완료 조건 |
|-------|-----------|
| A (P1) | 커스텀 날짜 피커 동작, 심리분석 탭 모든 섹션 렌더링, AI 리포트 히스토리 저장·조회·삭제 |
| B (P2) | MDD·변동성 KPI 표시, 히트맵 캘린더 색상 정상, 거래 패턴 차트 렌더링, 목표 게이지 동작 |
| C (P3) | 월간 자동 배너, 벤치마크 토글, PDF 표지 추가 |

---

## 수동 확인 항목 (완료 후 사용자 검토 필요)

- [ ] 심리 분석 차트 — 실제 거래 데이터로 의미있는 인사이트가 도출되는지 확인
- [ ] 히트맵 캘린더 — 수익/손실 날짜 색상이 직관적인지 확인
- [ ] MDD 수치 — 실제 느꼈던 최대 하락과 근사한지 확인
- [ ] 목표 달성률 게이지 — 기본값(10%) 및 연말 예상 수익률 계산식 합리적인지 확인
- [ ] AI 리포트 히스토리 — 과거 리포트 정상 저장·조회·삭제 확인
