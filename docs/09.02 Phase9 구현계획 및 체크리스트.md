# Phase 9 — 구현계획 & 체크리스트

> 설계 문서: `docs/09.01 유진투자증권 HTS 거래내역 연동 리서치.md` 참조
> 작성일: 2026-04-27
> 범위: 유진투자증권 HTS Excel 수동 다운로드 → my-portfolio-hub 일괄 import (MVP)

---

## 전체 진행 현황

| Sprint | 범위 | 상태 | 완료일 |
|--------|------|------|--------|
| 9.1 | 의존성 + 파싱 유틸 (`xlsx`, htsParser, columnDetector) | ⬜ 대기 | - |
| 9.2 | 중복 방지 + 스토어 스키마 확장 (externalId, source) | ⬜ 대기 | - |
| 9.3 | UI — 드롭존 + 컬럼 매퍼 + 미리보기 | ⬜ 대기 | - |
| 9.4 | UI — 확정 다이얼로그 + ImportHts 페이지 + 라우팅 | ⬜ 대기 | - |
| 9.5 | 보유종목 재계산 + 심리 일괄설정 + 결과 요약 | ⬜ 대기 | - |
| 9.6 | 테스트 + 문서 + 마무리 | ⬜ 대기 | - |

---

## 사전 확인 (사용자 액션 필요)

- [ ] **샘플 HTS Excel 파일 1개 제공** (1개월치, 계좌번호/주민번호 마스킹 후)
  - 사유: 정확한 컬럼명 / 헤더 행 위치 / 날짜 포맷 확정
- [ ] HTS 화면명 확인 (거래내역 / 실현손익 / 잔고)
- [ ] 해외주식 거래 포함 여부 (포함 시 통화 처리 추가 설계)
- [ ] `npm install xlsx` 패키지 추가 승인

---

## Sprint 9.1 — 의존성 + 파싱 유틸

### 9.1-A. 의존성 추가

- [ ] `npm install xlsx` 실행 (사용자 승인 후)
- [ ] `package.json`에 `xlsx` 의존성 등록 확인
- [ ] 라이선스 확인 (Apache-2.0)

### 9.1-B. HTS 파서

> 파일: `src/utils/htsParser.js` (신규)

- [ ] `parseHtsWorkbook(file: File): Promise<ParsedSheet[]>` 작성
  - SheetJS `read(arrayBuffer, { type: 'array', cellDates: true })`
  - 모든 시트 → `{ sheetName, rows: object[], headerRow: number }` 배열
- [ ] 빈 행/합계 행 자동 skip
- [ ] 종목코드 문자열 강제 변환 + `padStart(6, '0')`
- [ ] 숫자 컬럼 콤마 제거 후 `Number()` 변환

### 9.1-C. 컬럼 자동 감지

> 파일: `src/utils/htsColumnDetector.js` (신규)

- [ ] 한글 컬럼명 사전 작성
  ```js
  const COLUMN_DICTIONARY = {
    date: ['거래일자', '체결일자', '일자', '날짜'],
    ticker: ['종목코드', '단축코드'],
    name: ['종목명', '종목'],
    action: ['매매구분', '구분', '거래구분'],
    quantity: ['체결수량', '수량'],
    price: ['체결단가', '단가', '체결가'],
    fee: ['수수료'],
    tax: ['세금', '제세금'],
    orderNo: ['주문번호'],
  }
  ```
- [ ] `detectColumns(rows): { [field]: columnName | null }` 함수
- [ ] `normalizeAction(value)`: `'매수' → 'buy'`, `'매도' → 'sell'`
- [ ] `normalizeDate(value)`: `Date | string` → `'YYYY-MM-DD'`

### 9.1 완료 검증

- [ ] 샘플 파일로 `parseHtsWorkbook` 호출 시 시트 배열 정상 반환
- [ ] `detectColumns` 자동 매핑 정확도 80% 이상
- [ ] 단위 테스트 (`src/tests/htsParser.test.js`)
  - [ ] 정상 행 파싱
  - [ ] 합계 행 skip
  - [ ] 종목코드 zero-pad

---

## Sprint 9.2 — 중복 방지 + 스토어 스키마 확장

### 9.2-A. externalId 생성

> 파일: `src/utils/importDeduplicator.js` (신규)

- [ ] `generateExternalId(entry): Promise<string>` 작성
  - 입력: `{ date, ticker, action, quantity, price, orderNo? }`
  - Web Crypto API (`crypto.subtle.digest('SHA-256', ...)`)
  - 반환: hex string
- [ ] `findDuplicates(incoming, existing): { newEntries, duplicates }`
- [ ] orderNo 부재 시 fallback 키 사용 (`date|ticker|action|qty|price`)

### 9.2-B. journalStore 스키마 확장

> 파일: `src/store/journalStore.js` (수정)

- [ ] entry 스키마에 옵셔널 필드 추가 (하위호환)
  - `fee?: number`
  - `tax?: number`
  - `source?: 'manual' | 'eugene-hts'` (기본 `'manual'`)
  - `importedAt?: string` (ISO)
  - `externalId?: string`
- [ ] `addEntriesBulk(entries: Entry[])` 액션 추가 (Immer)
- [ ] `findEntryByExternalId(id)` selector 추가

### 9.2-C. importStore 신규

> 파일: `src/store/importStore.js` (신규)

- [ ] Zustand store 작성 (persist 미사용 — 세션 한정)
  - `parsedSheets: ParsedSheet[]`
  - `selectedSheet: string | null`
  - `columnMapping: { [field]: columnName }`
  - `previewRows: PreviewRow[]` (`{ entry, isDuplicate, isExcluded }`)
  - `bulkPsychology: string | null`
- [ ] 액션: `setParsedSheets`, `setColumnMapping`, `togglePreviewRow`, `setBulkPsychology`, `reset`

### 9.2 완료 검증

- [ ] 동일 입력 2회 import 시 두 번째는 전부 중복 표시
- [ ] `addEntriesBulk` 호출 후 `journalStore.entries` 길이 정상 증가
- [ ] 단위 테스트
  - [ ] `generateExternalId` 동일 입력 → 동일 해시
  - [ ] `findDuplicates` 분류 정확

---

## Sprint 9.3 — UI: 드롭존 + 매퍼 + 미리보기

### 9.3-A. 파일 드롭존

> 파일: `src/components/import/HtsFileDropzone.jsx` (신규)

- [ ] 드래그앤드롭 영역 (Tailwind 점선 테두리)
- [ ] 클릭 시 파일 선택 다이얼로그
- [ ] `.xlsx` / `.xls` 확장자 검증
- [ ] 파일 선택 시 `parseHtsWorkbook` 호출 → `importStore.setParsedSheets`
- [ ] 로딩 스피너 표시 (`LoadingSpinner` 재사용)
- [ ] 에러 토스트 (잘못된 파일 형식)

### 9.3-B. 컬럼 매퍼

> 파일: `src/components/import/ImportColumnMapper.jsx` (신규)

- [ ] 시트 선택 셀렉트 (다중 시트인 경우)
- [ ] 자동 매핑 결과 표시
- [ ] 필드별 셀렉트 박스로 사용자 수정 가능
- [ ] 필수 필드(date, ticker, action, quantity, price) 미매핑 시 경고

### 9.3-C. 미리보기 테이블

> 파일: `src/components/import/ImportPreviewTable.jsx` (신규)

- [ ] 매핑 결과 기반 행 렌더링 (shadcn/ui `Table`)
- [ ] 중복 행: 회색 배경 + "중복" 배지, 기본 제외 체크
- [ ] 신규 행: 흰 배경 + "신규" 배지
- [ ] 행별 체크박스로 개별 제외/포함 토글
- [ ] 상단 요약 바: `신규 N건 / 중복 M건 / 제외 K건`
- [ ] 페이지네이션 (50행 단위)

### 9.3 완료 검증

- [ ] 샘플 파일 드롭 → 매퍼 → 미리보기 흐름 정상
- [ ] 컬럼 수동 변경 시 미리보기 즉시 반영
- [ ] 100행 이상 미리보기에서 페이지네이션 동작

---

## Sprint 9.4 — 확정 다이얼로그 + 페이지 + 라우팅

### 9.4-A. 확정 다이얼로그

> 파일: `src/components/import/ImportConfirmDialog.jsx` (신규)

- [ ] shadcn/ui `Dialog` 사용
- [ ] 요약 표시: `추가될 N건 / 중복 M건 (skip) / 보유 종목 P건 변경`
- [ ] 심리 일괄 설정 셀렉트 (옵션, 기본 "기타")
- [ ] "취소" / "적용" 버튼
- [ ] 적용 시 `journalStore.addEntriesBulk` 호출
- [ ] 적용 후 결과 토스트 + `/journal` 이동

### 9.4-B. ImportHts 페이지

> 파일: `src/pages/ImportHts.jsx` (신규)

- [ ] 단계 진행 표시 (1. 파일 → 2. 매핑 → 3. 미리보기 → 4. 확정)
- [ ] 각 단계 컴포넌트 조건부 렌더링
- [ ] "이전/다음" 네비게이션 버튼
- [ ] 페이지 상단 안내 (HTS 엑셀 저장 방법 링크)

### 9.4-C. 라우팅 + 사이드바 메뉴

> 파일: `src/App.jsx`, `src/components/common/Sidebar.jsx` (수정)

- [ ] React Router에 `/import/hts` 라우트 추가
- [ ] Sidebar에 "거래내역 가져오기" 메뉴 추가 (lucide-react `Upload` 아이콘)

### 9.4 완료 검증

- [ ] `/import/hts` 라우트 직접 접근 가능
- [ ] 사이드바 메뉴 클릭 → 페이지 이동
- [ ] 4단계 흐름 끝까지 진행 후 일지 페이지에서 데이터 확인

---

## Sprint 9.5 — 보유종목 재계산 + 결과 요약

### 9.5-A. 보유종목 재계산 유틸

> 파일: `src/utils/recomputeHoldings.js` (신규)

- [ ] `recomputeHoldingsFromEntries(entries): Holding[]` 작성
  - FIFO 가정, 종목별 그룹핑
  - 매수: 평균단가 가중평균 갱신
  - 매도: 보유수량 차감 (음수 방지)
  - 0 수량 종목은 결과 제외
- [ ] 단위 테스트
  - [ ] 매수 1회 → holding 1건
  - [ ] 매수 후 부분 매도 → 수량 감소
  - [ ] 매수/매도 평형 → holding 0건

### 9.5-B. portfolioStore 통합

> 파일: `src/store/portfolioStore.js` (수정)

- [ ] `recomputeFromJournal()` 액션 추가
- [ ] import 확정 후 옵션 체크박스로 호출 ("보유 종목도 재계산")

### 9.5-C. 결과 요약 모달

> 파일: `src/components/import/ImportResultDialog.jsx` (신규)

- [ ] 추가/중복/오류/보유변경 건수 표시
- [ ] "일지 보기" / "포트폴리오 보기" 버튼

### 9.5 완료 검증

- [ ] 매수 10주 + 매도 5주 import → 보유 5주
- [ ] 옵션 체크 안 하면 portfolioStore 변경 없음

---

## Sprint 9.6 — 테스트 + 문서 + 마무리

### 9.6-A. 통합 테스트

- [ ] 샘플 파일 기반 E2E 시나리오 1회 수동 진행
- [ ] 동일 파일 재import → 전부 중복 처리 확인
- [ ] 일부 행 제외 → 제외분만 skip 확인

### 9.6-B. 문서 업데이트

- [ ] `CLAUDE.md` Phase 로드맵에 Phase 9 추가
- [ ] `README.md`에 거래내역 가져오기 사용법 1단락 추가
- [ ] 이 문서 진행 현황 표 모두 ✅ 처리

### 9.6-C. 보안 체크리스트

- [ ] 업로드 파일은 브라우저 내 처리, 외부 전송 없음 (Network 탭 확인)
- [ ] 주문번호/계좌번호는 LocalStorage에 평문 저장하지 않음 (해시만 저장)
- [ ] AI 분석 전송 시 마스킹 함수 적용 확인 (Phase 10 이슈로 분리)

### 9.6 완료 검증

- [ ] `npm run dev` → 전체 플로우 정상 동작
- [ ] `npm run build` 성공
- [ ] `npm test` 전체 통과

---

## 신규 파일 목록 요약

```
src/
├── pages/
│   └── ImportHts.jsx
├── components/import/
│   ├── HtsFileDropzone.jsx
│   ├── ImportColumnMapper.jsx
│   ├── ImportPreviewTable.jsx
│   ├── ImportConfirmDialog.jsx
│   └── ImportResultDialog.jsx
├── utils/
│   ├── htsParser.js
│   ├── htsColumnDetector.js
│   ├── importDeduplicator.js
│   └── recomputeHoldings.js
├── store/
│   └── importStore.js
└── tests/
    ├── htsParser.test.js
    ├── importDeduplicator.test.js
    └── recomputeHoldings.test.js
```

수정 파일: `src/App.jsx`, `src/components/common/Sidebar.jsx`, `src/store/journalStore.js`, `src/store/portfolioStore.js`, `package.json`, `CLAUDE.md`, `README.md`

---

## journalStore 확장 entry 합성 예시

```js
{
  date: '2026-04-01',          // YYYY-MM-DD
  ticker: '005930',             // 6자리 zero-pad string
  name: '삼성전자',
  action: 'buy',                // 'buy' | 'sell'
  price: 72000,
  quantity: 10,
  psychology: '기타',           // 사용자 추후 보강
  memo: '',
  result_pnl: null,
  fee: 1080,
  tax: 1440,
  source: 'eugene-hts',
  importedAt: '2026-04-27T12:00:00Z',
  externalId: '<sha256 hex>'
}
```

---

## 브랜치 / 커밋 전략

- 브랜치: `feature/phase9-hts-import` (develop 분기)
- 커밋 단위: Sprint 단위 (9.1~9.6) — 각 Sprint 완료 시 1 커밋
- PR: develop ← feature, Sprint 전체 완료 후 1개 PR

---

## 리스크 트래킹

| 리스크 | 발생 시점 | 대응책 |
|---|---|---|
| 샘플 파일 미확보 | Sprint 9.1 | 사용자 제공 대기, mock 데이터로 개발 병행 |
| HTS 버전별 컬럼 차이 | Sprint 9.3 | 컬럼 사전 확장 + 사용자 수동 매핑 |
| 대용량 파일 freeze | Sprint 9.6 | Web Worker 분리 (Phase 10 후속) |
| 외화 거래 처리 | Sprint 9.5 | currency 필드 분리, 환율 미적용 명시 |

---

*마지막 업데이트: 2026-04-27*
