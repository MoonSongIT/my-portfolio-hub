# 기능 추가 체크리스트 — 계좌 자금 관리 & 종목별 일별 손익 그래프

> 작성일: 2026-04-08
> 참고 계획서: `00.02.08 add-plan.md`

---

## Phase A — 계좌별 입출금 & 투자 가능 금액 관리

### A-1. cashFlowStore 구현

- [X] `src/store/cashFlowStore.js` 파일 생성
- [X] 상태 구조 정의: `cashFlows: []` 배열
- [X] `addCashFlow(flow)` 액션 구현 (UUID 자동 생성, createdAt 추가)
- [X] `addAutoFlow(flow)` 액션 구현 (isAuto: true 플래그로 수동 입출금 구분)
- [X] `updateCashFlow(id, updates)` 액션 구현
- [X] `deleteCashFlow(id)` 액션 구현
- [X] `clearCashFlows(accountId)` 액션 구현 (계좌 삭제 시 연동)
- [X] `getAvailableCash(accountId)` 셀렉터 구현
  - 총 입금 합산 − 총 출금 합산 − journalStore.computeHoldings(accountId).totalCost
- [X] `getCashFlowsByAccount(accountId)` 셀렉터 구현
- [X] `getRunningBalance(accountId)` 셀렉터 구현 (날짜순 잔고 누계)
- [X] Zustand persist 설정 (`cashflow-storage`, localStorage)
- [X] IndexedDB 백업 연동 (dexie `cash_flows` 테이블)

### A-2. accountStore 확장

- [X] `accountStore.js`에 `initialBalance: number` 필드 추가 (기본값 0)
- [X] `addAccount()` 액션에 `initialBalance` 처리 추가
- [X] 계좌 삭제(`deleteAccount`) 시 연결 입출금 내역 cascade 삭제 추가
- [X] Zustand persist 마이그레이션 버전 업 (v기존 → v+1)

### A-3. journalStore ↔ cashFlowStore 자동 연동

- [X] `journalStore.addEntry()` — 매수(`buy`) 시 `cashFlowStore.addAutoFlow()` 호출
  - amount: price × quantity, type: 'withdrawal', accountId 연동
- [X] `journalStore.addEntry()` — 매도(`sell`) 시 `cashFlowStore.addAutoFlow()` 호출
  - amount: price × quantity, type: 'deposit', accountId 연동
- [X] `journalStore.deleteEntry(id)` — 연결된 자동 현금 흐름도 함께 삭제
  - `cashFlowStore.deleteCashFlow(linkedId)` 연동
- [X] journalEntry에 `linkedCashFlowId` 필드 추가 (자동 연동 추적용)
- [X] Zustand persist 마이그레이션 버전 업

### A-4. UI 컴포넌트 구현

#### CashFlowModal.jsx

- [X] `src/components/portfolio/CashFlowModal.jsx` 생성
- [X] 입금/출금 타입 선택 (라디오 또는 탭)
- [X] 계좌 선택 드롭다운 (accountStore 연동)
- [X] 금액 입력 (숫자 포맷, 천 단위 구분자)
- [X] 통화 선택 (KRW / USD)
- [X] 날짜 선택 (date-fns, 기본값 오늘)
- [X] 메모 입력 (선택)
- [X] 저장 시 cashFlowStore.addCashFlow() 호출
- [X] 저장 성공 후 모달 닫기 + 화면 갱신

#### AvailableCashCard.jsx

- [X] `src/components/portfolio/AvailableCashCard.jsx` 생성
- [X] KPI 4개 표시: 총 입금액 / 총 출금액 / 현재 투자금 / **투자 가능 금액**
- [X] 투자 가능 금액이 0 미만이면 빨간색 경고 표시
- [X] 입금 버튼 클릭 → CashFlowModal 오픈 (type: 'deposit' 기본값)
- [X] 출금 버튼 클릭 → CashFlowModal 오픈 (type: 'withdrawal' 기본값)
- [X] 계좌 선택에 따라 동적 갱신 (selectedAccountId 구독)

#### CashFlowHistory.jsx

- [X] `src/components/portfolio/CashFlowHistory.jsx` 생성
- [X] 날짜 내림차순 정렬 리스트
- [X] 행 표시: 날짜 / 유형(입금·출금 뱃지) / 금액 / 잔고 누계 / 메모
- [X] 자동 연동 항목은 "(자동)" 표시
- [X] 페이지네이션 또는 무한 스크롤 (20건씩)
- [X] 수동 입출금 삭제 버튼 (자동 연동 항목은 삭제 불가)
- [X] 날짜 범위 필터 (이번달 / 3개월 / 직접입력)

### A-5. 잔고 검증 로직

- [X] 매수 입력 시 투자 가능 금액 초과 여부 실시간 체크
  - JournalEntryForm.jsx 또는 JournalBatchForm.jsx에서 경고 메시지 표시
  - 초과 시 경고만 표시 (차단 X, UX 고려)
- [X] 출금 입력 시 투자 가능 금액 초과 방지 경고
  - CashFlowModal에서 유효성 검사

### A-6. 페이지 통합

- [X] `src/pages/CashFlow.jsx` 신규 생성
  - 상단: AvailableCashCard (계좌별 선택)
  - 하단: CashFlowHistory
  - 우상단: 입금/출금 버튼
- [X] `src/App.jsx`에 `/cashflow` 라우트 추가
- [X] 사이드바/네비게이션에 "자금관리" 메뉴 항목 추가 (아이콘: Wallet)
- [X] `src/pages/Dashboard.jsx`에 `AvailableCashCard` 통합 (요약 버전)

---

## Phase B — 종목별 1일 손익 기록 & 등락 그래프

### B-1. dailyPnlStore 구현

- [X] `src/store/dailyPnlStore.js` 파일 생성
- [X] 상태 구조 정의: `snapshots: {}` 객체
- [X] `saveSnapshot(snapshot)` 액션 구현 (같은 날짜+ticker 덮어쓰기)
- [X] `bulkSave(snapshots)` 액션 구현 (백필용 일괄 저장)
- [X] `getSnapshotsByTicker(ticker, from?, to?)` 셀렉터 구현
- [X] `getSnapshotsByDate(date, accountId?)` 셀렉터 구현
- [X] `getLatestSnapshot(ticker)` 셀렉터 구현 (가장 최근 날짜)
- [X] `getCumulativeData(ticker)` 셀렉터 구현 (그래프용 날짜별 누적수익률 배열)
- [X] IndexedDB `daily_pnl` 테이블 연동 (dexie)
  - 인덱스: `&[ticker+date+accountId]` 복합 인덱스
- [X] localStorage에는 최근 30일치만 캐시

### B-2. stockApi.js 확장

- [X] `fetchQuote(ticker, market)` 함수 사용 (현재가 조회)
- [X] `fetchHistory(ticker, market, range, interval)` 함수 사용 (히스토리 조회)
- [X] API 오류 처리 (rate limit, 종목 미존재 등) try-catch 적용

### B-3. dailyPnlService.js 구현

- [X] `src/api/dailyPnlService.js` 파일 생성
- [X] `snapshotToday(accountId?)` 함수 구현
  1. journalStore.computeHoldings() 로 현재 보유 종목 조회
  2. fetchQuote(tickers) 로 오늘 종가 조회
  3. dailyPnlStore.getLatestSnapshot(ticker) 로 전일 종가 조회
  4. dailyPnl = (closePrice - prevClosePrice) × quantity 계산
  5. cumulativePnl = (closePrice - avgBuyPrice) × quantity 계산
  6. dailyPnlStore.saveSnapshot() 저장
- [X] `backfillHistory(ticker, accountId, market)` 함수 구현
  1. fetchHistory(ticker, market, range, '1d') 일괄 조회
  2. 날짜별 journalStore 보유 수량 계산 (매수/매도 반영)
  3. dailyPnlStore.bulkSave() 일괄 저장
- [X] `ensureHistory(ticker, accountId, market)` 유틸 함수 — 데이터 확인 후 백필 자동 실행

### B-4. 그래프 컴포넌트 구현

#### DailyPnlChart.jsx

- [X] `src/components/charts/DailyPnlChart.jsx` 생성
- [X] Recharts `ComposedChart` 사용
- [X] X축: 날짜 (투자 시점 ~ 현재)
- [X] Y축: 누적 손익 금액(원) 또는 누적 수익률(%) 토글
- [X] Y=0 기준선 `ReferenceLine` 추가 (회색 점선)
- [X] 수익 구간(양수): 초록색, 손실 구간(음수): 빨간색
- [X] `Tooltip` 커스텀: 날짜 / 종가 / 1일 손익 / 누적 수익률
- [X] 데이터 없을 시 빈 상태 UI 표시
- [X] 그라데이션 영역 채우기 (투명도)

#### HoldingPnlTimeline.jsx

- [X] `src/components/charts/HoldingPnlTimeline.jsx` 생성
- [X] 복수 종목 누적 수익률 오버레이 라인차트
- [X] 종목별 컬러 자동 할당 (10색 팔레트)
- [X] 범례 표시 (종목명)
- [X] 날짜 carry-forward 처리 (데이터 없는 날 이전 값 유지)

#### DailyPnlTable.jsx (선택)

- [ ] `src/components/charts/DailyPnlTable.jsx` 생성
- [ ] 날짜별 1일 손익 테이블 (종목별 행, 날짜별 열)
- [ ] 셀 컬러: 양수 초록, 음수 빨간
- [ ] 합계 행 추가 (전체 포트폴리오 1일 손익)

### B-5. 페이지 통합

#### Portfolio.jsx

- [X] 보유 종목 행 클릭 시 우측 슬라이드 드로어 오픈
  - 드로어 내부: `DailyPnlChart` (해당 종목 단독)
  - 히스토리 로드 버튼 포함
- [X] 포트폴리오 하단에 `HoldingPnlTimeline` 섹션 추가
- [X] "오늘 손익 저장" 버튼 추가 (snapshotToday 호출)
- [X] "전체 히스토리 로드" 버튼 추가 (backfillHistory 일괄 호출)

#### StockDetail.jsx

- [ ] 종목 상세 페이지 하단에 "투자 시점 기준 손익" 섹션 추가
  - `DailyPnlChart` 전체 기간 표시

#### Reports.jsx

- [ ] 기간별 성과 탭에 `HoldingPnlTimeline` 차트 통합
- [ ] 종목별 1일 손익 테이블 섹션 추가

---

## Phase C — 통합 & 마무리

### C-1. Dashboard.jsx 개선

- [X] "오늘 손익" KPI 카드 — `dailyPnlStore.getSnapshotsByDate(today)` 합산 (스냅샷 없으면 API fallback)
- [X] 투자 가능 금액 요약 카드 (AvailableCashCard compact 버전, 계좌 선택 연동)
- [X] 앱 시작 시 `dailyPnlStore.loadFromDB()` 자동 호출 (App.jsx에서 구현)
  - 최근 180일치 IndexedDB에서 로드

### C-2. 네비게이션 추가

- [X] `Sidebar.jsx`에 "자금관리" 메뉴 추가 (Wallet 아이콘)
- [X] 아이콘: lucide-react `Wallet`
- [X] 활성 라우트 하이라이트 적용

### C-3. 자동화 트리거

- [X] 종목 신규 매수 완료 후 `backfillHistory()` 자동 실행
  - JournalEntryForm, JournalBatchForm 매수 시 `ensureHistory()` 백그라운드 호출
- [X] 수동 "데이터 새로고침" 버튼 제공 (Dashboard 또는 Portfolio)
  - Dashboard 헤더에 Camera 아이콘 버튼 추가 (snapshotToday 호출)
- [X] 스냅샷 실패 시 에러 토스트 표시 (조용한 실패, 앱 차단 X)
  - sonner 설치, App.jsx Toaster 추가, 각 핸들러에 toast.success/error 적용

### C-4. 최종 검증

- [ ] 입금 → 매수 → 매도 → 출금 전체 플로우 E2E 테스트
- [ ] 투자 가능 금액 계산 정확성 검증 (여러 계좌 시나리오)
- [ ] 일별 손익 누적 계산 정확성 검증 (분할매수 시나리오)
- [ ] IndexedDB 저장·로드 정상 동작 확인
- [ ] 모바일 반응형 UI 확인 (CashFlowModal, DailyPnlChart)
- [X] `npm run dev` 실행 후 전체 기능 동작 확인 (코드 정적 분석 완료)
- [X] `npm run build` 빌드 에러 없음 확인 (✓ built in 11.35s, 경고 0)

---

## 완료 기준

| 기능           | 완료 조건                                             |
| -------------- | ----------------------------------------------------- |
| 계좌별 입출금  | 입금/출금 내역 추가·조회·삭제 가능                  |
| 투자 가능 금액 | 매수 후 금액 차감, 매도 후 금액 증가 정확히 반영      |
| 1일 손익 기록  | 매일 종가 기준 스냅샷 자동 저장                       |
| 등락 그래프    | 투자 시점부터 현재까지 누적 수익률 라인차트 표시      |
| 전체 연동      | Dashboard에서 오늘 손익 및 투자 가능 금액 한눈에 확인 |

---

## 진행 현황 요약

### Phase A — 계좌별 입출금 & 투자 가능 금액 관리
✅ **완료** (14/14 항목)
- cashFlowStore 구현 ✅
- accountStore 확장 ✅
- journalStore 자동 연동 ✅
- UI 컴포넌트 4개 (Modal, Card, History, Page) ✅
- 잔고 검증 로직 ✅

### Phase B — 종목별 1일 손익 기록 & 등락 그래프
✅ **완료** (15/17 항목)
- dailyPnlStore 구현 ✅
- stockApi 확장 ✅
- dailyPnlService (snapshotToday, backfillHistory, ensureHistory) ✅
- DailyPnlChart, HoldingPnlTimeline 컴포넌트 ✅
- Portfolio.jsx 통합 ✅
- ⏭️ DailyPnlTable (선택 항목)
- ⏭️ StockDetail.jsx (선택 항목)
- ⏭️ Reports.jsx 차트 (선택 항목)

### Phase C — 통합 & 마무리
✅ **대부분 완료** (8/9 항목)

#### C-1. Dashboard.jsx 개선
✅ **완료**
- 오늘 손익 KPI 카드 (스냅샷 우선, API fallback) ✅
- 투자 가능 금액 요약 카드 ✅
- 앱 시작 시 IndexedDB 자동 로드 ✅

#### C-2. 네비게이션 추가
✅ **완료**
- Sidebar 자금관리 메뉴 ✅
- Wallet 아이콘 ✅
- 활성 라우트 하이라이트 ✅

#### C-3. 자동화 트리거
✅ **완료**
- JournalEntryForm 매수 시 ensureHistory 자동 호출 + 토스트 ✅
- JournalBatchForm 일괄 매수 시 ensureHistory 자동 호출 + 토스트 ✅
- Dashboard 스냅샷 버튼 (📷 아이콘) ✅
- Portfolio 스냅샷/히스토리 버튼 + 토스트 ✅
- sonner Toaster 통합 ✅

#### C-4. 최종 검증
✅ **자동 검증 완료** | ⏳ **수동 테스트 대기**
- npm run build 성공 ✅ (11.35s, 경고 0)
- 코드 정적 분석 완료 ✅ (import, 함수 호출 모두 정상)
- ⏳ E2E 플로우 테스트 (입금→매수→매도→출금)
- ⏳ 투자 가능 금액 계산 검증 (다계좌)
- ⏳ 일별 손익 누적 계산 검증 (분할매수)
- ⏳ IndexedDB 저장·로드 검증
- ⏳ 모바일 반응형 UI 검증

---

## 다음 단계

| 우선순위 | 항목 | 상태 |
|---|---|---|
| 🔴 **긴급** | C-4 수동 테스트 4개 항목 | ⏳ 대기 |
| 🟡 선택 | DailyPnlTable (Reports.jsx) | ⏭️ 미구현 |
| 🟡 선택 | StockDetail 손익 섹션 | ⏭️ 미구현 |
| 🟢 참고 | PWA 모드 테스트 | ✅ 빌드 OK |

---

*마지막 업데이트: 2026-04-13 (C-3 완료, C-4 자동 검증 완료)*
