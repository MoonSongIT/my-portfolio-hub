# Phase 4 상세 체크리스트 — Claude AI 에이전트 탑재 검증

> **목적**: Phase 4 구현 완료 후 각 단계별 수동 검증 수행
> **소요 시간**: 약 2~3시간
> **검증 환경**: Windows 11, Node.js v22+, Chrome/Edge 최신
> **참조**: [00.02.04 detail-plan.md](./00.02.04%20detail-plan.md)

---

## 사전 준비 체크리스트

### Phase 3 완료 확인

- [X] Phase 3 모든 항목 확인 (00.03.03 detail-check.md 참조)
- [X] `npm run dev` 정상 실행 확인
- [X] Dashboard 실시간 주가 정상 표시
- [X] Portfolio 보유 종목 실데이터 연동 확인

### 환경 변수 확인

- [ ] `.env` 파일에 `ANTHROPIC_API_KEY=sk-ant-...` 설정
- [ ] `.env.example`에 `ANTHROPIC_API_KEY=여기에_API_키_입력` 존재
- [ ] `.gitignore`에 `.env` 포함 확인
- [ ] `VITE_API_BASE_URL=http://localhost:3001/api` 설정 확인

### 패키지 설치 확인

- [ ] `express` — package.json dependencies에 존재
- [ ] `cors` — package.json dependencies에 존재
- [ ] `concurrently` — package.json devDependencies에 존재
- [ ] `react-markdown` — package.json dependencies에 존재
- [ ] `remark-gfm` — package.json dependencies에 존재
- [ ] `node_modules/express` 폴더 존재 (`npm ls express`)
- [ ] `node_modules/react-markdown` 폴더 존재

---

## Step 1: 로컬 Express 프록시 서버 검증

### 1-1. 파일 구조 확인

- [ ] `server/index.js` 파일 존재
- [ ] `api/claude.js` 파일 존재 (Vercel Serverless)
- [ ] `vercel.json` 파일 존재

### 1-2. server/index.js 코드 확인

- [ ] `express`, `cors`, `dotenv` import 확인
- [ ] `POST /api/claude` 엔드포인트 정의 확인
- [ ] CORS 허용 origin `http://localhost:5173` 설정 확인
- [ ] `process.env.ANTHROPIC_API_KEY` 사용 (하드코딩 없음)
- [ ] `model: 'claude-sonnet-4-6'` 설정 확인
- [ ] `max_tokens: 4096` 설정 확인
- [ ] 포트 3001로 listen 확인

### 1-3. 서버 실행 확인

- [ ] `npm run server` 실행 시 터미널에 "Claude 프록시 서버: http://localhost:3001" 출력
- [ ] `npm run dev:all` 명령어 존재 확인 (package.json scripts)
- [ ] `concurrently` 로 두 서버 동시 실행 확인

### 1-4. Vite 프록시 설정 확인

- [ ] `vite.config.js`에 `/api/claude` 프록시 정의
- [ ] target: `http://localhost:3001`
- [ ] `changeOrigin: true` 설정

### 1-5. 프록시 동작 테스트

```
검증 방법:
1. npm run dev:all 실행
2. 터미널에서 서버 정상 시작 메시지 확인 (포트 3001, 5173)
3. curl 또는 Postman으로 POST http://localhost:3001/api/claude 직접 호출
   Body: {"systemPrompt":"당신은 도움이 되는 AI입니다.","messages":[{"role":"user","content":"안녕하세요"}]}
4. AI 응답 JSON 수신 확인
```

- [ ] 직접 호출 시 `{ content: [{ text: "..." }] }` 형식 응답 수신
- [ ] Vite 프록시 경유 (`/api/claude`) 시 동일 응답 수신
- [ ] 브라우저 네트워크 탭에서 API 키 노출 없음 확인

---

## Step 2: Vercel Serverless Function 검증

### 2-1. api/claude.js 코드 확인

- [ ] `CORS 헤더` 설정 (`Access-Control-Allow-Origin: *`)
- [ ] `OPTIONS` 메서드 처리 (Preflight)
- [ ] `POST` 외 메서드 → 405 반환
- [ ] `systemPrompt`, `messages` 필수 파라미터 검증
- [ ] `process.env.ANTHROPIC_API_KEY` 사용 (하드코딩 없음)
- [ ] `anthropic-version: 2023-06-01` 헤더 포함
- [ ] 에러 응답 시 적절한 HTTP 상태코드 반환

### 2-2. vercel.json 설정 확인

- [ ] `api/claude.js` 함수 `maxDuration: 60` 설정
- [ ] rewrites 규칙 존재

### 2-3. Vercel 배포 환경변수 (배포 시 필수)

- [ ] Vercel 대시보드 → Settings → Environment Variables
- [ ] `ANTHROPIC_API_KEY` 환경변수 등록 확인 (Value 숨김 처리 확인)
- [ ] Production + Preview 환경 모두 적용 확인

---

## Step 3: 에이전트 시스템 프롬프트 검증

### 3-1. 파일 구조 확인

- [ ] `src/agents/orchestrator.js` 존재
- [ ] `src/agents/researchAgent.js` 존재
- [ ] `src/agents/portfolioAgent.js` 존재
- [ ] `src/agents/alertAgent.js` 존재
- [ ] `src/agents/reportAgent.js` 존재

### 3-2. orchestrator.js 확인

- [ ] `ROUTING_RULES` 배열 정의 (research/portfolio/alert/report)
- [ ] `routeToAgent(userMessage)` 함수 export 확인
- [ ] `ORCHESTRATOR_PROMPT` 상수 export 확인
- [ ] `AGENT_LABELS` 객체 (label, icon, color) export 확인
- [ ] 기본 라우팅 fallback → `'portfolio'`

### 3-3. 라우팅 로직 테스트 (브라우저 콘솔 또는 Node.js REPL)

```javascript
// 브라우저 콘솔에서 테스트
import { routeToAgent } from './src/agents/orchestrator.js'
```

- [ ] `routeToAgent('삼성전자 분석해줘')` → `'research'` 반환
- [ ] `routeToAgent('내 포트폴리오 현황')` → `'portfolio'` 반환
- [ ] `routeToAgent('오늘 관심종목 체크')` → `'alert'` 반환
- [ ] `routeToAgent('이번달 성과 리포트')` → `'report'` 반환
- [ ] `routeToAgent('안녕하세요')` → `'portfolio'` (기본값) 반환

### 3-4. 에이전트 프롬프트 내용 확인

- [ ] ResearchAgent: 분석 프레임워크 4개 항목 (기본정보, 재무, 기술적, 리스크) 포함
- [ ] PortfolioAgent: 매도/매수 직접 추천 금지 문구 포함
- [ ] AlertAgent: 🔴🟡🟢 우선순위 기준 포함
- [ ] ReportAgent: 금액·수익률 출력 형식 규칙 포함
- [ ] 모든 에이전트: 면책 문구 언급 또는 응답 시 포함 지시

### 3-5. 컨텍스트 빌더 함수 확인

- [ ] `buildResearchContext(stockData)` — stockData 없을 때 빈 문자열 반환
- [ ] `buildPortfolioContext(holdings, exchangeRate)` — holdings 빈 배열 시 '[보유 종목 없음]' 반환
- [ ] `buildAlertContext(watchlist, quotesMap)` — watchlist 빈 배열 시 '[관심종목 없음]' 반환
- [ ] `buildReportContext(holdings, period, exchangeRate)` — 기간 명시 포함

---

## Step 4: Claude API 호출 모듈 검증

### 4-1. 파일 구조 확인

- [ ] `src/api/claudeApi.js` 파일 존재
- [ ] axios 인스턴스 생성 (`claudeApi`)
- [ ] `baseURL: import.meta.env.VITE_API_BASE_URL || '/api'` 설정
- [ ] `timeout: 60_000` (60초) 설정

### 4-2. sendToAgent() 함수 확인

- [ ] `sendToAgent(userMessage, context, forceAgent)` export 확인
- [ ] 에이전트 프롬프트 맵 (`AGENT_PROMPTS`) 4개 에이전트 모두 포함
- [ ] 컨텍스트 빌더 맵 (`CONTEXT_BUILDERS`) 4개 에이전트 모두 포함
- [ ] `contextText`와 `userMessage` 결합 로직 확인
- [ ] 반환값: `{ text, agentType, agentInfo }` 형식

### 4-3. 에러 처리 확인

- [ ] HTTP 429 → "AI 요청 한도..." 메시지 반환
- [ ] HTTP 401 → "API 키 오류..." 메시지 반환
- [ ] HTTP 500 → "서버 오류..." + retry 로직
- [ ] 타임아웃 (ECONNABORTED) → "응답 시간 초과..." 메시지
- [ ] 최대 2회 retry (500 에러 시)

### 4-4. sendWithHistory() 함수 확인

- [ ] `sendWithHistory(messages, systemPrompt)` export 확인
- [ ] messages 배열 (role/content 형식) 전달 확인

---

## Step 5: chatStore 검증

### 5-1. 파일 구조 확인

- [ ] `src/store/chatStore.js` 파일 존재
- [ ] `useChatStore` export 확인
- [ ] Zustand `persist` 미들웨어 적용

### 5-2. 상태 및 액션 확인

- [ ] `messages: []` 초기 상태
- [ ] `isLoading: false` 초기 상태
- [ ] `error: null` 초기 상태
- [ ] `lastAgentType: null` 초기 상태
- [ ] `addUserMessage(text)` — role: 'user', id, timestamp 포함 메시지 추가
- [ ] `addAIMessage(text, agentType, agentInfo)` — role: 'assistant' 메시지 추가
- [ ] `setLoading(bool)` 상태 업데이트
- [ ] `setError(msg)` — error 설정 + isLoading: false
- [ ] `clearHistory()` — messages, error, lastAgentType 초기화

### 5-3. persist 설정 확인

- [ ] `name: 'chat-history'` 스토리지 키
- [ ] `partialize` — 최근 50개 메시지만 저장
- [ ] 브라우저 새로고침 후 메시지 히스토리 유지 확인

---

## Step 6: 채팅 UI 컴포넌트 검증

### 6-1. 파일 구조 확인

- [ ] `src/components/chat/ChatPanel.jsx` 존재
- [ ] `src/components/chat/MessageBubble.jsx` 존재
- [ ] `src/components/chat/AgentBadge.jsx` 존재
- [ ] `src/components/chat/QuickPromptButtons.jsx` 존재

### 6-2. AgentBadge 컴포넌트 확인

- [ ] `agentType` props 수신
- [ ] `AGENT_LABELS`에서 icon, color 참조
- [ ] 4개 에이전트 각각 다른 색상 배지 표시
- [ ] 알 수 없는 agentType → null 반환 (에러 없음)

### 6-3. MessageBubble 컴포넌트 확인

- [ ] `role: 'user'` 메시지 → 오른쪽 정렬 + 파란색 배경
- [ ] `role: 'assistant'` 메시지 → 왼쪽 정렬 + 회색 배경
- [ ] `loading: true` → 3점 점멸 애니메이션 표시
- [ ] AI 메시지에 `AgentBadge` 표시
- [ ] ReactMarkdown으로 마크다운 렌더링
- [ ] `remark-gfm`으로 테이블, 취소선 등 GFM 문법 지원
- [ ] 타임스탬프 표시 (시:분 형식)

### 6-4. 마크다운 렌더링 테스트

```
검증 방법:
AI 응답에 아래 마크다운 포함 시 올바르게 렌더링 확인
| 종목 | 수익률 |
|------|--------|
| 삼성전자 | +12.3% |
```

- [ ] 테이블 렌더링 정상 (테두리, 헤더 스타일 포함)
- [ ] 리스트 (`- 항목`) 렌더링 정상
- [ ] 코드 인라인 (`` `code` ``) 렌더링 정상
- [ ] 굵은 글씨 (`**텍스트**`) 렌더링 정상
- [ ] 헤더 (`## 제목`) 렌더링 정상

### 6-5. QuickPromptButtons 확인

- [ ] 빠른 질문 버튼 4개 표시
- [ ] 버튼 클릭 시 `onSelect` 콜백 호출
- [ ] 호출 시 자동으로 AI 전송 처리

### 6-6. ChatPanel 동작 확인

- [ ] shadcn/ui `Sheet` 컴포넌트로 우측 슬라이드 패널 구현
- [ ] 너비: `sm:max-w-[480px]`
- [ ] 메시지 목록 스크롤 + 최신 메시지 자동 스크롤
- [ ] 빈 대화 시 안내 메시지 표시
- [ ] `Enter` 키로 메시지 전송
- [ ] 전송 버튼 (`Send` 아이콘)
- [ ] 대화 초기화 버튼 (`Trash2` 아이콘)
- [ ] isLoading 중 입력창 disabled 처리
- [ ] 에러 메시지 빨간 박스로 표시
- [ ] 하단 면책 문구 표시

---

## Step 7: AI 채팅 페이지 검증

### 7-1. 파일 구조 확인

- [ ] `src/pages/AIChat.jsx` 존재
- [ ] `App.jsx`에 `/ai-chat` 라우트 추가
- [ ] `Sidebar.jsx`에 AI 분석 메뉴 항목 추가

### 7-2. AIChat 페이지 동작 확인

- [ ] `http://localhost:5173/ai-chat` 접속 가능
- [ ] 사이드바에서 "AI 분석" 메뉴 클릭 → 페이지 이동
- [ ] 메시지 입력 → 전송 → AI 응답 수신 확인
- [ ] 빠른 질문 버튼 동작 확인
- [ ] 대화 초기화 버튼 동작 확인
- [ ] 하단 면책 문구 표시 확인
- [ ] 전체 화면 높이에서 스크롤 가능한 메시지 영역 확인

---

## Step 8: 각 페이지 AI 연동 검증

### 8-1. Header AI 버튼 확인

- [ ] `Header.jsx`에 "🤖 AI 분석" 버튼 존재
- [ ] 버튼 클릭 시 `ChatPanel` 슬라이드 패널 열림
- [ ] 패널 닫기 (X) 동작 확인
- [ ] 모든 페이지에서 헤더 버튼 동작 확인

### 8-2. Portfolio 페이지 AI 연동

- [ ] "🤖 포트폴리오 분석" 버튼 존재
- [ ] 버튼 클릭 시 ChatPanel 열리고 자동으로 질문 전송
- [ ] AI 응답에 실제 보유 종목 데이터 반영 확인
  - (예: "삼성전자 100주" 등 실제 보유 내역 언급)

### 8-3. StockDetail 페이지 AI 연동

- [ ] "🔍 AI 분석" 버튼 존재
- [ ] 클릭 시 `forceAgent: 'research'`로 ResearchAgent 강제 지정
- [ ] AI 응답에 해당 종목명/티커 포함 확인
- [ ] AI 응답에 4개 분석 섹션 포함 확인 (기본정보, 재무, 기술적, 리스크)

### 8-4. Watchlist 페이지 AI 연동

- [ ] "🔔 오늘 브리핑" 버튼 존재
- [ ] 클릭 시 `forceAgent: 'alert'`로 AlertAgent 강제 지정
- [ ] AI 응답에 관심종목 데이터 반영 확인
- [ ] 우선순위 이모지 (🔴🟡🟢) 포함 확인

---

## Step 9: 에러 처리 검증

### 9-1. API 키 미설정 시 에러 처리

```
검증 방법:
1. .env에서 ANTHROPIC_API_KEY 주석 처리 후 서버 재시작
2. AI 분석 요청 전송
3. 에러 메시지 표시 확인
```

- [ ] 서버 → 클라이언트로 에러 전달 확인
- [ ] "API 키 미설정" 메시지 표시 (또는 서버 500 에러)
- [ ] 앱 크래시 없이 에러 메시지만 표시
- [ ] `.env` 복원 후 정상 동작 확인

### 9-2. 네트워크 오프라인 시

- [ ] 네트워크 비활성화 후 AI 요청 시 에러 메시지 표시
- [ ] "네트워크 연결을 확인해주세요" 메시지 표시
- [ ] 네트워크 복원 후 정상 요청 가능

### 9-3. Rate Limit (429) 에러 처리

```
검증 방법:
서버 응답을 수동으로 429 시뮬레이션 (Mock)
또는 실제 한도 초과 후 확인
```

- [ ] "AI 요청 한도에 도달했습니다. 잠시 후 다시 시도해주세요." 메시지 표시
- [ ] retry 로직 미동작 (429는 재시도 불필요)

### 9-4. 타임아웃 처리

- [ ] 60초 타임아웃 설정 확인 (`claudeApi.timeout`)
- [ ] 타임아웃 시 "응답 시간이 초과되었습니다" 메시지 표시

---

## Step 10: 통합 테스트

### 10-1. 전체 플로우 테스트

```
검증 시나리오 1: Research 플로우
1. 종목 탐색 페이지에서 "AAPL" 검색
2. 검색 결과 카드 클릭 → StockDetail 이동
3. "AI 분석" 버튼 클릭
4. ResearchAgent 응답 수신 확인
5. 응답에 기본정보/재무/기술/리스크 4섹션 포함 확인
6. 면책 문구 포함 확인
```

- [ ] 시나리오 1 전체 플로우 정상 동작

```
검증 시나리오 2: Portfolio 분석 플로우
1. 포트폴리오 페이지 접속
2. "포트폴리오 분석" 버튼 클릭
3. PortfolioAgent 응답 수신
4. 실제 보유 종목 언급 확인
5. 수익률 분석 포함 확인
```

- [ ] 시나리오 2 전체 플로우 정상 동작

```
검증 시나리오 3: AI 채팅 페이지 자유 대화
1. /ai-chat 페이지 접속
2. "삼성전자 어때?" 입력 → ResearchAgent 라우팅 확인
3. "내 포트폴리오 현황은?" 입력 → PortfolioAgent 라우팅 확인
4. "오늘 시장 브리핑해줘" 입력 → AlertAgent 라우팅 확인
5. "이번달 성과는?" 입력 → ReportAgent 라우팅 확인
```

- [ ] 시나리오 3 전체 플로우 정상 동작

### 10-2. 빌드 검증

```bash
npm run build
```

- [ ] `npm run build` 빌드 성공 (에러 없음)
- [ ] `dist/` 폴더 생성 확인
- [ ] 청크 크기 경고만 있고 에러 없음

### 10-3. AI 응답 품질 확인

| 에이전트       | 확인 항목                      | 결과 |
| -------------- | ------------------------------ | ---- |
| ResearchAgent  | 4개 분석 섹션 포함             | [ ]  |
| ResearchAgent  | 유사 종목 추천 2~3개           | [ ]  |
| ResearchAgent  | 투자 매력도 [상/중/하] 표시    | [ ]  |
| PortfolioAgent | 실제 보유 종목 데이터 반영     | [ ]  |
| PortfolioAgent | 집중도 위험 경고 (30% 초과 시) | [ ]  |
| AlertAgent     | 🔴🟡🟢 우선순위 표시           | [ ]  |
| AlertAgent     | 날짜 + 브리핑 형식             | [ ]  |
| ReportAgent    | 마크다운 테이블 형식           | [ ]  |
| ReportAgent    | 금액 천단위 구분자             | [ ]  |
| 전체 에이전트  | 면책 문구 포함                 | [ ]  |

### 10-4. 다크모드 UI 확인

- [ ] ChatPanel 다크모드 스타일 정상
- [ ] MessageBubble 다크모드 배경/텍스트 정상
- [ ] AgentBadge 다크모드 색상 정상
- [ ] 마크다운 테이블 다크모드 테두리/배경 정상

### 10-5. 반응형 확인

- [ ] 모바일 뷰 (375px) ChatPanel 전체 너비 표시
- [ ] 태블릿 뷰 (768px) ChatPanel 480px 너비 표시
- [ ] AI 채팅 페이지 모바일 스크롤 정상

---

## 검증 결과 요약

> 검증 완료 후 아래 통계를 업데이트하세요.

| 구분                       | 항목 수       | 통과        | 실패        | 통과율       |
| -------------------------- | ------------- | ----------- | ----------- | ------------ |
| 사전 준비                  | 11            | 0           | 0           | 0%           |
| Step 1 (로컬 프록시)       | 13            | 0           | 0           | 0%           |
| Step 2 (Vercel Serverless) | 10            | 0           | 0           | 0%           |
| Step 3 (에이전트 프롬프트) | 20            | 0           | 0           | 0%           |
| Step 4 (Claude API 모듈)   | 14            | 0           | 0           | 0%           |
| Step 5 (chatStore)         | 13            | 0           | 0           | 0%           |
| Step 6 (채팅 UI)           | 22            | 0           | 0           | 0%           |
| Step 7 (AI 채팅 페이지)    | 8             | 0           | 0           | 0%           |
| Step 8 (페이지 AI 연동)    | 14            | 0           | 0           | 0%           |
| Step 9 (에러 처리)         | 11            | 0           | 0           | 0%           |
| Step 10 (통합 테스트)      | 18            | 0           | 0           | 0%           |
| **합계**             | **154** | **0** | **0** | **0%** |

### 미완료 항목 목록

> 검증 후 [ ] 상태인 항목을 이곳에 기록하세요.

| # | Step | 항목 | 메모 |
| - | ---- | ---- | ---- |
| 1 | -    | -    | -    |

---

*작성: Claude Code | Phase 4 — Claude AI 에이전트 탑재*
*참조: [CLAUDE.md](../CLAUDE.md) | [00.02.04 detail-plan.md](./00.02.04%20detail-plan.md)*
