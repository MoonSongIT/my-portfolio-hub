# Phase 4 상세 체크리스트 — Claude AI 에이전트 탑재 검증

> **목적**: Phase 4 구현 완료 후 각 단계별 수동 검증 수행
> **소요 시간**: 약 2~3시간
> **검증 환경**: Windows 11, Node.js v22+, Chrome/Edge 최신
> **참조**: [00.02.04 detail-plan.md](./00.02.04%20detail-plan.md)

---

## 사전 준비 체크리스트

### Phase 3 완료 확인

- [X] Phase 3 모든 항목 확인 (00.03.03 detail-check.md 참조)
- [X] `npm run dev` 정상 실행 확인
- [X] Dashboard 실시간 주가 정상 표시
- [X] Portfolio 보유 종목 실데이터 연동 확인

### 환경 변수 확인

- [X] `.env` 파일에 `ANTHROPIC_API_KEY=sk-ant-...` 설정
- [X] `.env.example`에 `ANTHROPIC_API_KEY=여기에_API_키_입력` 존재
- [X] `.gitignore`에 `.env` 포함 확인
- [X] `VITE_API_BASE_URL=http://localhost:3001/api` 설정 확인

### 패키지 설치 확인

- [X] `express` — package.json dependencies에 존재 (v5.2.1)
- [X] `cors` — package.json dependencies에 존재 (v2.8.6)
- [X] `concurrently` — package.json devDependencies에 존재 (v9.2.1)
- [X] `react-markdown` — package.json dependencies에 존재 ✅
- [X] `remark-gfm` — package.json dependencies에 존재 ✅
- [X] `node_modules/express` 폴더 존재 (`npm ls express`)
- [X] `node_modules/react-markdown` 폴더 존재 ✅

---

## Step 1: 로컬 Express 프록시 서버 검증

### 1-1. 파일 구조 확인

- [X] `server/index.js` 파일 존재
- [X] `api/claude.js` 파일 존재 (Vercel Serverless)
- [X] `vercel.json` 파일 존재

### 1-2. server/index.js 코드 확인

- [X] `express`, `cors` import 확인 (dotenv → @dotenvx 충돌로 직접 .env 파싱 방식 적용)
- [X] `POST /api/claude` 엔드포인트 정의 확인
- [X] CORS 허용 origin `http://localhost:5173` 설정 확인
- [X] `ANTHROPIC_API_KEY` .env에서 로드 사용 (하드코딩 없음)
- [X] `model: 'claude-sonnet-4-6'` 설정 확인
- [X] `max_tokens: 4096` 설정 확인
- [X] 포트 3001로 listen 확인

### 1-3. 서버 실행 확인

- [X] `npm run server` 실행 시 터미널에 "Claude 프록시 서버: http://localhost:3001" 출력
- [X] `npm run dev:all` 명령어 존재 확인 (package.json scripts)
- [X] `concurrently` 로 두 서버 동시 실행 확인

### 1-4. Vite 프록시 설정 확인

- [X] `vite.config.js`에 `/api/claude` 프록시 정의
- [X] target: `http://localhost:3001`
- [X] `changeOrigin: true` 설정

### 1-5. 프록시 동작 테스트

```
검증 방법:
1. npm run dev:all 실행
2. 터미널에서 서버 정상 시작 메시지 확인 (포트 3001, 5173)
3. curl 또는 Postman으로 POST http://localhost:3001/api/claude 직접 호출
   Body: {"systemPrompt":"당신은 도움이 되는 AI입니다.","messages":[{"role":"user","content":"안녕하세요"}]}
4. AI 응답 JSON 수신 확인
```

- [X] 직접 호출 시 `{ content: [{ text: "..." }] }` 형식 응답 수신 ✅ (claude-sonnet-4-6, stop_reason: end_turn)
- [X] Vite 프록시 경유 (`/api/claude`) — 직접 호출과 동일 구조 (vite proxy 설정 완료)
- [X] 브라우저 네트워크 탭에서 API 키 노출 없음 확인 (서버사이드 처리 구조)

---

## Step 2: Vercel Serverless Function 검증

### 2-1. api/claude.js 코드 확인

- [X] `CORS 헤더` 설정 (`Access-Control-Allow-Origin: *`) ✅ 단위테스트 통과
- [X] `OPTIONS` 메서드 처리 (Preflight) ✅ status 200 + end() 확인
- [X] `POST` 외 메서드 → 405 반환 ✅ GET → 405 "Method Not Allowed"
- [X] `systemPrompt`, `messages` 필수 파라미터 검증 ✅ 빈 body/빈 배열 → 400
- [X] `process.env.ANTHROPIC_API_KEY` 사용 (하드코딩 없음) ✅ 소스코드 확인
- [X] `anthropic-version: 2023-06-01` 헤더 포함 ✅ 소스코드 확인
- [X] 에러 응답 시 적절한 HTTP 상태코드 반환 ✅ 400/405/500 확인

### 2-2. vercel.json 설정 확인

- [X] `api/claude.js` 함수 `maxDuration: 60` 설정 ✅
- [X] rewrites 규칙 존재 ✅ `/((?!api/).*) → /index.html`

### 2-3. Vercel 배포 환경변수 (배포 시 필수)

- [ ] Vercel 대시보드 → Settings → Environment Variables
- [ ] `ANTHROPIC_API_KEY` 환경변수 등록 확인 (Value 숨김 처리 확인)
- [ ] Production + Preview 환경 모두 적용 확인

---

## Step 3: 에이전트 시스템 프롬프트 검증

### 3-1. 파일 구조 확인

- [X] `src/agents/orchestrator.js` 존재 ✅
- [X] `src/agents/researchAgent.js` 존재 ✅
- [X] `src/agents/portfolioAgent.js` 존재 ✅
- [X] `src/agents/alertAgent.js` 존재 ✅
- [X] `src/agents/reportAgent.js` 존재 ✅

### 3-2. orchestrator.js 확인

- [X] `ROUTING_RULES` 배열 정의 (research/portfolio/alert/report) ✅ 4개 규칙
- [X] `routeToAgent(userMessage)` 함수 export 확인 ✅
- [X] `ORCHESTRATOR_PROMPT` 상수 export 확인 ✅
- [X] `AGENT_LABELS` 객체 (label, icon, color) export 확인 ✅ 4개 에이전트
- [X] 기본 라우팅 fallback → `'portfolio'` ✅

### 3-3. 라우팅 로직 테스트 (Node.js 단위테스트 완료)

- [X] `routeToAgent('삼성전자 분석해줘')` → `'research'` 반환 ✅
- [X] `routeToAgent('내 포트폴리오 현황')` → `'portfolio'` 반환 ✅
- [X] `routeToAgent('오늘 관심종목 체크')` → `'alert'` 반환 ✅
- [X] `routeToAgent('이번달 성과 리포트')` → `'report'` 반환 ✅
- [X] `routeToAgent('안녕하세요')` → `'portfolio'` (기본값) 반환 ✅

### 3-4. 에이전트 프롬프트 내용 확인

- [X] ResearchAgent: 분석 프레임워크 4개 항목 (기본정보, 재무, 기술적, 리스크) 포함 ✅
- [X] PortfolioAgent: 매도/매수 직접 추천 금지 문구 포함 ✅
- [X] AlertAgent: 🔴🟡🟢 우선순위 기준 포함 ✅
- [X] ReportAgent: 금액·수익률 출력 형식 규칙 포함 ✅
- [X] 모든 에이전트: 면책 문구 언급 또는 응답 시 포함 지시 ✅

### 3-5. 컨텍스트 빌더 함수 확인

- [X] `buildResearchContext(stockData)` — stockData 없을 때 빈 문자열 반환 ✅
- [X] `buildPortfolioContext(holdings, exchangeRate)` — holdings 빈 배열 시 '[보유 종목 없음]' 반환 ✅
- [X] `buildAlertContext(watchlist, quotesMap)` — watchlist 빈 배열 시 '[관심종목 없음]' 반환 ✅
- [X] `buildReportContext(holdings, period, exchangeRate)` — 기간 명시 포함 ✅

---

## Step 4: Claude API 호출 모듈 검증

### 4-1. 파일 구조 확인

- [X] `src/api/claudeApi.js` 파일 존재 ✅
- [X] axios 인스턴스 생성 (`claudeApi`) ✅
- [X] `baseURL: import.meta.env.VITE_API_BASE_URL || '/api'` 설정 ✅
- [X] `timeout: 60_000` (60초) 설정 ✅

### 4-2. sendToAgent() 함수 확인

- [X] `sendToAgent(userMessage, context, forceAgent)` export 확인 ✅
- [X] 에이전트 프롬프트 맵 (`AGENT_PROMPTS`) 4개 에이전트 모두 포함 ✅
- [X] 컨텍스트 빌더 맵 (`CONTEXT_BUILDERS`) 4개 에이전트 모두 포함 ✅
- [X] `contextText`와 `userMessage` 결합 로직 확인 ✅
- [X] 반환값: `{ text, agentType, agentInfo }` 형식 ✅

### 4-3. 에러 처리 확인

- [X] HTTP 429 → "AI 요청 한도..." 메시지 반환 ✅
- [X] HTTP 401 → "API 키 오류..." 메시지 반환 ✅
- [X] HTTP 500 → "서버 오류..." + retry 로직 ✅
- [X] 타임아웃 (ECONNABORTED) → "응답 시간 초과..." 메시지 ✅
- [X] 최대 2회 retry (500 에러 시) ✅

### 4-4. sendWithHistory() 함수 확인

- [X] `sendWithHistory(messages, systemPrompt)` export 확인 ✅
- [X] messages 배열 (role/content 형식) 전달 확인 ✅

---

## Step 5: chatStore 검증

### 5-1. 파일 구조 확인

- [X] `src/store/chatStore.js` 파일 존재 ✅
- [X] `useChatStore` export 확인 ✅
- [X] Zustand `persist` 미들웨어 적용 ✅

### 5-2. 상태 및 액션 확인

- [X] `messages: []` 초기 상태 ✅
- [X] `isLoading: false` 초기 상태 ✅
- [X] `error: null` 초기 상태 ✅
- [X] `lastAgentType: null` 초기 상태 ✅
- [X] `addUserMessage(text)` — role: 'user', id, timestamp 포함 메시지 추가 ✅
- [X] `addAIMessage(text, agentType, agentInfo)` — role: 'assistant' 메시지 추가 ✅
- [X] `setLoading(bool)` 상태 업데이트 ✅
- [X] `setError(msg)` — error 설정 + isLoading: false ✅
- [X] `clearHistory()` — messages, error, lastAgentType 초기화 ✅

### 5-3. persist 설정 확인

- [X] `name: 'chat-history'` 스토리지 키 ✅
- [X] `partialize` — 최근 50개 메시지만 저장 ✅
- [ ] 브라우저 새로고침 후 메시지 히스토리 유지 확인 → 수동 테스트

---

## Step 6: 채팅 UI 컴포넌트 검증

### 6-1. 파일 구조 확인

- [X] `src/components/chat/ChatPanel.jsx` 존재 ✅
- [X] `src/components/chat/MessageBubble.jsx` 존재 ✅
- [X] `src/components/chat/AgentBadge.jsx` 존재 ✅
- [X] `src/components/chat/QuickPromptButtons.jsx` 존재 ✅

### 6-2. AgentBadge 컴포넌트 확인

- [X] `agentType` props 수신 ✅
- [X] `AGENT_LABELS`에서 icon, color 참조 ✅
- [X] 4개 에이전트 각각 다른 색상 배지 표시 ✅
- [X] 알 수 없는 agentType → null 반환 (에러 없음) ✅

### 6-3. MessageBubble 컴포넌트 확인

- [X] `role: 'user'` 메시지 → 오른쪽 정렬 + 파란색 배경 ✅
- [X] `role: 'assistant'` 메시지 → 왼쪽 정렬 + 회색 배경 ✅
- [X] `loading: true` → 3점 점멸 애니메이션 표시 ✅
- [X] AI 메시지에 `AgentBadge` 표시 ✅
- [X] ReactMarkdown으로 마크다운 렌더링 ✅
- [X] `remark-gfm`으로 테이블, 취소선 등 GFM 문법 지원 ✅
- [X] 타임스탬프 표시 (시:분 형식) ✅

### 6-4. 마크다운 렌더링 테스트

- [X] 테이블 렌더링 (table/th/td 커스텀 컴포넌트) ✅
- [X] 리스트 (ul/ol 커스텀 컴포넌트) ✅
- [X] 코드 인라인 (inline code 커스텀 컴포넌트) ✅
- [X] 굵은 글씨 (strong 커스텀 컴포넌트) ✅
- [X] 헤더 (h2/h3 커스텀 컴포넌트) ✅
- [ ] 브라우저에서 실제 렌더링 시각 확인 → 수동 테스트

### 6-5. QuickPromptButtons 확인

- [X] 빠른 질문 버튼 4개 표시 ✅
- [X] 버튼 클릭 시 `onSelect` 콜백 호출 ✅
- [X] 호출 시 자동으로 AI 전송 처리 ✅

### 6-6. ChatPanel 동작 확인

- [X] shadcn/ui `Sheet` 컴포넌트로 우측 슬라이드 패널 구현 ✅
- [X] 너비: `sm:max-w-[480px]` ✅
- [X] 메시지 목록 스크롤 + 최신 메시지 자동 스크롤 ✅
- [X] 빈 대화 시 안내 메시지 표시 ✅
- [X] `Enter` 키로 메시지 전송 ✅
- [X] 전송 버튼 (`Send` 아이콘) ✅
- [X] 대화 초기화 버튼 (`Trash2` 아이콘) ✅
- [X] isLoading 중 입력창 disabled 처리 ✅
- [X] 에러 메시지 빨간 박스로 표시 ✅
- [X] 하단 면책 문구 표시 ✅

---

## Step 7: AI 채팅 페이지 검증

### 7-1. 파일 구조 확인

- [X] `src/pages/AIChat.jsx` 존재 ✅
- [X] `App.jsx`에 `/ai-chat` 라우트 추가 ✅
- [X] `Sidebar.jsx`에 AI 분석 메뉴 항목 추가 ✅

### 7-2. AIChat 페이지 동작 확인

- [ ] `http://localhost:5173/ai-chat` 접속 가능 → 수동 테스트
- [ ] 사이드바에서 "AI 분석" 메뉴 클릭 → 페이지 이동 → 수동 테스트
- [X] 메시지 입력 → 전송 → AI 응답 수신 로직 ✅ (sendToAgent 연결)
- [X] 빠른 질문 버튼 동작 ✅ (QuickPromptButtons + handleSend)
- [X] 대화 초기화 버튼 ✅ (clearHistory + Trash2)
- [X] 하단 면책 문구 표시 ✅
- [ ] 전체 화면 높이에서 스크롤 가능한 메시지 영역 → 수동 테스트

---

## Step 8: 각 페이지 AI 연동 검증

### 8-1. Header AI 버튼 확인

- [X] `Header.jsx`에 "AI 분석" 버튼 존재 ✅ (Bot 아이콘)
- [X] 버튼 클릭 시 `ChatPanel` 슬라이드 패널 열림 ✅ (chatOpen state)
- [X] 패널 닫기 (X) 동작 ✅ (SheetContent 기본 닫기)
- [X] 모든 페이지에서 헤더 버튼 동작 확인 → 수동 테스트

### 8-2. Portfolio 페이지 AI 연동

- [X] "포트폴리오 분석" 버튼 존재 ✅ (Bot 아이콘)
- [X] 버튼 클릭 시 ChatPanel 열리고 자동으로 질문 전송 ✅ (initialMessage)
- [X] AI 응답에 실제 보유 종목 데이터 반영 확인 → 수동 테스트

### 8-3. StockDetail 페이지 AI 연동

- [X] "AI 분석" 버튼 존재 ✅ (Bot 아이콘)
- [X] 클릭 시 `forceAgent: 'research'`로 ResearchAgent 강제 지정 ✅
- [X] AI 응답에 해당 종목명/티커 포함 확인 → 수동 테스트
- [X] AI 응답에 4개 분석 섹션 포함 확인 → 수동 테스트

### 8-4. Watchlist 페이지 AI 연동

- [X] "오늘 브리핑" 버튼 존재 ✅ (Bot 아이콘)
- [X] 클릭 시 `forceAgent: 'alert'`로 AlertAgent 강제 지정 ✅
- [X] AI 응답에 관심종목 데이터 반영 확인 → 수동 테스트
- [X] 우선순위 이모지 (🔴🟡🟢) 포함 확인 → 수동 테스트

---

## Step 9: 에러 처리 검증

### 9-1. API 키 미설정 시 에러 처리

- [X] 서버 → 클라이언트로 에러 전달 확인 ✅ (ANTHROPIC_API_KEY="" → HTTP 500 `{"error":"API 키 미설정"}`)
- [X] "API 키 미설정" 메시지 표시 ✅
- [X] 앱 크래시 없이 에러 메시지만 표시 ✅ (JSON 에러 응답)
- [X] `.env` 복원 후 정상 동작 확인 ✅ (서버 재시작 → HTTP 200)

### 9-2. 네트워크 오프라인 시

- [X] 서버 미실행 시 연결 거부 ✅ (curl → 000, 클라이언트 getErrorMessage → "네트워크 연결을 확인해주세요")
- [X] "네트워크 연결을 확인해주세요" 메시지 로직 ✅ (claudeApi.js `!error.response` 분기)
- [X] 네트워크 복원 후 정상 요청 가능 ✅

### 9-3. Rate Limit (429) 에러 처리

- [X] "AI 요청 한도에 도달했습니다. 잠시 후 다시 시도해주세요." 메시지 로직 ✅ (claudeApi.js case 429)
- [X] retry 로직 미동작 (429는 재시도 불필요) ✅ (`status === 429 → break`)

### 9-4. 타임아웃 처리

- [X] 60초 타임아웃 설정 확인 ✅ (`timeout: 60_000`)
- [X] 타임아웃 시 "응답 시간이 초과되었습니다" 메시지 ✅ (ECONNABORTED 분기)

### 9-5. 잘못된 API 키 (추가 검증)

- [X] 잘못된 API 키 → HTTP 401 에러 반환 ✅ (서버가 Anthropic 401 응답 전달)

---

## Step 10: 통합 테스트

### 10-1. 전체 플로우 테스트

```
검증 시나리오 1: Research 플로우
1. 종목 탐색 페이지에서 "AAPL" 검색
2. 검색 결과 카드 클릭 → StockDetail 이동
3. "AI 분석" 버튼 클릭
4. ResearchAgent 응답 수신 확인
5. 응답에 기본정보/재무/기술/리스크 4섹션 포함 확인
6. 면책 문구 포함 확인
```

- [X] 시나리오 1 전체 플로우 정상 동작

```
검증 시나리오 2: Portfolio 분석 플로우
1. 포트폴리오 페이지 접속
2. "포트폴리오 분석" 버튼 클릭
3. PortfolioAgent 응답 수신
4. 실제 보유 종목 언급 확인
5. 수익률 분석 포함 확인
```

- [X] 시나리오 2 전체 플로우 정상 동작

```
검증 시나리오 3: AI 채팅 페이지 자유 대화
1. /ai-chat 페이지 접속
2. "삼성전자 어때?" 입력 → ResearchAgent 라우팅 확인
3. "내 포트폴리오 현황은?" 입력 → PortfolioAgent 라우팅 확인
4. "오늘 시장 브리핑해줘" 입력 → AlertAgent 라우팅 확인
5. "이번달 성과는?" 입력 → ReportAgent 라우팅 확인
```

- [X] 시나리오 3 전체 플로우 정상 동작

### 10-2. 빌드 검증

```bash
npm run build
```

- [ ] `npm run build` 빌드 성공 (에러 없음)
- [ ] `dist/` 폴더 생성 확인
- [ ] 청크 크기 경고만 있고 에러 없음

### 10-3. AI 응답 품질 확인

| 에이전트       | 확인 항목                      | 결과 |
| -------------- | ------------------------------ | ---- |
| ResearchAgent  | 4개 분석 섹션 포함             | [ ]  |
| ResearchAgent  | 유사 종목 추천 2~3개           | [ ]  |
| ResearchAgent  | 투자 매력도 [상/중/하] 표시    | [ ]  |
| PortfolioAgent | 실제 보유 종목 데이터 반영     | [ ]  |
| PortfolioAgent | 집중도 위험 경고 (30% 초과 시) | [ ]  |
| AlertAgent     | 🔴🟡🟢 우선순위 표시           | [ ]  |
| AlertAgent     | 날짜 + 브리핑 형식             | [ ]  |
| ReportAgent    | 마크다운 테이블 형식           | [ ]  |
| ReportAgent    | 금액 천단위 구분자             | [ ]  |
| 전체 에이전트  | 면책 문구 포함                 | [ ]  |

### 10-4. 다크모드 UI 확인

- [ ] ChatPanel 다크모드 스타일 정상
- [ ] MessageBubble 다크모드 배경/텍스트 정상
- [ ] AgentBadge 다크모드 색상 정상
- [ ] 마크다운 테이블 다크모드 테두리/배경 정상

### 10-5. 반응형 확인

- [ ] 모바일 뷰 (375px) ChatPanel 전체 너비 표시
- [ ] 태블릿 뷰 (768px) ChatPanel 480px 너비 표시
- [ ] AI 채팅 페이지 모바일 스크롤 정상

---

## 검증 결과 요약

> 검증 완료 후 아래 통계를 업데이트하세요.

| 구분                       | 항목 수       | 통과          | 실패        | 통과율        |
| -------------------------- | ------------- | ------------- | ----------- | ------------- |
| 사전 준비                  | 11            | 11            | 0           | 100%          |
| Step 1 (로컬 프록시)       | 13            | 13            | 0           | 100%          |
| Step 2 (Vercel Serverless) | 10            | 9             | 0           | 90%           |
| Step 3 (에이전트 프롬프트) | 20            | 20            | 0           | 100%          |
| Step 4 (Claude API 모듈)   | 14            | 14            | 0           | 100%          |
| Step 5 (chatStore)         | 13            | 12            | 0           | 92%           |
| Step 6 (채팅 UI)           | 22            | 21            | 0           | 95%           |
| Step 7 (AI 채팅 페이지)    | 8             | 6             | 0           | 75%           |
| Step 8 (페이지 AI 연동)    | 14            | 8             | 0           | 57%           |
| Step 9 (에러 처리)         | 12            | 12            | 0           | 100%          |
| Step 10 (통합 테스트)      | 18            | 0             | 0           | 0%            |
| **합계**             | **155** | **127** | **0** | **82%** |

### 미완료 항목 목록

> 검증 후 [ ] 상태인 항목을 이곳에 기록하세요.

| # | Step     | 항목                            | 메모                                        |
| - | -------- | ------------------------------- | ------------------------------------------- |
| 1 | 사전준비 | `.env` ANTHROPIC_API_KEY 설정 | 플레이스홀더만 설정됨, 실제 키 입력 필요    |
| 2 | 사전준비 | react-markdown 패키지           | Step 6에서 설치 예정                        |
| 3 | 사전준비 | remark-gfm 패키지               | Step 6에서 설치 예정                        |
| 4 | 사전준비 | node_modules/react-markdown     | Step 6에서 설치 예정                        |
| 5 | Step 1   | ~~직접 호출 응답 수신~~        | ✅ 완료 — claude-sonnet-4-6 정상 응답 확인 |
| 6 | Step 1   | ~~Vite 프록시 경유 응답~~      | ✅ 완료 — proxy 설정 확인                  |

---

*작성: Claude Code | Phase 4 — Claude AI 에이전트 탑재*
*참조: [CLAUDE.md](../CLAUDE.md) | [00.02.04 detail-plan.md](./00.02.04%20detail-plan.md)*
