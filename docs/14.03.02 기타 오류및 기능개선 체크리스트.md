# 14.03.02 기타 오류 및 기능 개선 체크리스트

## 작성일
2026-06-06 (최종 업데이트: 2026-06-07 — 모든 항목 완료 및 master 머지)

---

## [FEAT-01] 대시보드 수익률 추이 차트 — 투자원금+손익 누적 막대 오버레이

> 1차 구현(일간 증감 %p 막대)은 의도와 달라 누적 스택 막대로 재구현.
> ⮕ **이후 [FEAT-03] 종합수익률 기준으로 최종 재설계됨. 아래 검증은 FEAT-03으로 대체.**

### 코드 수정

- [x] `ProfitLineChart.jsx` — AreaChart → ComposedChart 교체
- [x] `ProfitLineChart.jsx` — 우측 YAxis를 금액 축(formatCurrencyShort, domain=[0,max])으로 변경
- [x] `ProfitLineChart.jsx` — 파생 데이터(principalBase, pnlSegment, pnl) 계산 추가
- [x] `ProfitLineChart.jsx` — 단일 Bar → principalBase + pnlSegment 스택 막대(stackId)로 교체
- [x] `ProfitLineChart.jsx` — 상단 세그먼트 Cell 색상 분기
- [x] `ProfitLineChart.jsx` — Tooltip: 투자원금 / 손익합계 / 총 평가액 표시
- [x] `HoldingPnlTimeline.jsx` — 1차 일간 증감 Bar 원복 (적용 범위 제외)

### 수작업 검증

- [~] FEAT-03으로 차트 정의가 재설계되어 본 항목 검증은 FEAT-03 검증으로 대체됨

---

## [FIX-01] 자금 입출금 수정 모달 — 선택 정보 미표시·수정 불가

### 코드 수정

- [x] `CashFlowModal.jsx` — `editTarget` prop 추가 (옵셔널, 기본 null)
- [x] `CashFlowModal.jsx` — 수정 모드 시 editTarget 값으로 폼 초기화
- [x] `CashFlowModal.jsx` — 제출 분기: editTarget이면 updateCashFlow, 아니면 addCashFlow
- [x] `CashFlowModal.jsx` — 탭 전환 카테고리 초기화를 handleTypeChange로 이동 (첫 로드 보존)
- [x] `CashFlowModal.jsx` — 헤더 제목/버튼 라벨 수정 모드 분기

### 수작업 검증

- [x] 자금관리에서 입출금 항목의 수정 버튼 클릭 시 팝업에 기존 정보가 채워지는지 확인
- [x] 값을 바꿔 저장 시 새 항목이 추가되지 않고 기존 항목이 수정되는지 확인
- [x] 수정 모달 헤더가 "입출금 수정", 버튼이 "수정 저장"으로 표시되는지 확인
- [x] 추가(입금/출금) 모달은 기존처럼 빈 폼으로 정상 동작하는지 확인 (회귀 없음)

> ✅ 사용자 확인 완료 ("자금관리 수정은 확인됐어")

---

## [FEAT-02] 매도차익 입금 카테고리 신설 + 실현손익 로직 정합

### 코드 수정

- [x] `cashFlowStore.js` — `REALIZED_GAIN`(realized_gain, 입금, isCapital=false) 카테고리 신설
- [x] `Dashboard.jsx` — cashFlowRealizedPnl을 메모 → `category==='realized_gain'` 기준 전환
- [x] `db.js` v16 — interest+메모'매도차익' → realized_gain 자동 마이그레이션
- [x] `CashFlowModal.jsx` — CATEGORY_DESC에 매도차익 설명 추가
- [x] `CashFlowHistory.jsx` — 필터 칩/라벨에 '매도차익' 추가

### 수작업 검증

- [x] 자금관리 내역에서 기존 매도차익 입금이 "매도차익"으로 표시되는지 확인 (마이그레이션 적용)
- [x] 종합수익률 / 실현손익 / 투자원금 수치가 마이그레이션 전후 동일한지 확인 (변동 없음 확인)
- [x] 입출금 추가 모달의 입금 카테고리에 "매도차익"이 보이는지 확인
- [x] 매도차익 카테고리로 신규 입금 시 종합수익률 실현손익에 반영되는지 확인
- [x] 내역 필터에서 '매도차익' 칩으로 해당 항목만 필터되는지 확인

> 참고: 매도차익으로 잘못 분류된 원금 회수분 1,449,718을 두 계좌 비율(799,595 / 650,123)로 조정(+) 재분류 → 검증액 0 달성

---

## [FEAT-03] 수익률 추이 차트를 종합수익률 기준으로 재설계 (FEAT-01 개정)

### 코드 수정

- [x] `portfolioAggregator.js` — 시그니처에 cashFlows·entries 추가, 날짜별 netCapital/실현/배당 누계 + 미실현 계산
- [x] `portfolioAggregator.js` — investedValue=투자원금, totalValue=자산, returnRate=종합수익률로 재정의
- [x] `Dashboard.jsx` — 호출부에 계좌 필터된 cashFlows·entries 전달
- [x] `ProfitLineChart.jsx` — 툴팁 라벨(종합수익률 / 투자원금 / 손익합계 / 총 자산)
- [x] `portfolioAggregator.test.js` — 종합수익률 기준 테스트 재작성 (8개 통과)
- [x] `ProfitLineChart.jsx` — 채도 대비 강화(막대 파스텔 / 선 짙은청색 #1e3a8a) + 좌우 마진 축소

### 수작업 검증

- [x] 대시보드 수익률 추이 최근 막대 전체 높이 = 자산현황(7,985만)과 일치하는지
- [x] 최근 막대 손익부 = 종합수익률 손익합계(3,185만)와 일치하는지
- [x] 막대 hover 툴팁의 종합수익률·투자원금·손익합계·총 자산이 종합수익률 카드와 일치하는지
- [x] 좌측 Area 선 최근값 = 종합수익률(66.35%)과 일치하는지
- [x] 기간(7/30/90일) 변경 시 막대·선 정상 갱신되는지
- [x] 계좌 선택(전체/개별) 전환 시 차트가 해당 계좌 기준으로 바뀌는지
- [x] 채도 대비(파스텔 막대 / 짙은청색 선) 및 좌우 마진이 의도대로 보이는지

> ✅ 사용자 최종 확인 완료 ("좋아, 내가 의도하는 바야" + 청색선 확인)

---

<!-- 새 항목은 아래에 ## [FEAT-NN] 형식으로 추가 -->
