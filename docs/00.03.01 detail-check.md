# Phase 1 상세 체크리스트 — 프로젝트 초기화 검증

> **목적**: Phase 1 구현 완료 후 각 단계별 수동 검증 및 단위 테스트 수행
> **소요 시간**: 약 30-45분
> **검증 환경**: Windows 10/11, Node.js v18.0.0+, 로컬 개발 환경

---

## 📋 사전 준비 체크리스트

### 필수 환경 확인

- [X] Node.js 버전 확인

  ```bash
  node --version
  # 결과: v18.x.x 이상 => v22.18.0
  ```
- [X] npm 버전 확인

  ```bash
  npm --version
  # 결과: v9.x.x 이상 = > 11.5.2
  ```
- [X] Git 설치 확인

  ```bash
  git --version
  # 결과: git version 2.x.x => git version 2.46.0.windows.1
  ```
- [X] 프로젝트 경로 확인

  ```bash
  cd C:\Users\dohay\ClaudeWork\my-portfolio-hub
  pwd
  # 결과: /c/Users/dohay/ClaudeWork/my-portfolio-hub
  ```

---

## ✅ Step 1: Vite 6 + React 18 설치 검증

### 1-1. package.json 생성 확인

**검증 항목**

```
📁 my-portfolio-hub/
  ✓ package.json 파일 존재
```

**확인 방법**

```bash
# 파일 존재 여부 확인
ls -la | grep package.json

# 내용 확인 (필드별)
cat package.json | grep -E '"name"|"version"|"scripts"'
```

**기대 결과**

```
"name": "my-portfolio-hub"
"version": "0.1.0"
"type": "module"
```

### 1-2. npm 의존성 설치 확인

**검증 항목**

```
✓ node_modules/ 폴더 생성됨
✓ react, react-dom 설치됨
✓ vite, @vitejs/plugin-react-swc 설치됨
```

**확인 방법**

```bash
# 설치 완료 여부
npm list react react-dom vite

# 예상 출력:
# my-portfolio-hub@0.1.0
# ├── react@18.3.1
# ├── react-dom@18.3.1
# └── vite@5.x.x
```

**트러블슈팅**

```bash
# 의존성 재설치
rm -rf node_modules package-lock.json
npm install

# 특정 버전 강제 설치
npm install react@18.3.1 react-dom@18.3.1 --save
```

### 1-3. 스크립트 설정 확인

**검증 항목**

```
✓ npm run dev 명령 존재
✓ npm run build 명령 존재
✓ npm run preview 명령 존재
```

**확인 방법**

```bash
# package.json 스크립트 섹션 확인
cat package.json | grep -A 5 '"scripts"'

# 또는 직접 실행 테스트
npm run
# 결과에 dev, build, preview가 나열되어야 함
```

---

## ✅ Step 2: Vite 설정 검증

### 2-1. vite.config.js 파일 검증

**검증 항목**

```
✓ vite.config.js 파일 존재
✓ react 플러그인 설정 포함
✓ @vitejs/plugin-react-swc 사용 확인
```

**확인 방법**

```bash
# 파일 존재 확인
ls -la vite.config.js

# 내용 검증 (SWC 플러그인 포함 여부)
cat vite.config.js | grep -i "react-swc\|plugin"

# 예상 출력:
# import react from '@vitejs/plugin-react-swc'
# plugins: [react()],
```

### 2-2. 개발 서버 포트 설정 확인

**검증 항목**

```
✓ port: 5173 설정됨
✓ open: true 설정됨 (자동 브라우저 열기)
```

**확인 방법**

```bash
cat vite.config.js | grep -E "port|open"

# 예상 출력:
# port: 5173,
# open: true,
```

### 2-3. 번들 최적화 설정 확인

**검증 항목**

```
✓ manualChunks 설정 포함 (recharts, query 라이브러리)
✓ build.outDir = 'dist' 설정
```

**확인 방법**

```bash
cat vite.config.js | grep -A 10 "build"

# recharts, query 청크가 분리되어 있는지 확인
```

---

## ✅ Step 3: Tailwind CSS 설정 검증

### 3-1. Tailwind 의존성 설치 확인

**검증 항목**

```
✓ tailwindcss 설치됨
✓ postcss 설치됨
✓ autoprefixer 설치됨
```

**확인 방법**

```bash
npm list tailwindcss postcss autoprefixer

# 예상 출력:
# my-portfolio-hub@0.1.0
# ├── autoprefixer@10.4.x
# ├── postcss@8.4.x
# └── tailwindcss@3.4.x
```

### 3-2. tailwind.config.js 검증

**검증 항목**

```
✓ tailwind.config.js 파일 존재
✓ content 배열에 필요한 경로 포함
  - "./index.html"
  - "./src/**/*.{js,jsx}"
✓ 커스텀 색상 정의 확인
```

**확인 방법**

```bash
# 파일 존재 확인
ls -la tailwind.config.js

# content 배열 확인
cat tailwind.config.js | grep -A 5 "content:"

# 커스텀 색상 확인
cat tailwind.config.js | grep -A 5 "colors:"
```

**수동 검증**

```javascript
// 브라우저 개발자 도구에서 실행 (개발 서버 실행 후)
// 어떤 요소든 우클릭 → 검사 → Styles 탭에서 Tailwind CSS 스타일이 적용되는지 확인
```

### 3-3. postcss.config.js 검증

**검증 항목**

```
✓ postcss.config.js 파일 존재
✓ tailwindcss 플러그인 설정 확인
✓ autoprefixer 플러그인 설정 확인
```

**확인 방법**

```bash
cat postcss.config.js

# 예상 출력:
# export default {
#   plugins: {
#     tailwindcss: {},
#     autoprefixer: {},
#   },
# }
```

### 3-4. src/index.css 검증

**검증 항목**

```
✓ index.css 파일 존재
✓ @tailwind 지시문 포함 (base, components, utilities)
✓ 커스텀 컴포넌트 클래스 정의
```

**확인 방법**

```bash
cat src/index.css | head -20

# 예상 출력:
# @tailwind base;
# @tailwind components;
# @tailwind utilities;
```

---

## ✅ Step 4: shadcn/ui 초기화 검증

### 4-1. shadcn/ui 컴포넌트 설치 확인

**검증 항목**

```
✓ src/components/ui/ 폴더 생성됨
✓ 필수 컴포넌트 파일 존재
  - button.jsx
  - card.jsx
  - dialog.jsx
  - table.jsx
  - tabs.jsx
  - select.jsx
  - command.jsx
  - tooltip.jsx
```

**확인 방법**

```bash
# ui 폴더 확인
ls -la src/components/ui/

# 컴포넌트 파일 개수 확인 (최소 8개)
ls -la src/components/ui/ | wc -l
# 결과: 11 이상 (포함: .gitkeep, 8개 컴포넌트)
```

### 4-2. shadcn/ui 유틸리티 함수 확인

**검증 항목**

```
✓ src/lib/utils.js 파일 존재
✓ clsx, cn 함수 포함
```

**확인 방법**

```bash
cat src/lib/utils.js

# 예상 포함 내용:
# import { clsx } from "clsx"
# import { twMerge } from "tailwind-merge"
#
# export function cn(...inputs) {
#   return twMerge(clsx(inputs))
# }
```

### 4-3. shadcn/ui 의존성 설치 확인

**검증 항목**

```
✓ class-variance-authority 설치됨
✓ clsx 설치됨
✓ tailwind-merge 설치됨
```

**확인 방법**

```bash
npm list class-variance-authority clsx tailwind-merge

# 예상 출력:
# ├── class-variance-authority@0.7.x
# ├── clsx@2.x.x
# └── tailwind-merge@2.x.x
```

### 4-4. 컴포넌트 임포트 테스트

**검증 항목**

```
✓ Button 컴포넌트 임포트 가능
✓ Card 컴포넌트 임포트 가능
```

**테스트 코드 (src/components/common/Header.jsx에 임시 추가)**

```javascript
import { Button } from '@/components/ui/button'
import { Card } from '@/components/ui/card'

export default function Header() {
  return (
    <Card>
      <Button>테스트 버튼</Button>
    </Card>
  )
}
```

**확인 방법**

```bash
# 개발 서버 실행 후 브라우저에서 확인
npm run dev

# 예상:
# - 콘솔에 에러 없음
# - Button과 Card가 정상 렌더링됨
```

---

## ✅ Step 5: 폴더 구조 및 기본 파일 검증

### 5-1. 폴더 구조 확인

**검증 항목**

```
src/
  ✓ agents/
  ✓ api/
  ✓ components/
    ✓ common/
    ✓ portfolio/
    ✓ charts/
    ✓ chat/
    ✓ ui/
  ✓ pages/
  ✓ store/
  ✓ utils/
```

**확인 방법**

```bash
# 폴더 트리 출력
tree src/ -L 2

# 또는 Windows powershell에서:
Get-ChildItem -Path src -Recurse -Directory | Format-List FullName
```

### 5-2. 필수 파일 존재 확인

**검증 항목**

```
✓ src/main.jsx 존재
✓ src/App.jsx 존재
✓ src/index.css 존재
✓ index.html 존재
```

**확인 방법**

```bash
# 파일 존재 확인
ls -la src/{main.jsx,App.jsx,index.css}
ls -la index.html

# 예상 결과: 모두 파일이 존재해야 함
```

### 5-3. 페이지 파일 확인

**검증 항목**

```
✓ src/pages/Dashboard.jsx
✓ src/pages/Portfolio.jsx
✓ src/pages/Research.jsx
✓ src/pages/Watchlist.jsx
✓ src/pages/Reports.jsx
```

**확인 방법**

```bash
ls -la src/pages/

# 예상 결과: 5개 .jsx 파일
```

### 5-4. 공통 컴포넌트 확인

**검증 항목**

```
✓ src/components/common/Header.jsx
✓ src/components/common/Sidebar.jsx
✓ src/components/common/LoadingSpinner.jsx
```

**확인 방법**

```bash
ls -la src/components/common/

# 예상 결과: 3개 .jsx 파일
```

---

## ✅ Step 6: 필수 컴포넌트 코드 검증

### 6-1. App.jsx 라우팅 검증

**검증 항목**

```
✓ BrowserRouter 사용
✓ 5개 Route 정의
  - / → Dashboard
  - /portfolio → Portfolio
  - /research → Research
  - /watchlist → Watchlist
  - /reports → Reports
✓ Header, Sidebar 컴포넌트 포함
```

**확인 방법**

```bash
cat src/App.jsx | grep -E "Route|BrowserRouter"

# 예상:
# <BrowserRouter>
# <Route path="/" element={<Dashboard />} />
# <Route path="/portfolio" element={<Portfolio />} />
# ... (나머지 라우트)
```

### 6-2. Sidebar 네비게이션 검증

**검증 항목**

```
✓ 5개의 메뉴 항목 정의
✓ lucide-react 아이콘 사용
✓ Link로 라우팅 연결
```

**확인 방법**

```bash
cat src/components/common/Sidebar.jsx | grep -E "menu|Link|icon"

# 예상: 대시보드, 포트폴리오, 종목 탐색, 관심종목, 리포트
```

### 6-3. Header 컴포넌트 검증

**검증 항목**

```
✓ "My Portfolio Hub" 제목 표시
✓ Tailwind CSS로 스타일링됨
```

**확인 방법**

```bash
cat src/components/common/Header.jsx | grep "My Portfolio Hub"
```

---

## ✅ Step 7: Zustand 상태관리 검증

### 7-1. portfolioStore 검증

**검증 항목**

```
✓ usePortfolioStore 함수 정의
✓ persist 미들웨어 사용
✓ immer 미들웨어 사용
✓ holdings, cash 상태 정의
✓ addHolding, updatePrice, removeHolding 액션 정의
```

**확인 방법**

```bash
cat src/store/portfolioStore.js | grep -E "create|persist|immer|addHolding|updatePrice"
```

### 7-2. watchlistStore 검증

**검증 항목**

```
✓ useWatchlistStore 함수 정의
✓ watchlist 상태 정의
✓ addToWatchlist, removeFromWatchlist 액션 정의
```

**확인 방법**

```bash
cat src/store/watchlistStore.js | grep -E "create|addToWatchlist|removeFromWatchlist"
```

### 7-3. settingsStore 검증

**검증 항목**

```
✓ useSettingsStore 함수 정의
✓ theme, language 상태 정의
```

**확인 방법**

```bash
cat src/store/settingsStore.js | grep -E "theme|language"
```

### 7-4. 상태관리 단위 테스트

**테스트 코드 (src/pages/Dashboard.jsx에 임시 추가)**

```javascript
import { usePortfolioStore } from '@/store/portfolioStore'

export default function Dashboard() {
  const { holdings, cash, addHolding } = usePortfolioStore()

  const handleTestAdd = () => {
    addHolding({
      ticker: 'TEST',
      name: '테스트 종목',
      quantity: 100,
      avg_price: 10000,
      current_price: 11000,
    })
  }

  return (
    <div className="p-6">
      <h2>대시보드</h2>
      <p>보유 종목 수: {holdings.length}</p>
      <p>현금: {cash.toLocaleString()}원</p>
      <button onClick={handleTestAdd}>테스트 종목 추가</button>
    </div>
  )
}
```

**확인 방법**

```bash
# 개발 서버 실행
npm run dev

# 브라우저에서:
# 1. http://localhost:5173 접속
# 2. "테스트 종목 추가" 버튼 클릭
# 3. "보유 종목 수: 1"로 변경되는지 확인
# 4. 페이지 새로고침 → "보유 종목 수: 1" 유지되는지 확인 (localStorage 저장)
```

---

## ✅ Step 8: 환경변수 설정 검증

### 8-1. .env 파일 확인

**검증 항목**

```
✓ .env 파일 존재
✓ 필수 환경변수 정의:
  - ANTHROPIC_API_KEY (또는 비워둠)
  - VITE_API_BASE_URL
  - VITE_ALPHA_VANTAGE_KEY (선택)
  - VITE_KRX_API_KEY (선택)
```

**확인 방법**

```bash
cat .env

# 예상 형식:
# ANTHROPIC_API_KEY=sk-ant-xxxxx
# VITE_API_BASE_URL=http://localhost:5173/api
```

### 8-2. .env.example 확인

**검증 항목**

```
✓ .env.example 파일 존재
✓ 실제 키값 포함 안 됨
✓ 플레이스홀더만 포함
```

**확인 방법**

```bash
cat .env.example

# 예상: sk-ant-xxxxx 형태의 플레이스홀더만 포함
# 실제 API 키가 노출되지 않아야 함
```

### 8-3. .gitignore 확인

**검증 항목**

```
✓ .gitignore 파일 존재
✓ .env 파일이 무시 목록에 포함
✓ node_modules/ 무시
✓ dist/ 무시
```

**확인 방법**

```bash
cat .gitignore | grep -E "^\.env$|^node_modules|^dist"

# 예상 결과:
# .env
# node_modules/
# dist/
```

### 8-4. 환경변수 로드 테스트

**테스트 코드 (src/pages/Dashboard.jsx에 임시 추가)**

```javascript
export default function Dashboard() {
  const apiUrl = import.meta.env.VITE_API_BASE_URL

  return (
    <div className="p-6">
      <h2>환경변수 테스트</h2>
      <p>API Base URL: {apiUrl || '(설정되지 않음)'}</p>
    </div>
  )
}
```

**확인 방법**

```bash
npm run dev

# 브라우저에서:
# API Base URL이 정상 표시되는지 확인
# 콘솔에 에러 없는지 확인
```

---

## ✅ Step 9: 유틸리티 함수 검증

### 9-1. formatters.js 검증

**검증 항목**

```
✓ formatCurrency 함수 정의
✓ formatPercent 함수 정의
✓ formatDate 함수 정의
```

**확인 방법**

```bash
cat src/utils/formatters.js | grep -E "export const"
```

### 9-2. calculator.js 검증

**검증 항목**

```
✓ calculateReturn 함수 정의
✓ calculateTotalValue 함수 정의
✓ calculatePortfolioReturn 함수 정의
```

**확인 방법**

```bash
cat src/utils/calculator.js | grep -E "export const"
```

### 9-3. storage.js 검증

**검증 항목**

```
✓ storage 객체 정의
✓ get, set, remove, clear 메서드 포함
```

**확인 방법**

```bash
cat src/utils/storage.js | grep -E "get:|set:|remove:|clear:"
```

### 9-4. 유틸리티 함수 단위 테스트

**테스트 코드 (브라우저 콘솔)**

```javascript
// 개발 서버 실행 후 브라우저 개발자 도구 → Console 탭에서 실행

// 1. formatCurrency 테스트
import { formatCurrency } from './utils/formatters.js'
formatCurrency(1000000)
// 예상: "1,000,000원"

// 2. formatPercent 테스트
import { formatPercent } from './utils/formatters.js'
formatPercent(12.345)
// 예상: "+12.35%"

// 3. calculateReturn 테스트
import { calculateReturn } from './utils/calculator.js'
calculateReturn(10000, 11000)
// 예상: 10 (10% 수익)

// 4. storage 테스트
import { storage } from './utils/storage.js'
storage.set('test', { data: 'test' })
storage.get('test')
// 예상: { data: 'test' }
```

---

## ✅ Step 10: GitHub 저장소 검증

### 10-1. Git 저장소 초기화 확인

**검증 항목**

```
✓ .git 폴더 생성됨
✓ git status 정상 작동
```

**확인 방법**

```bash
# Git 저장소 확인
git status

# 예상 출력:
# On branch main
# nothing to commit, working tree clean
```

### 10-2. 초기 커밋 확인

**검증 항목**

```
✓ 초기 커밋 존재
✓ 커밋 메시지 포함
```

**확인 방법**

```bash
git log --oneline | head -5

# 예상:
# xxxxxxx Initial commit: Phase 1 프로젝트 초기화 완료
```

### 10-3. 원격 저장소 연동 확인

**검증 항목**

```
✓ origin 원격 저장소 설정됨
✓ main 브랜치 설정됨
```

**확인 방법**

```bash
git remote -v

# 예상:
# origin  https://github.com/MoonSongIT/my-portfolio-hub.git (fetch)
# origin  https://github.com/MoonSongIT/my-portfolio-hub.git (push)

git branch

# 예상:
# * main
```

### 10-4. Push 완료 확인

**검증 항목**

```
✓ GitHub에 push 완료
✓ README 또는 파일이 GitHub 저장소에 보임
```

**확인 방법**

```bash
# GitHub 웹사이트에서 MoonSongIT/my-portfolio-hub 저장소 확인
# https://github.com/MoonSongIT/my-portfolio-hub

# 또는 명령줄에서:
git ls-remote origin

# 예상:
# ref: refs/heads/main HEAD
# xxxxxxx refs/heads/main
```

---

## ✅ Step 11: 개발 서버 실행 및 통합 테스트

### 11-1. 개발 서버 시작

**검증 항목**

```
✓ npm run dev 실행 성공
✓ 포트 5173에서 개발 서버 시작
✓ 브라우저 자동 열기 (또는 수동으로 http://localhost:5173 접속)
✓ 콘솔에 에러 없음
```

**실행 명령어**

```bash
npm run dev
```

**예상 출력**

```
> my-portfolio-hub@0.1.0 dev
> vite

  VITE v5.0.0  ready in 123 ms

  ➜  Local:   http://localhost:5173/
  ➜  press h + enter to show help
```

### 11-2. 브라우저에서 페이지 로드 확인

**검증 항목**

```
✓ http://localhost:5173 접속 성공
✓ 페이지 렌더링 완료
✓ "My Portfolio Hub" 제목 보임
✓ 사이드바에 5개 메뉴 항목 보임
✓ 콘솔 에러 없음
```

**수동 확인 단계**

1. 브라우저 개발자 도구 열기 (F12)
2. Console 탭 확인 → 빨간색 에러 없는지 확인
3. Elements 탭에서 HTML 구조 확인
4. Network 탭에서 상태코드 200인 요청 확인

### 11-3. 라우팅 검증

**검증 항목**

```
✓ 각 메뉴 클릭 시 페이지 이동
✓ URL 변경됨
✓ 컴포넌트가 교체됨
```

**수동 테스트 단계**

```
1. 사이드바에서 "포트폴리오" 클릭
   예상: URL이 /portfolio로 변경, Portfolio 페이지 표시

2. "종목 탐색" 클릭
   예상: URL이 /research로 변경

3. "관심종목" 클릭
   예상: URL이 /watchlist로 변경

4. "리포트" 클릭
   예상: URL이 /reports로 변경

5. 브라우저 뒤로 가기 버튼
   예상: 이전 페이지로 이동 (히스토리 작동)

6. 직접 URL 입력 (http://localhost:5173/portfolio)
   예상: 해당 페이지 직접 로드
```

### 11-4. 스타일링 검증

**검증 항목**

```
✓ Tailwind CSS 스타일 적용됨
✓ 사이드바가 어두운 색 (회색)
✓ 메인 콘텐츠 영역이 밝은 색
✓ 텍스트가 읽기 쉬운 크기
```

**브라우저에서 확인**

- [ ] 사이드바: 어두운 배경색 (회색)
- [ ] 헤더: 흰색 배경, 제목 검은색 텍스트
- [ ] 메인 영역: 밝은 회색 배경
- [ ] 폰트: 명확하고 읽기 쉬움

### 11-5. lucide-react 아이콘 확인

**검증 항목**

```
✓ 각 메뉴 옆에 아이콘 표시됨
✓ 아이콘이 올바른 종류 (집, 서류, 검색 등)
```

**수동 확인**

- [ ] 대시보드 옆: 집 아이콘
- [ ] 포트폴리오 옆: 서류 아이콘
- [ ] 종목 탐색 옆: 돋보기 아이콘
- [ ] 관심종목 옆: 눈 아이콘
- [ ] 리포트 옆: 차트 아이콘

### 11-6. 콘솔 메시지 검증

**검증 항목**

```
✓ 콘솔에 에러 메시지 없음
✓ 콘솔에 경고 메시지 최소화
```

**확인 방법**

```bash
# 브라우저 개발자 도구 Console 탭에서 확인
# 필터: "Errors" 선택 → 0개
# 필터: "Warnings" 선택 → 없거나 최소 개수
```

---

## ✅ Step 12: 성능 및 최적화 검증

### 12-1. 번들 크기 확인

**검증 항목**

```
✓ 빌드 성공
✓ dist 폴더 생성됨
✓ 번들 크기 합리적 (JavaScript < 200KB)
```

**실행 명령어**

```bash
npm run build

# 예상 출력:
# vite v5.0.0 building for production...
#
# ✓ 123 modules transformed.
#
# dist/index.html              0.40 kB │ gzip: 0.30 kB
# dist/assets/index-xxx.js   150.25 kB │ gzip: 48.5 kB
```

### 12-2. 청크 분리 확인

**검증 항목**

```
✓ recharts, query 라이브러리가 별도 청크로 분리됨
✓ 여러 .js 파일이 생성됨
```

**확인 방법**

```bash
ls -la dist/assets/

# 예상: 여러 개의 .js 파일이 있어야 함
# (index-xxx.js, recharts-xxx.js, query-xxx.js 등)
```

### 12-3. 빌드 미리보기

**검증 항목**

```
✓ npm run preview 실행 성공
✓ 프로덕션 빌드가 브라우저에서 정상 작동
```

**실행 명령어**

```bash
npm run preview

# 예상 출력:
# ➜  Local:   http://localhost:4173/
```

**확인 방법**

```bash
# 별도 터미널에서
curl -s http://localhost:4173 | grep -i "portfolio"

# 예상: HTML 응답에 "Portfolio" 포함
```

---

## ✅ 최종 점검 체크리스트

### 파일 및 폴더 체크리스트

```
Phase 1 완료 체크리스트
===================

📦 루트 레벨 파일
  ☐ package.json (의존성 정의)
  ☐ package-lock.json (의존성 잠금)
  ☐ vite.config.js (Vite 설정)
  ☐ tailwind.config.js (Tailwind 설정)
  ☐ postcss.config.js (PostCSS 설정)
  ☐ index.html (진입점)
  ☐ .env (환경변수)
  ☐ .env.example (환경변수 예시)
  ☐ .gitignore (Git 무시 목록)
  ☐ CLAUDE.md (프로젝트 지침)

📁 src/ 폴더
  ☐ main.jsx (React 진입점)
  ☐ App.jsx (메인 앱 컴포넌트)
  ☐ index.css (전역 스타일)

📁 src/components/
  ☐ common/Header.jsx
  ☐ common/Sidebar.jsx
  ☐ common/LoadingSpinner.jsx
  ☐ ui/ (shadcn/ui 컴포넌트 8개 이상)

📁 src/pages/
  ☐ Dashboard.jsx
  ☐ Portfolio.jsx
  ☐ Research.jsx
  ☐ Watchlist.jsx
  ☐ Reports.jsx

📁 src/store/
  ☐ portfolioStore.js
  ☐ watchlistStore.js
  ☐ settingsStore.js

📁 src/utils/
  ☐ formatters.js
  ☐ calculator.js
  ☐ storage.js
  ☐ lib/utils.js (shadcn/ui)

📁 src/agents/ (빈 폴더)
  ☐ orchestrator.js
  ☐ researchAgent.js
  ☐ portfolioAgent.js
  ☐ alertAgent.js
  ☐ reportAgent.js

📁 src/api/ (빈 폴더)
  ☐ claudeApi.js
  ☐ stockApi.js
```

### 기능 검증 체크리스트

```
개발 환경 및 기본 기능
===================

개발 서버
  ☐ npm run dev 실행 성공
  ☐ 브라우저에서 http://localhost:5173 접속 가능
  ☐ 콘솔에 에러 없음

라우팅
  ☐ 대시보드 페이지 로드 (/)
  ☐ 포트폴리오 페이지 로드 (/portfolio)
  ☐ 종목 탐색 페이지 로드 (/research)
  ☐ 관심종목 페이지 로드 (/watchlist)
  ☐ 리포트 페이지 로드 (/reports)
  ☐ 메뉴 클릭으로 페이지 이동
  ☐ 브라우저 뒤로가기 작동

UI/스타일
  ☐ Tailwind CSS 스타일 적용됨
  ☐ shadcn/ui 버튼/카드 렌더링됨
  ☐ lucide-react 아이콘 표시됨
  ☐ 반응형 레이아웃 (사이드바, 메인 영역)

상태관리
  ☐ Zustand 스토어 생성 가능
  ☐ 상태 업데이트 작동
  ☐ localStorage 자동 저장
  ☐ 페이지 새로고침 후 상태 유지

환경변수
  ☐ .env 파일 로드됨
  ☐ import.meta.env.VITE_* 접근 가능
  ☐ 민감한 키 노출 안 됨

빌드
  ☐ npm run build 성공
  ☐ dist 폴더 생성됨
  ☐ npm run preview 작동
  ☐ 프로덕션 빌드가 브라우저에서 작동

GitHub
  ☐ Git 저장소 초기화 완료
  ☐ MoonSongIT 원격 저장소 연동
  ☐ 초기 커밋 완료
  ☐ main 브랜치 push 완료
```

---

## 📊 점검 결과 기록

### 실행 일시: ___________________

### 총 검증 항목: 100+

### 통과한 항목: _____ / 100+

### 통과율: _____%

### 발견된 이슈

| 이슈 | 심각도 | 상태 | 비고 |
| ---- | ------ | ---- | ---- |
|      |        |      |      |
|      |        |      |      |

---

## ✅ Phase 1 완료 기준

다음 항목이 모두 확인되면 Phase 1 완료:

```
필수 조건 (모두 필수)
  ☐ npm run dev 실행 가능 (에러 없음)
  ☐ 모든 페이지 라우팅 작동
  ☐ Tailwind CSS 스타일 적용됨
  ☐ shadcn/ui 컴포넌트 임포트 가능
  ☐ Zustand 상태관리 작동
  ☐ 환경변수 설정 완료
  ☐ GitHub push 완료
  ☐ npm run build 성공

최종 검증 (모두 필수)
  ☐ 콘솔 에러 0개
  ☐ 콘솔 경고 최소 (무시 가능 범위)
  ☐ 브라우저에서 시각적 문제 없음
  ☐ 모든 메뉴 클릭 작동
  ☐ 새로고침 후 상태 유지
```

---

> **다음 단계**: Phase 2 상세 계획 및 체크리스트 진행
>
> Phase 2에서는 샘플 데이터를 활용하여 포트폴리오 관리 UI를 구현합니다.

*작성: Claude Code | 날짜: 2026-04-01*
*예상 검증 소요 시간: 30-45분*
