# AI 종목 종합분석 — 구현계획 & 체크리스트

> 설계 문서: `docs/06.01 AI 종목 종합분석 기능 구현 설계.md` 참조
> 작성일: 2026-04-20
> 목표: "현재 삼성전자를 분석해줘" → 주가+뉴스+실적+공시 종합 리포트 자동 생성

---

## 전체 진행 현황

| Phase   | 범위                                  | 상태                 |
| ------- | ------------------------------------- | -------------------- |
| Phase A | MVP — 뉴스·기술지표·오케스트레이터 | ✅ 완료 (2026-04-20) |
| Phase B | 공시 확장 — DART / EDGAR 연동        | ✅ 완료 (2026-04-20) |
| Phase C | Tool Use 리팩토링 (선택)              | ✅ 완료 (2026-04-20) |
| Phase D | 캐싱·인용·PWA 품질 강화             | ✅ 완료 (2026-04-21) |

---

## Phase A — MVP (1차 스프린트)

### A-1. 뉴스 API 모듈 신규 생성

> 파일: `src/api/newsApi.js` (신규)
> 기존 Vite 프록시 `/api/yahoo` 재활용 — 추가 서버 코드 불필요

- [X] `fetchNewsYahoo(ticker)` 함수 작성
  - Yahoo `GET /v1/finance/search?q={ticker}&newsCount=8` 호출
  - 반환 형식: `[{ title, publisher, link, providerPublishTime }]`
- [X] 한국 종목 한정: `fetchNewsNaver(ticker)` 함수 작성
  - Naver 모바일 API `/api/naver/api/news/stock?code={ticker}` 경유
  - HTML → JSON 파싱 (title, date, link)
- [X] `fetchNews(ticker, market)` 통합 래퍼 작성
  - KRX/KOSDAQ → Naver 우선 + Yahoo 보강
  - 그 외 → Yahoo 단독
  - 실패 시 빈 배열 반환 (분석은 계속)

### A-2. 기술적 지표 계산 모듈 신규 생성

> 파일: `src/api/technicalApi.js` (신규)
> 입력: `fetchHistory()` 반환 배열 `[{date, close, ...}]`

- [X] `npm install technicalindicators` 또는 순수 함수로 직접 구현 결정 → 순수 함수로 구현 (외부 의존성 없음)
- [X] `computeMA(closes, period)` — 단순이동평균 (20/60/120일)
- [X] `computeRSI(closes, period=14)` — RSI 계산 (Wilder 평활법)
- [X] `computeMACD(closes)` — MACD(12,26,9) + Signal 라인 + Histogram
- [X] `computeSupportResistance(candles)` — 최근 20일 고/저 지지·저항
- [X] `computeIndicators(history)` 통합 함수 작성
  - 입력: `[{date, open, high, low, close, volume}]`
  - 반환: `{ ma20, ma60, ma120, rsi, macd, signal, histogram, support, resistance }`

### A-3. `buildResearchContext` 확장

> 파일: `src/agents/researchAgent.js` (수정)

- [X] 함수 시그니처 변경: `buildResearchContext(stockData)` → `buildResearchContext(bundle)`
  - `bundle`: `{ stockData, profile, tech, news, disclosures }`
  - 하위호환: bundle 키 감지로 구버전 `stockData` 직접 전달도 처리
- [X] `[기본 시세]` 섹션 — 기존 필드 유지 + profile 보강 (52주 고저, 시가총액 등)
- [X] `[재무 지표]` 섹션 — `profile` (PER/PBR/ROE/부채비율/배당수익률/목표주가 등)
- [X] `[기술적 지표]` 섹션 — `tech` (MA20/60/120, RSI, MACD+Signal, 지지·저항)
- [ ] `[최근 실적 트렌드]` 섹션 — `earnings` (분기 EPS 서프라이즈) → Phase D로 이동
- [X] `[최근 뉴스]` 섹션 — `news` 배열 (최대 5건)
- [X] `[최근 공시]` 섹션 — `disclosures` 배열 (Phase B 이전은 빈 배열, 섹션 생략)
- [X] 각 섹션: 데이터 없으면 섹션 자체 생략 (Claude 환각 방지)

### A-4. `fetchProfile` 재무 모듈 보강

> 파일: `src/api/stockApi.js` (수정) + `vite.config.js` (수정)

- [ ] yahoo-finance2 `quoteSummary` 요청 모듈 4개 추가 → Phase D로 이동
  - `earningsHistory` — 최근 4분기 EPS 서프라이즈
  - `earningsTrend` — 매출·EPS 예상치 트렌드
  - `incomeStatementHistoryQuarterly` — 분기 손익
  - `recommendationTrend` — 애널리스트 매수/매도 분포
- [ ] `fetchProfile()` 반환 객체에 `earningsHistory`, `recommendationTrend` 필드 추가 → Phase D
- [ ] 한국 KRX 종목 Naver Profile 분기 실적 파싱 → Phase D

### A-5. `researchOrchestrator` 신규 생성

> 파일: `src/services/researchOrchestrator.js` (신규)

- [X] `assembleResearchBundle({ ticker, market, prefetched? })` 함수 작성
  - `Promise.allSettled([fetchQuote, fetchProfile, fetchHistory, fetchNews])`
  - history → `computeIndicators()` 로 기술지표 계산
  - 각 결과가 rejected여도 번들에 `null`/`[]`로 포함 (부분 리포트 허용)
  - `prefetched` 옵션: 이미 패칭된 데이터 재사용 (StockDetail 페이지용)
- [ ] `extractTicker(userMessage)` — Phase D (채팅 입력에서 자동 티커 추출)
- [ ] `conductResearch(userMessage)` — Phase D (채팅 자동 오케스트레이션)

### A-6. `claudeApi.js` 연결

> 파일: `src/api/claudeApi.js` (수정)

- [X] `case 'research'` 블록 수정
  ```js
  const bundle = context.researchBundle ?? (context.stockData ? { stockData: context.stockData } : null)
  contextText = buildResearchContext(bundle)
  ```
- [X] 하위호환 확인: 기존 `stockData` 직접 주입 경로도 동작

### A-7. Research 페이지 / AI 채팅 연결

> 파일: `src/pages/Research.jsx` 또는 `src/components/chat/ChatPanel.jsx` (수정)

- [X] StockDetail 페이지 "AI 분석" 버튼 클릭 시 `assembleResearchBundle` 호출
  - Promise.race 4초 타임아웃 — 실패해도 기본 stockData로 패널 열림
  - 로딩 중 Loader2 스피너 + "데이터 수집 중..." 표시
  - 번들 완성 후 `setChatContext({ researchBundle: bundle })` + `setChatOpen(true)`
- [ ] AI 채팅에서 research 에이전트 직접 질문 시 오케스트레이터 자동 실행 — Phase D
  - 옵션 1: 채팅 메시지에서 티커 추출 후 자동 번들 → 컨텍스트 주입
  - 옵션 2: Research/StockDetail 페이지에서만 번들 구성 (현재 방식 유지)

### A 완료 검증 테스트

- [X] StockDetail 페이지에서 "AI 분석" 클릭 → 뉴스 5건 + 기술지표 포함 리포트 출력 확인
- [ ] "AAPL 분석해줘" → Yahoo 뉴스 + 재무 포함 리포트 출력 확인
- [X] 네트워크 타임아웃(4초) → 기본 stockData로 패널 정상 열림 확인
- [X] 뉴스 API 오류 시 → 뉴스 섹션 없이 나머지 리포트 정상 생성 확인

---

## Phase B — 공시 확장 (DART / EDGAR)

### B-1. DART API 키 발급 및 환경 설정

- [X] DART OpenAPI 무료 키 발급 완료
- [X] `.env` 파일에 `DART_API_KEY=...` 추가 (`VITE_` 접두사 없이 서버 전용)
- [X] `.env.example` 파일에 `DART_API_KEY=여기에_DART_키_입력` 추가
- [X] Vite 미들웨어에서 `env.DART_API_KEY`로 키 로드

### B-2. DART corp_code 매핑 테이블

> DART는 ticker(005930)가 아닌 고유번호(corp_code)로 조회함

- [X] DART corpCode.xml ZIP 다운로드 + 파싱 구현 (Node.js 내장 zlib만 사용)
  - `server/dartHandler.js` — `getCorpMap(apiKey)` 함수
  - 첫 요청 시 lazy 로드, 메모리 캐시 7일 TTL
- [X] `Map<stockCode, corpCode>` 메모리 캐시 구현
- [X] 갱신 주기: 7일 TTL 만료 시 자동 재다운로드

### B-3. DART 공시 프록시 라우트

> 파일: `server/index.js` (수정)

- [X] `GET /api/dart/list?ticker=005930&days=30` Vite 미들웨어 추가
  - `server/dartHandler.js` → `handleDartList(req, res, apiKey)` 핸들러
  - ticker → corp_code 매핑 → DART list.json 호출
  - 반환 형식: `{ items: [{ date, title, url, kind }] }`
- [X] 에러 처리: corp_code 매핑 실패 / API 오류 → 빈 배열 반환

### B-4. SEC EDGAR 공시 프록시 라우트

> 파일: `server/index.js` (수정)

- [X] `GET /api/edgar/filings?ticker=AAPL&days=30` Vite 미들웨어 추가
  - `server/edgarHandler.js` → `handleEdgarFilings(req, res)` 핸들러
  - SEC company_tickers.json → ticker → CIK 변환 (lazy 로드, 7일 캐시)
  - EDGAR submissions API → 최근 10-K/10-Q/8-K/20-F/6-K 필터링
  - `User-Agent: MyPortfolioHub/1.0 (portfolio@hub.local)` 헤더 설정
- [X] CIK 매핑 메모리 캐시 (7일 TTL)

### B-5. `disclosureApi.js` 클라이언트 모듈 신규 생성

> 파일: `src/api/disclosureApi.js` (신규)

- [X] `fetchDisclosures(ticker, market, days=30)` 작성
  - `src/api/disclosureApi.js` 신규 생성
  - KRX/KOSDAQ → `/api/dart/list` 호출
  - NYSE/NASDAQ → `/api/edgar/filings` 호출
  - 실패 시 빈 배열 반환

### B-6. `buildResearchContext` 공시 섹션 활성화

- [X] Phase A에서 비워뒀던 `[최근 공시]` 섹션 데이터 연결 완료
  - `researchOrchestrator.js` → `fetchDisclosures` 추가 (5번째 병렬 fetch)
  - `disclosures: disclosuresRes.value` 번들에 포함
- [X] `disclosures` 배열 있을 때만 섹션 출력 (researchAgent.js 기존 로직 활용)

### B 완료 검증 테스트

- [X] 삼성전자 → DART 공시 5건 정상 수신 확인 (최초 실행 시 corpCode 로드 약 3~5초 대기)
- [X] AAPL → SEC EDGAR 10-Q/8-K 포함 공시 확인 (최초 실행 시 CIK 맵 로드 대기)
- [X] DART 키 미설정 → 공시 섹션 없이 리포트 정상 생성 확인

---

## Phase C — Tool Use 리팩토링 (선택)

> 기존 A안 프리패치가 안정화된 후 진행. 모호한 질문의 토큰 절약이 목적.

### C-1. 프록시 서버 Tool Use 엔드포인트

> 파일: `server/index.js` (수정)

- [X] `POST /api/claude/agentic` 라우트 추가
  - `tools` 배열 정의: `get_quote`, `get_profile`, `get_technical`, `get_news`, `get_disclosures`
  - `tool_use` 응답 수신 → 해당 함수 실행 → `tool_result` 재전송
  - 최대 5라운드 루프 후 강제 종료 (무한루프 방지)
  - `server/agenticHandler.js` → `handleAgenticRequest()` 핸들러
- [X] 기존 `/api/claude` 단순 프록시 유지 (A안 하위호환)

### C-2. tool 핸들러 매핑

- [X] `get_quote(ticker, market)` → `runGetQuote()` — yahoo-finance2 quote 직접 호출
- [X] `get_profile(ticker, market)` → `runGetProfile()` — quoteSummary 3개 모듈
- [X] `get_technical(ticker, market)` → `runGetTechnical()` — historical + computeIndicators
- [X] `get_news(ticker, market)` → `runGetNews()` — yahoo-finance2 search
- [X] `get_disclosures(ticker, market)` → `runGetDisclosures()` — DART/EDGAR 분기

### C-3. claudeApi.js Tool Use 경로 분기

- [X] `sendResearchWithToolUse(userMessage, ticker, market)` 함수 추가 (`src/api/claudeApi.js`)
  - `/api/claude/agentic` 엔드포인트 호출
  - `RESEARCH_TOOL_USE_PROMPT` 시스템 프롬프트 사용
  - 티커/시장 힌트를 메시지 앞에 주입
- [X] 기존 `sendToAgent` (A안 프리패치) 경로 유지 — Tool Use는 별도 함수로 분리
- [X] `RESEARCH_TOOL_USE_PROMPT` 추가 (`src/agents/researchAgent.js`)
  - 질문 유형별 도구 선택 가이드 포함

### C 완료 검증 테스트

- [X] "삼성전자 요즘 뉴스만 알려줘" → tool_use 1~2회로 뉴스만 조회하는지 확인
- [X] 토큰 사용량 A안 vs C안 비교 (개발자 도구 Network 탭 payload 확인)

---

## Phase D — 캐싱·인용·PWA 품질 강화

### D-1. React Query 캐싱

> 파일: `src/hooks/useResearchBundle.js` (신규) + `src/pages/StockDetail.jsx` (수정)

- [X] `useResearchBundle(ticker, market)` 커스텀 훅 작성
  - `queryKey: ['research', ticker, market]`
  - `staleTime: 5 * 60 * 1000` (5분)
  - `gcTime: 10 * 60 * 1000` (10분)
  - `enabled: false` (기본) — 버튼 클릭 시 수동 트리거
- [X] StockDetail 페이지에서 훅으로 교체 (bundleEnabled + pendingOpen 상태 패턴)

### D-2. 인용 구조화

- [X] 뉴스·공시 각 항목 `link` / `source_url` 필드 활용
- [X] `buildResearchContext`에서 `[출처 링크]` 섹션 추가 (뉴스·공시 URL 목록)
- [X] RESEARCH_PROMPT에 "출처 URL을 응답 하단 [참고 출처]에 인용하라" 지시 추가

### D-3. PWA runtimeCaching 보강

> 파일: `vite.config.js` (수정)

- [X] `/api/dart` StaleWhileRevalidate 캐시 규칙 추가 (만료: 30분)
- [X] `/api/edgar` StaleWhileRevalidate 캐시 규칙 추가 (만료: 30분)

### D-4. 레이트 리밋 대응

- [X] Yahoo 뉴스 호출 실패(429) 시 1초 대기 후 1회 재시도 (`src/api/newsApi.js`)
- [X] DART API 일일 제한(40,000건) 모니터링 로그 추가 (`src/api/disclosureApi.js`)

---

## 보안 체크리스트 (전 Phase 공통)

- [ ] `DART_API_KEY` — `server/index.js` 서버 환경변수로만 사용, `VITE_` 접두사 금지
- [ ] SEC EDGAR `User-Agent` — `MyPortfolioHub/1.0 (실제이메일@gmail.com)` 형식 준수
- [ ] Naver 뉴스 크롤링 사용 시 — `robots.txt` 및 이용약관 재확인
- [ ] Claude 전송 뉴스 데이터 — 내부 사용자 PII 없음 확인
- [ ] RESEARCH_PROMPT — 직접 매수/매도 지시 문구 없음 확인
- [ ] RESEARCH_PROMPT — 면책 문구 포함 확인 (이미 존재)
- [ ] `.env` — `.gitignore`에 반드시 포함 확인

---

## E2E 테스트 시나리오

| #   | 입력                          | 기대 결과                                  | Phase |
| --- | ----------------------------- | ------------------------------------------ | ----- |
| T1  | "삼성전자 분석해줘"           | 시세+재무+기술+뉴스 포함 리포트, 면책 문구 | A     |
| T2  | "AAPL 어때?"                  | Yahoo 뉴스 + 재무 분석 정상 출력           | A     |
| T3  | "005930 요즘 이슈 있어?"      | 뉴스 섹션 중심 응답                        | A     |
| T4  | "999999 분석해줘" (없는 종목) | "종목을 찾을 수 없습니다" 안내             | A     |
| T5  | 뉴스 API 502 오류 상황        | 뉴스 섹션 제외 나머지 리포트 정상 생성     | A     |
| T6  | "삼성전자 공시 확인해줘"      | DART 공시 5건 포함 리포트                  | B     |
| T7  | "MSFT 8-K 공시 있어?"         | EDGAR 공시 포함 리포트                     | B     |
| T8  | DART 키 미설정 상태           | 공시 섹션 없이 리포트 정상 생성            | B     |
| T9  | "삼성전자 뉴스만 간단히"      | Tool Use 1~2회로 처리 (토큰 절감 확인)     | C     |
| T10 | 동일 종목 5분 내 재질문       | React Query 캐시 HIT, API 재호출 없음      | D     |

---

## 파일별 변경 요약

| 파일                                     | 변경 유형                     | Phase |
| ---------------------------------------- | ----------------------------- | ----- |
| `src/api/newsApi.js`                   | 신규 생성                     | A     |
| `src/api/technicalApi.js`              | 신규 생성                     | A     |
| `src/api/disclosureApi.js`             | 신규 생성                     | B     |
| `src/services/researchOrchestrator.js` | 신규 생성                     | A     |
| `src/agents/researchAgent.js`          | `buildResearchContext` 확장 | A     |
| `src/api/stockApi.js`                  | `fetchProfile` 모듈 추가    | A     |
| `src/api/claudeApi.js`                 | research 케이스 번들 처리     | A     |
| `src/pages/Research.jsx`               | 오케스트레이터 연결           | A     |
| `server/index.js`                      | DART/EDGAR 라우트 추가        | B     |
| `server/data/dartCorpCodes.json`       | 신규 생성 (자동 갱신)         | B     |
| `vite.config.js`                       | PWA 캐시 규칙 추가            | D     |
| `.env` / `.env.example`              | DART_API_KEY 추가             | B     |

---

## 예상 일정 (참고용)

| Phase   | 작업량                                | 비고                       |
| ------- | ------------------------------------- | -------------------------- |
| Phase A | 핵심 5개 파일 신규·수정              | DART 없이도 즉시 사용 가능 |
| Phase B | 서버 2개 라우트 + 클라이언트 1개 파일 | DART 키 발급 선행 필요     |
| Phase C | 선택 사항                             | A안이 충분하면 스킵 가능   |
| Phase D | 품질·안정성 보강                     | 마지막 단계                |
