# 10.09.03 AI 분석 기능 개선 — 구현 체크리스트

작성일: 2026-05-19  
브랜치: worktree-feature+ai-analysis-improvement

---

## Phase 1 — 에이전트 백엔드

### 1-1. analysisAgent.js 신규 생성

- [x] `src/agents/analysisAgent.js` 파일 생성
- [x] `ANALYSIS_PROMPT` 작성 (급등락 원인 분석 전용 시스템 프롬프트)
  - [x] 핵심 요인 / 세부 분석 / 투자자 유의점 출력 형식 지정
  - [x] 데이터 기반 분석, 추측 금지 지시사항
  - [x] 면책 문구 포함 지시
- [x] `MARKET_BRIEF_PROMPT` 작성 (시장 전반 브리핑 전용 프롬프트)
  - [x] 강세/약세/혼조 판단 + 국내/해외 구분 출력 형식
  - [x] 면책 문구 포함 지시
- [x] `buildMovementContext(ticker, name, changePercent, news, disclosures)` 구현
  - [x] 종목 기본 정보 (티커·종목명·등락률) 포함
  - [x] 뉴스 최대 8건 (제목·날짜·출처) 포함
  - [x] 공시 최대 5건 (제목·날짜·종류) 포함
- [x] `buildMarketBriefContext(indices, news)` 구현
  - [x] 지수 목록 (지수명·현재가·등락률) 포함
  - [x] 뉴스 최대 8건 포함

### 1-2. orchestrator.js 수정

- [x] `ROUTING_RULES` 배열 맨 앞에 `analysis` 규칙 추가 (최우선 라우팅)
  - [x] 키워드: '왜', '이유', '원인', '어떻게 된', '무슨 일', '급락', '급등', '폭락', '폭등', '하락 이유', '상승 이유', '오늘 시장', '시장 왜', '코스피 왜', '나스닥 왜'
- [x] `AGENT_LABELS`에 `analysis` 항목 추가
  - [x] label: '원인 분석', icon: '🔎', color: 'indigo'

---

## Phase 2 — UI 컴포넌트

### 2-1. MovementExplainerCard.jsx 신규 생성

- [ ] `src/components/research/MovementExplainerCard.jsx` 파일 생성
- [ ] Props: `{ ticker, name, changePercent, news, disclosures, market }`
- [ ] 자동 트리거: `|changePercent| >= 3` 이면 컴포넌트 마운트 시 자동 분석 시작
- [ ] "오늘의 이유 분석" 버튼: 변동률 무관하게 수동 실행 가능
- [ ] API 키 미설정 시 ApiKeyRequiredDialog 표시
- [ ] 로딩 상태: 스켈레톤 UI 표시
- [ ] 결과: ReactMarkdown으로 마크다운 렌더링
- [ ] 새로고침 버튼: sessionStorage 캐시 무효화 후 재요청
- [ ] 캐시 키: `movement_${ticker}_${today}` (today = YYYY-MM-DD)
- [ ] 오류 시: 에러 메시지 + 재시도 버튼
- [ ] 면책 문구 항상 표시

### 2-2. MarketBriefCard.jsx 신규 생성

- [ ] `src/components/research/MarketBriefCard.jsx` 파일 생성
- [ ] Props: 없음 (자체적으로 데이터 수집)
- [ ] 마운트 시 자동으로 지수 데이터 + 뉴스 수집 후 분석 시작
- [ ] 수집 대상 지수: `^KS11`(KOSPI), `^KQ11`(KOSDAQ), `^IXIC`(NASDAQ), `^GSPC`(S&P500)
- [ ] 뉴스 수집: Yahoo Finance `^GSPC` 기준 글로벌 마켓 뉴스
- [ ] API 키 미설정 시 카드 숨김 처리 (에러 표시 안 함)
- [ ] 로딩 상태: 스켈레톤 UI 표시
- [ ] 결과: ReactMarkdown 렌더링
- [ ] 갱신 버튼: 캐시 무효화 후 재요청
- [ ] 캐시 키: `market_brief_${today}`
- [ ] 날짜 표시: "AI 시장 브리핑 — YYYY-MM-DD"
- [ ] 면책 문구 항상 표시

### 2-3. Research.jsx 수정

- [ ] `MarketBriefCard` import 추가
- [ ] 기존 "시장 요약" 섹션 (MarketIndexCard 행) 바로 아래에 `<MarketBriefCard />` 삽입
- [ ] 기존 MarketIndexCard UI 변경 없음

### 2-4. ResearchDetail.jsx 수정

- [ ] `MovementExplainerCard` import 추가
- [ ] 시세 카드(가격·등락률 표시 영역) 아래, AI 종합 분석 섹션 위에 삽입
- [ ] `changePercent`, `news`, `disclosures`, `ticker`, `name`, `market` props 전달
- [ ] 기존 AI 분석 기능 변경 없음

---

## Phase 3 — 채팅 연동

### 3-1. ChatPanel.jsx 수정

- [ ] `routeToAgent` 반환값이 `'analysis'`일 때 케이스 추가
- [ ] 종목 티커 포함 여부 판별 (`extractTicker` 재사용)
  - [ ] 티커 있음: 뉴스+공시 수집 → `buildMovementContext` → `ANALYSIS_PROMPT`로 Claude 호출
  - [ ] 티커 없음: 지수 데이터+뉴스 수집 → `buildMarketBriefContext` → `MARKET_BRIEF_PROMPT`로 Claude 호출
- [ ] AgentBadge에서 `analysis` 에이전트 배지 표시 확인

---

## 검증 시나리오

### 시나리오 A — 종목 상세 급등락 자동 분석
- [ ] 삼성전자 상세 페이지 진입 시 등락률 -3% 이상이면 MovementExplainerCard 자동 표시
- [ ] "새로고침" 버튼 클릭 시 캐시 무시하고 재분석
- [ ] 변동률 0%일 때도 "오늘의 이유 분석" 버튼으로 수동 실행 가능

### 시나리오 B — 시장 브리핑
- [ ] 종목탐색 페이지 진입 시 MarketBriefCard 자동 로드
- [ ] 오늘 날짜 + 국내/해외 시장 구분 설명 포함
- [ ] "갱신" 버튼 클릭 시 재분석

### 시나리오 C — 채팅 원인 분석 라우팅
- [ ] 채팅에 "삼성전자 왜 떨어졌어?" → analysis 에이전트 배지 + 원인 분석 응답
- [ ] 채팅에 "오늘 코스피 왜 내려?" → analysis 에이전트 배지 + 시장 브리핑 응답
- [ ] 채팅에 "AAPL 급등 이유가 뭐야?" → analysis 에이전트 배지 + 원인 분석 응답

### 시나리오 D — API 키 미설정
- [ ] API 키 없을 때 MovementExplainerCard → ApiKeyRequiredDialog 표시
- [ ] API 키 없을 때 MarketBriefCard → 카드 자체 숨김

### 시나리오 E — 오류 처리
- [ ] AI API 500 에러 → 에러 메시지 + 재시도 버튼
- [ ] 뉴스 수집 실패 → 뉴스 없는 상태로 제한적 분석 수행

---

## 완료 기준

- [ ] Phase 1~3 모든 체크박스 완료
- [ ] 시나리오 A~E 모두 정상 동작 확인
- [ ] `npm run dev`로 빌드 오류 없음 확인
- [ ] 기존 ResearchAgent·AlertAgent 기능 회귀 없음 확인
