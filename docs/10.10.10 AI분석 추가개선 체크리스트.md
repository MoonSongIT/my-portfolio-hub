# AI분석 추가개선 체크리스트

_작성일: 2026-05-20_
_참조: 10.10.09 AI분석 추가개선 구현계획_

---

## 진행 현황

| Phase | 내용 | 상태 |
|-------|------|------|
| Phase 1 | 호출 기반 안정화 (오류 복구) | ✅ 완료 (2026-05-20) |
| Phase 2 | 컨텍스트 버그 수정 | ✅ 완료 (2026-05-20) |
| Phase 3 | 데이터 신선도 개선 + Research 프리패치 복구 | ✅ 완료 (2026-05-20) |
| Phase 4 | 응답 형식 구조화 | ✅ 완료 (2026-05-20) |
| Phase 5 | 라우팅 개선 (하이브리드 점수+Haiku 라우팅) | ✅ 완료 (2026-05-21) |
| Phase 6 | 에이전트 협력 | ✅ 완료 (2026-05-22) |
| Phase 7 | 피드백 루프 + 환각 감지 | ✅ 완료 (2026-05-22) |

---

## Phase 1 — 호출 기반 안정화 (10.10.07)

> **브랜치**: `feature/ai-error-recovery`
> **목표**: 모든 Claude 호출이 일시적 오류에도 자동 복구되도록 기반을 다진다.
> **완료 기준**: 네트워크 오프라인 → 온라인 복구 시 자동 재시도 성공. 잘린 응답에 안내 문구 표시.

### 구현 태스크

- [x] **1-1** `callClaudeWithRetry()` 공통 함수 추출 — `src/api/claudeApi.js`
- [x] **1-2** 네트워크 오류(ECONNRESET 등) + HTTP 503 재시도 추가 — `src/api/claudeApi.js`
- [x] **1-3** `sendWithHistory`에 재시도 로직 적용 — `src/api/claudeApi.js`
- [x] **1-4** `stop_reason === 'max_tokens'` 감지 → `incomplete` 플래그 반환 — `src/api/claudeApi.js`
- [x] **1-5** 잘린 응답 안내 문구 표시 — `src/components/chat/MessageBubble.jsx`
- [x] **1-6** `getErrorMessage` 사용자 친화적 메시지 확장 (429/401/503/네트워크 구분) — `src/api/claudeApi.js`

### 추가 수정 (2026-05-20)

- [x] **1-7** axios ECONNABORTED timeout → `fetch + AbortSignal.timeout(180s)` 교체 — `src/api/claudeApi.js`, `vite.config.js`
- [x] **1-8** Vite proxy SSE 버퍼링 고착 → JSON 응답으로 전환 (서버 180s timeout 동일 적용) — `vite.config.js`, `server/index.js`
- [x] **1-9** `incomplete` 플래그 전달 누락 수정 — `ChatPanel.jsx`, `AIChat.jsx`, `chatStore.js`
- [x] **1-10** `claudeApi.post` 직접 호출 3곳 → `callClaudeWithRetry` 통일 — `MovementExplainerCard.jsx`, `MarketBriefCard.jsx`, `Research.jsx`

### 완료 검증

- [ ] 네트워크 오프라인 → 온라인 전환 시 자동 재시도 성공 ← 수동 확인 필요
- [ ] HTTP 503 응답 시 재시도 후 성공 ← 수동 확인 필요
- [ ] `sendWithHistory` 네트워크 오류 시 재시도 동작 ← 수동 확인 필요
- [x] 잘린 응답에 "이어서 요청하세요" 안내 표시 ← 화면 확인 완료 (2026-05-20)
- [x] 기술적 오류 메시지("Network Error") 사용자에게 노출 없음 ← `getErrorMessage` 확인 완료
- [x] `stop_reason: max_tokens` → timeout 메시지 오표시 수정 ← 화면 확인 완료 (2026-05-20)

---

## Phase 2 — 컨텍스트 버그 수정 (10.10.02)

> **브랜치**: `feature/ai-context-bugfix`
> **목표**: 현재 완전히 동작이 깨진 버그를 수정한다.
> **완료 기준**: alert 에이전트 급등락 신호 정상 표시. "주간 리포트" 요청 시 weekly 프롬프트 적용.

### 구현 태스크

- [x] **2-1** `quotesMap` 실시간 시세 맵 구성 후 context에 포함 — `src/api/claudeApi.js` (alert case 내 병렬 fetch)
- [x] **2-2** `alertAgent.js` — `quotesMap` 수신해 🔴🟡🟢 신호 생성 — 2-1 구현으로 자동 해결 (기존 buildAlertContext 로직 완비)
- [x] **2-3** `changePercent` holdings에 포함 (analysis 경로 중복 fetch 제거) — `src/api/claudeApi.js`
- [x] **2-4** `context.period` 사용자 메시지에서 감지해 주입 — `src/pages/AIChat.jsx` (detectPeriod 함수)
- [x] **2-5** `reportAgent.js` — period별 프롬프트 분기 적용 — `src/api/claudeApi.js` (report case systemPrompt 교체)
- [x] **2-6** `journalCoachAgent` 멀티턴 시스템 프롬프트 중복 삽입 방지 — `src/api/claudeApi.js` (sendWithHistory filteredMessages)

### 완료 검증

- [x] alert 에이전트 — ±5% 이상 급등락 종목에 🔴🟡🟢 신호 정상 표시 ← 확인 완료 (2026-05-20)
- [x] "주간 리포트" 요청 시 weekly 프롬프트 적용 확인 ← 확인 완료 (2026-05-20)
- [x] 포트폴리오 분석 시 changePercent 있는 종목 중복 fetchQuote 없음 (네트워크 탭 확인) ← 확인 완료 (2026-05-20)
- [x] journal coach 멀티턴 시 두 번째 메시지부터 시스템 프롬프트 중복 없음 ← 확인 완료 (2026-05-20)

---

## Phase 3 — 데이터 신선도 개선 (10.10.03)

> **브랜치**: `feature/ai-data-freshness`
> **목표**: 중복 API 호출을 줄이고 데이터 품질(시간순, 중복 제거)을 높인다.
> **완료 기준**: 동일 세션 3회 연속 분석 시 뉴스 API 중복 호출 없음. 뉴스 최신순 정렬 확인.

### 구현 태스크

- [x] **3-1** `apiCache.js` 신규 — `getCached` / `setCached` / `invalidateCache` (TTL 맵) — `src/api/apiCache.js`
- [x] **3-2** 뉴스 fetch에 TTL 3분 캐시 적용 — `src/api/newsApi.js`
- [x] **3-3** 시세 fetch에 TTL 1분 캐시 적용 — `src/api/stockApi.js`
- [x] **3-4** DART 공시 fetch에 TTL 15분 캐시 적용, DART 38,000건 도달 시 차단 — `src/api/disclosureApi.js`
- [x] **3-5** 뉴스 병합 시 날짜 기준 정렬 + 제목 중복 제거 — `src/api/newsApi.js`
- [x] **3-6** `claudeApi.js` — `getMarketIndices()` 60초 캐시, alert case 지수 요약 추가, disclosureDays 동적 추출 — `src/api/claudeApi.js`

### 추가 수정 (2026-05-20) — Research 실제 데이터 패치 복구

- [x] **3-7** `sendResearchWithToolUse` (Tool Use XML 생성 방식) 완전 제거 — `src/api/claudeApi.js`
- [x] **3-8** research case 실제 프리패치 파이프라인으로 교체 — `src/api/claudeApi.js`
  - `searchByQuery(전체 메시지)` → 실패 시 단어 토큰 단위 폴백 검색
  - 히트 종목에 대해 `fetchQuote` + `fetchNews` + `fetchDisclosures` 병렬 실행
  - `buildResearchContext(bundle)` → `RESEARCH_PROMPT` 컨텍스트 주입
- [x] **3-9** `RESEARCH_TOOL_USE_PROMPT` export 삭제 — `src/agents/researchAgent.js`
- [x] **3-10** `AGENT_MAX_TOKENS.research` 3072 → 4096 상향 (응답 잘림 방지) — `src/api/claudeApi.js`

### 완료 검증

- [ ] 동일 세션 3회 연속 뉴스 요청 시 API 호출 1회 (캐시 히트 2회)
- [ ] 뉴스 목록이 최신순으로 정렬됨
- [ ] DART 38,000건 도달 시 이후 요청 차단 메시지 반환
- [x] "삼성전자 종합 분석해줘" 요청 시 실제 시세·뉴스·공시 데이터로 분석 응답 ← 확인 완료 (2026-05-20)
- [x] `<tool_call>` XML 텍스트 응답 없음 (Tool Use 방식 완전 제거) ← 확인 완료 (2026-05-20)

---

## Phase 4 — 응답 형식 구조화 (10.10.06) ← Phase 3과 병렬 진행 가능

> **브랜치**: `feature/ai-response-format`
> **목표**: 에이전트 응답에서 구조화 데이터를 추출해 UI 배지·칩으로 활용한다.
> **완료 기준**: research 응답 투자 매력도 배지 표시. JSON 블록 없는 응답 시 텍스트만 정상 표시(fallback).

### 구현 태스크

- [x] **4-1** `schemas.js` 신규 — 에이전트별 출력 스키마 정의 — `src/agents/schemas.js`
- [x] **4-2** `responseParser.js` 신규 — `extractStructuredData`, `stripJsonBlock` — `src/utils/responseParser.js`
- [x] **4-3** `researchAgent.js` — 프롬프트 끝에 JSON 블록 지시 추가 — `src/agents/researchAgent.js`
- [x] **4-4** `portfolioAgent.js` — JSON 블록 지시 추가 — `src/agents/portfolioAgent.js`
- [x] **4-5** `reportAgent.js` — JSON 블록 지시 추가 — `src/agents/reportAgent.js`
- [x] **4-6** `alertAgent.js` — JSON 블록 지시 추가 — `src/agents/alertAgent.js`
- [x] **4-7** `MessageBubble.jsx` — 투자 매력도 배지, alert 건수 칩 렌더링 — `src/components/chat/MessageBubble.jsx`

### 완료 검증

- [ ] research 응답에서 투자 매력도 배지(상/중/하) 표시
- [ ] JSON 블록 없는 응답 시 배지 없이 텍스트만 정상 표시
- [ ] `responseParser.js` 단위 테스트 통과 (있음/없음/파싱실패 케이스)

---

## Phase 5 — 라우팅 개선 (10.10.01)

> **브랜치**: `feature/ai-routing-improvement`
> **목표**: 점수 기반 라우팅과 Haiku 의도 분류를 결합해 오라우팅을 줄인다.
> **선행 조건**: Phase 2~3 완료 후 진행.
> **완료 기준**: "포트폴리오 현황 보여줘" → portfolio. "삼성전자 분석" → research.

### 선행 수정 (2026-05-20) — 연속 발화 라우팅 오류 수정

- [x] **5-0a** `CONTINUATION_RE` 정규식 정의 (이어서|계속|더 설명 등) — `src/components/chat/ChatPanel.jsx`
- [x] **5-0b** `resolveAgent()` 신규 — 연속 발화 감지 시 마지막 AI 메시지의 `agentType` 재사용 — `src/components/chat/ChatPanel.jsx`
- [x] **5-0c** `handleSend` — `forceAgent` 대신 `effectiveAgent = resolveAgent(msg)` 사용 — `src/components/chat/ChatPanel.jsx`

> **효과**: "이어서 설명해줘" 입력 시 `routeToAgent` 폴백(portfolio)으로 넘어가지 않고 직전 에이전트(research 등)를 유지.

### 구현 태스크

- [x] **5-1** `ROUTING_RULES` 점수 배열 정비 — 중립 키워드 제거, 가중치 조정 — `src/agents/orchestrator.js`
- [x] **5-2** `scoreRouting()` 리팩터링 — 점수 계산 로직 명시적 분리 — `src/agents/orchestrator.js`
- [x] **5-3** `classifyIntent()` 신규 — Haiku로 low-confidence 케이스 보조 분류 — `src/agents/orchestrator.js`
- [x] **5-4** 하이브리드 라우팅 진입점 연결 — score 임계값 미달 시 Haiku 호출 — `src/agents/orchestrator.js`
- [x] **5-5** 라우팅 결과 디버그 로깅 (개발 모드) — `src/agents/orchestrator.js`

### 추가 수정 (2026-05-21) — 이어쓰기 실제 컨텍스트 전달 + 토큰 축소

- [x] **5-6** `continuePreviousResponse()` export 신규 — `src/api/claudeApi.js`
  - `[lastUserMsg, lastAIMsg, '이어서 작성해주세요']` 3-턴 구성으로 멈춘 지점부터 이어쓰기
- [x] **5-7** `AIChat.jsx` `handleSend` 분기 교체 — `src/pages/AIChat.jsx`
  - 기존: `continuationAgent`로 에이전트만 유지, 내용은 새 질문 처리 (컨텍스트 없음)
  - 변경: `CONTINUATION_RE` 매칭 시 `continuePreviousResponse()` 호출 → 직전 대화 이력 전달
- [x] **5-8** 연속 이어쓰기 시 원본 질문 복원 — `src/pages/AIChat.jsx`
  - 2번째 이어서 요청 시 `lastUser`가 "이어서 설명해줘"로 잡히는 버그 수정
  - `CONTINUATION_RE.test(m.content)` 해당 안 되는 가장 최근 user 메시지를 원본으로 사용
- [x] **5-9** `AGENT_MAX_TOKENS` 전체 절반 축소 — `src/api/claudeApi.js`
  - journal/report/research: 4096 → 2048
  - analysis/portfolio/alert: 2048 → 1024
  - `sendWithHistory` fallback: 4096 → 2048

### 완료 검증

- [ ] "포트폴리오 현황 보여줘" → portfolio 라우팅 (기존 analysis 오라우팅 없음) ← 수동 확인 필요
- [ ] "삼성전자 분석" → research 라우팅 ← 수동 확인 필요
- [ ] 라우팅 디버그 로그에서 점수 분포 확인 가능 ← 수동 확인 필요
- [x] 첫 번째 "이어서 설명해줘" → 직전 응답 이어서 생성 ← 화면 확인 완료 (2026-05-21)
- [x] 연속 "이어서" 반복 시 원본 질문 컨텍스트 유지 ← 화면 확인 완료 (2026-05-21)

---

## Phase 6 — 에이전트 협력 (10.10.04)

> **브랜치**: `feature/ai-agent-pipeline`
> **목표**: 복합 질문에 대해 두 에이전트를 순차 실행하고 결과를 연결한다.
> **선행 조건**: Phase 5 완료 후 진행.
> **완료 기준**: "삼성전자 분석 + 포트폴리오 영향" → research → portfolio 2단계 응답.

### 구현 태스크

- [x] **6-1** `COMPOUND_PATTERNS` 정의 (3가지 패턴) — `src/agents/orchestrator.js`
- [x] **6-2** `detectCompoundIntent()` 구현 — `src/agents/orchestrator.js`
- [x] **6-3** `runAgentPipeline()` 구현 — 순차 에이전트 실행 + 컨텍스트 누적 — `src/api/claudeApi.js`
- [x] **6-4** `synthesizeResults()` 구현 — 다단계 결과 합성 — `src/api/claudeApi.js`
- [x] **6-5** `AIChat.jsx` — 복합/단순 의도 분기 연결 — `src/pages/AIChat.jsx`
- [x] **6-6** `portfolioAgent.js`, `researchAgent.js`, `claudeApi.js` — `previousAnalysis` 컨텍스트 수신 지원
- [x] **6-7** 파이프라인 실행 시 로딩 단계 표시 ("1/2 분석 중...") — `src/pages/AIChat.jsx`

### 추가 수정 (2026-05-22) — 수동 테스트 후 발견 버그 수정

- [x] **6-8** `AgentBadge`: `agentInfo` prop 폴백 추가 + `violet` 색상 매핑 — `src/components/chat/AgentBadge.jsx`
  - `agentType='research+portfolio'`는 `AGENT_LABELS`에 없어 배지가 렌더링되지 않던 문제 수정
- [x] **6-9** `MessageBubble`: `agentInfo` prop 전달 + `hr` 커스텀 렌더러 추가 — `src/components/chat/MessageBubble.jsx`
  - `---` 구분선이 스타일 없이 렌더링되어 보이지 않던 문제 수정
- [x] **6-10** `runAgentPipeline`: 각 단계의 `incomplete` 플래그 전파 — `src/api/claudeApi.js`
  - 파이프라인 중 `max_tokens` 발생 시 "이어서 설명해줘" 안내 문구가 표시되지 않던 문제 수정

### 완료 검증

- [x] "LG전자 분석해줘, 내 포트폴리오에 담아야 할지 영향도도 봐줘" → research → portfolio 2단계 응답 ← 확인 완료 (2026-05-22)
- [x] 단순 질문은 기존과 동일한 단일 에이전트 응답 (회귀 없음) ← 확인 완료 (2026-05-22)
- [x] 파이프라인 응답 시간 단순 호출 대비 2배 이내 ← 확인 완료 (2026-05-22)

---

## Phase 7 — 피드백 루프 + 환각 감지 (10.10.05 + 10.10.07)

> **브랜치**: `feature/ai-feedback-hallucination`
> **목표**: 응답 품질 데이터를 수집하고 환각 의심 패턴에 경고를 표시한다.
> **선행 조건**: Phase 1~6 완료 후 진행.
> **완료 기준**: 부정 키워드 입력 시 이전 응답에 플래그 기록. 허구 공시 패턴에 경고 배지.

### 구현 태스크

- [x] **7-1** 메시지 스키마 확장 — `feedback`, `qualityScore`, `negativeKeyword`, `followUpWithin60s` — `src/store/chatStore.js` (`addAIMessage` + `updateMessageFeedback` + `updateMessageSignals`)
- [x] **7-2** `feedbackDetector.js` 신규 — 부정 키워드 감지, 암묵적 신호 기록 — `src/utils/feedbackDetector.js`
- [x] **7-3** 메시지 전송 시 이전 응답 암묵적 신호 기록 — `src/pages/AIChat.jsx`
- [x] **7-4** `FeedbackButtons.jsx` 신규 — 👍/👎 평점 UI — `src/components/chat/FeedbackButtons.jsx`
- [x] **7-5** `MessageBubble.jsx` — FeedbackButtons 추가 — `src/components/chat/MessageBubble.jsx`
- [x] **7-6** `generateQualityReport()` — 에이전트별 품질 집계 (개발 모드) — `src/utils/feedbackDetector.js`
- [x] **7-7** `hallucinationGuard.js` 신규 — 환각 의심 패턴 감지 — `src/utils/hallucinationGuard.js`
- [x] **7-8** 환각 의심 경고 배지 추가 — `src/components/chat/MessageBubble.jsx`

### 완료 검증

- [ ] "틀렸어" 입력 시 이전 응답 `negativeKeyword: true` 기록 ← 수동 확인 필요
- [ ] 👍 클릭 시 해당 메시지 `feedback: 'helpful'` Zustand 저장 확인 ← 수동 확인 필요
- [ ] research 에이전트가 구체적 수치 포함 응답 시 🔍 경고 배지 표시 ← 수동 확인 필요
- [ ] journal 에이전트 응답에 환각 경고 배지 미표시 (제외 조건 확인) ← 수동 확인 필요

---

## 신규 생성 파일 체크리스트

| 파일 | Phase | 완료 |
|------|-------|------|
| `src/api/apiCache.js` | 3 | - [x] |
| `src/agents/schemas.js` | 4 | - [x] |
| `src/utils/responseParser.js` | 4 | - [x] |
| `src/utils/feedbackDetector.js` | 7 | - [x] |
| `src/utils/hallucinationGuard.js` | 7 | - [x] |
| `src/components/chat/FeedbackButtons.jsx` | 7 | - [x] |

---

## 위험요소 확인 체크리스트

- [x] Haiku 의도 분류 임계값 보수적으로 설정 (Phase 5) — score <70점 시에만 Haiku 호출, 실패 시 'portfolio' fallback
- [x] `previousAnalysis` 1,000자 제한 적용 (Phase 6) — `.slice(0, 1000)` 적용 확인
- [ ] `extractStructuredData` null fallback 동작 확인 (Phase 4)
- [ ] `quotesMap` holdings 요청 시점에만 fetch (항상 선제 호출 금지) (Phase 2)
- [x] 환각 감지 패턴 최소 3개 유지 + journal 에이전트 제외 (Phase 7) — 4개 패턴, GUARDED_AGENTS에서 journal/analysis 제외 확인
- [x] IndexedDB 새 필드 `?? 기본값` 하위 호환 처리 (Phase 7) — `feedback ?? null`, `qualityScore ?? 70` 적용 확인
