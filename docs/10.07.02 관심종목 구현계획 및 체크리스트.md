# 10.07 관심종목 개선 — 구현계획 및 체크리스트

> 설계 문서: [10.07.01 관심종목 개선 계획.md](./10.07.01%20관심종목%20개선%20계획.md)  
> 브랜치: `worktree-watchlist-improvement`  
> 시작일: 2026-05-15

---

## Phase A — 그룹(태그) 관리 + 목표가 트래킹

### A-1. watchlistStore.js 확장

- [x] `groups: []` 배열 필드 추가
- [x] `alertHistory: []` 배열 필드 추가
- [x] watchlist item에 `groupIds: []` 필드 추가
- [x] watchlist item에 `priceAtAdded`, `targetPrice`, `stopLoss`, `entryPrice` 필드 추가
- [x] alert에 `paused: boolean` 필드 추가
- [x] `addGroup(name, color)` 액션 구현 (최대 10개)
- [x] `renameGroup(id, name)` 액션 구현
- [x] `removeGroup(id)` 액션 구현 (종목 groupIds에서도 제거)
- [x] `updateWatchlistGroups(ticker, groupIds)` 액션 구현
- [x] `updateWatchlistTargets(ticker, { targetPrice, stopLoss, entryPrice })` 액션 구현
- [x] `reorderWatchlist(newOrder)` 액션 구현 (DnD용)
- [x] `toggleAlertPaused(alertId)` 액션 구현
- [x] `addAlertHistory(entry)` 액션 구현 (최대 100건 유지)
- [x] `markAlertHistoryRead(id)` / `markAllAlertHistoryRead()` 액션 구현
- [x] `clearAlertHistory()` 액션 구현
- [x] `checkAlerts()` — paused 알림 건너뜀 처리
- [x] `addToWatchlist()` — priceAtAdded 스냅샷 자동 포함
- [x] persist version 4 → 6 업그레이드, migrate 함수 작성
- [x] 검증: 브라우저 localStorage reload 후 데이터 유지 확인

---

### A-2. GroupManager.jsx 신규

- [x] 태그 목록 표시 (색상 칩 + 이름)
- [x] 태그 추가 — 이름 입력 + 색상 선택 (8가지 프리셋)
- [x] 태그 이름 인라인 수정
- [x] 태그 삭제 (연결된 종목 groupIds에서 자동 제거)
- [x] 10개 초과 시 추가 버튼 비활성화
- [x] 검증: 태그 추가·수정·삭제 후 종목 필터 정상 동작 확인

---

### A-3. 종목 태그 편집 UI (Watchlist.jsx 내)

- [x] 카드·테이블 뷰에 종목별 태그 칩 표시
- [x] 태그 칩 클릭 → 태그 부착·해제 팝오버
- [x] 태그 필터 탭 바 (전체 + 태그별) — 상단
- [x] 검증: 태그 필터 선택 시 해당 태그 종목만 표시 확인

---

### A-4. TargetPriceBar.jsx 신규

- [x] 목표가·손절가·매입가 입력 폼 (알림 설정과 별도)
- [x] 달성률 프로그레스 바: `(현재가 / 목표가) × 100`
- [x] 목표가 초과 시 100% + 초과 표시
- [x] 손절가 대비 안전(✅) / 경고(⚠️) 표시
- [x] 카드 뷰에서 접을 수 있는 섹션으로 구현
- [x] 검증: 목표가 입력 후 프로그레스 바 정상 렌더링 확인

---

## Phase B — 알림 고도화

### B-1. AlertHistoryDrawer.jsx 신규

- [x] `shadcn/ui Sheet` 사용 (우측 슬라이드 드로어)
- [x] 알림 이력 리스트 (firedAt 기준 최신순)
- [x] 읽음/안 읽음 시각 구분 (배경색)
- [x] 개별 읽음 처리 + 전체 읽음 버튼
- [x] 이력 비어있을 때 빈 상태 표시
- [x] 이력 전체 삭제 버튼
- [x] 검증: 알림 발동 → 드로어에 이력 추가 확인

---

### B-2. Watchlist.jsx — 벨 아이콘 + 읽지 않은 건수 배지

- [x] 헤더 우측에 벨 아이콘 추가
- [x] 읽지 않은 이력 건수 배지 (1 이상일 때)
- [x] 클릭 시 AlertHistoryDrawer 오픈
- [x] 알림 발동 시 `addAlertHistory()` 호출 연동
- [x] 검증: 알림 발동 후 벨 배지 숫자 증가 확인

---

### B-3. AlarmDialog.jsx 수정

- [x] 등록된 알림 항목에 일시정지 토글 스위치 추가
- [x] 일시정지 상태 알림 — 회색 텍스트 + `(일시정지)` 라벨
- [x] 검증: 토글 후 알림 체크에서 제외 확인

---

## Phase C — 성과 분석 + 편의기능

### C-1. 추가일 기준 수익률 배지

- [x] `priceAtAdded` 없는 종목은 배지 미표시
- [x] 수익률 계산: `(현재가 - priceAtAdded) / priceAtAdded × 100`
- [x] 카드 뷰 — 스파크라인 옆 `since ±X.X%` 배지
- [x] 테이블 뷰 — `추가후 변동` 컬럼 추가
- [x] 한국 컨벤션: 양수=빨강, 음수=파랑
- [ ] 검증: 추가일 대비 가격 변동률 정확성 확인

---

### C-2. 섹터 분포 미니 파이차트

- [x] 관심종목 상단 접을 수 있는 섹터 분포 패널
- [x] Recharts `PieChart` 사용 (기존 라이브러리 재사용)
- [x] 섹터별 종목 수 + 비중(%) 레이블
- [x] `sector` 필드 없는 종목은 "기타" 처리
- [ ] 검증: 섹터 분포 파이차트 정상 렌더링 확인

---

### C-3. 드래그앤드롭 재정렬

- [x] `@dnd-kit/core`, `@dnd-kit/sortable` 사용
- [x] 카드 뷰 DnD 구현
- [ ] 테이블 뷰 DnD 구현 (행 핸들러)
- [x] 정렬 기준(`sortBy`)이 `custom` 아닐 때 DnD 비활성화
- [x] `reorderWatchlist()` 호출로 store 반영
- [ ] 검증: 드래그 후 순서 reload 시에도 유지 확인

---

### C-4. CSV 내보내기

- [x] 헤더 영역에 내보내기 버튼 추가
- [x] 현재 필터 적용된 목록 기준 CSV 생성
- [x] 컬럼: 티커, 종목명, 시장, 현재가, 변동률, 추가일, 메모, 목표가, 손절가, 추가후수익률
- [x] 파일명: `watchlist_YYYYMMDD.csv`, BOM 포함 (Excel 한글 깨짐 방지)
- [ ] 검증: CSV 다운로드 후 Excel에서 한글 정상 표시 확인

---

## 최종 완료 조건

- [x] `npm run build` 빌드 오류 없음
- [ ] 관심종목 페이지 전체 기능 수동 테스트 통과
- [ ] localStorage 재로드 후 데이터(태그·목표가·알림이력·순서) 유지 확인
- [ ] 기존 기능(실시간 가격·알림·AI 브리핑·스파크라인) 회귀 없음

---

## 커밋 계획

```
feat: watchlistStore — groups·alertHistory·목표가 필드 추가, persist v6
feat: GroupManager — 태그 CRUD + 색상 선택 UI
feat: TargetPriceBar — 목표가·손절가 달성률 프로그레스 바
feat: AlertHistoryDrawer — 알림 발동 이력 Sheet 드로어
feat: AlarmDialog — 알림 일시정지 토글
feat: Watchlist — 태그 필터 탭·DnD 재정렬·섹터 차트·CSV·벨 배지
docs: Sprint 10.7 관심종목 개선 구현 문서
```
