# 15.01.03 AI 투자 가이드라인 고도화 체크리스트

## 작성일
2026-06-23

## 근거 문서
- [15.01.01 AI를 활용한 투자 가이드라인 고도화 방법 조사](./15.01.01%20AI를%20활용한%20투자%20가이드라인%20고도화%20방법%20조사.md)
- [15.01.02 AI 투자 가이드라인 고도화 구현계획](./15.01.02%20AI%20투자%20가이드라인%20고도화%20구현계획.md)

> 진행하며 `[ ]` → `[x]` 로 갱신한다. 수작업 확인 항목은 ✋ 표시. 보류·제외는 `[~]` 표시.

---

## 사전 결정 (Phase 1 착수 전)

- [x] ① 선행지표 **6종 확정** (SOX·VIX·US10Y·DXY·원달러 + **WTI**) — 2026-06-23
- [x] ② VIX 레짐 임계 확정 (33/67 백분위 + crisis: VIX≥35 또는 1일 +20%) — 기본안 확정 2026-06-23
- [x] ③ `BIAS_WEIGHTS` 초기 가중치 확정 (금리·달러·환율=2 / SOX·S&P=1.5 / 나스닥·VIX=1 / **WTI=0.5**) — 거시 자금 채널 최우선
- [x] ✋ `/api/yahoo` 프록시 6개 티커 허용 확인 — **화이트리스트 없음(passthrough)**, 수정 불요. dev=vite.config.js:402, prod=vercel.json:16. `^` 경로는 기존 `^GSPC`로 검증됨. (구현 시 `KRW=X`·`CL=F`의 `=` 인코딩만 라이브 1회 실측)

---

## Phase 1 — 데이터·계산 기반 (권장 1차)

### P2 — 변동성 레짐 & 변동성 지표 (선행 — TDD)

#### 단위 테스트 작성 (RED)
- [x] `computeAtr` 테스트 (정상 / 데이터 부족 / atrPercent)
- [x] `computeBollinger` 테스트 (upper·lower·bandwidth·percentB / 데이터 부족)
- [x] `computeVixRegime` 테스트 (low·normal·high 경계 33/67 / crisis 절대·급변 / 히스토리 부족)
- [x] `computeOvernightBias` 테스트 (강세·약세·중립=0 / invert 부호 / contributions)

#### 구현 (GREEN) — `src/api/regimeApi.js`
- [x] 파일 첫 줄 한국어 역할 주석
- [x] `computeAtr(history, period=14)` — Wilder 평활 True Range
- [x] `computeBollinger(closes, period=20, mult=2)`
- [x] `computeVixRegime(vixHistory, currentVix)` — 252일 trailing 33/67 백분위 + crisis
- [x] `BIAS_WEIGHTS` 명명 상수 + `computeOvernightBias(snapshot)` — 5단계 라벨
- [x] look-ahead 회피 확인 (입력은 현재 시점까지 데이터만, 순수 함수)
- [x] 단위 테스트 전체 통과 (vitest — regimeApi 18건)

### P1 — 선행지표 시세 수집

#### src/api/claudeApi.js
- [x] `LEADING_INDICATORS` 상수 추가 (6종 — SOX·VIX·US10Y·DXY·원달러·WTI, `key`·`label`·`ticker`·`market`·`invert`)
- [x] `getLeadingIndicators()` — 병렬 `fetchQuote`, 실패 `null` 스킵, 60초 캐시(`indicators:leading`) + `snapshot` 반환

#### src/api/stockApi.js
- [x] 선행지표 티커 `fetchQuote` 정상 반환 점검 — 미국 지수 경로(`toYahooTicker` 그대로 통과)로 분기 불요 확인
- [x] 프록시 화이트리스트 추가 불요 — passthrough 구조(vite.config.js:402 / vercel.json:16) 확인

#### P1·P2 검증
- [x] ✋ 6개 선행지표 시세 + 등락률 조회 확인 (서버 배포본, 2026-06-23) — 전종목 정상, `KRW=X`·`CL=F`의 `=` 인코딩 무문제. `chartPreviousClose` fallback으로 등락률 산출 확인 (SOX +3.59% / VIX −3.08% / 美10Y −0.23% / DXY +0.06% / 원달러 +0.27% / WTI −0.58%)
- [x] ✋ 부분 실패 내성 — `catch(()=>null)` 코드 구현 확인 (라이브 6종 전부 성공으로 실패 케이스 미발생, 자연 발생 시 재확인)

### 완료 처리 (Phase 1)
- [x] `npx vitest run` — 신규 regimeApi 18건 포함 108건 통과 (htsParser 4건은 사전 존재 실패, 무관)
- [x] `npm run build` 성공 (12.68s)
- [x] 커밋 분리 (docs / regimeApi+test / 선행지표 수집) — ffdc8fb7 / 5e9f5a9d / c22a0516
- [x] master 머지·푸시 (fast-forward, origin/master c22a0516) — 2026-06-23

---

## ✅ Phase 1 완료 (2026-06-23)

선행지표 6종 수집 + 변동성 레짐·ATR·볼린저·overnightBias 계산 모듈을 서버 배포본에서 라이브 검증 완료.
실측 데이터로 overnightBias 동작 확인(SOX·VIX 우호를 달러·환율 악재가 상쇄 → 중립). 다음은 Phase 2(브리핑 컨텍스트 결합 + 시장 국면 프롬프트 + UI 카드).

---

## Phase 2 — AI 종합 & UI

### 사전 결정 (Phase 2 착수 전)
- [x] ④ 에이전트 방식 확정 → **안 A** (기존 analysis 분기 강화) — 2026-06-23
- [x] ⑤ 확장적 사고·web_search 적용 범위 확정 → **기존 14.04 자산 그대로 유지** (thinking adaptive + webSearchEnabled 종속)

### P3 — 브리핑 컨텍스트 확장

#### src/agents/analysisAgent.js
- [x] `buildMarketBriefContext` 시그니처 확장 `{ indices, leading, bias, regime, news }` (하위호환)
- [x] `[미국 선행지표]`·`[종합 방향 가설 overnightBias]`·`[변동성 레짐]` 블록 렌더
- [x] 컨텍스트 직렬화 단위 테스트 5건 (하위호환 + 신규 인자, marketBriefContext.test.js)

#### src/api/claudeApi.js (시장 브리핑 경로)
- [x] `getMarketIndices()` + `getLeadingIndicators()` 병렬 조회
- [x] `computeOvernightBias()` 계산 결합 (지수 S&P·NASDAQ 등락도 반영)
- [x] `^VIX` 히스토리 → `computeVixRegime()` (`getVixRegime`, 6시간 캐시, 실패 시 null)
- [x] 확장 인자를 `buildMarketBriefContext`에 전달

### P4 — 시장 국면 프롬프트 + 라우팅

#### 프롬프트 (안 A 기준)
- [x] `MARKET_BRIEF_PROMPT` 레짐 인식 강화 (방향 5단계 × 변동성 레짐 4단계 출력)
- [x] 핵심 동인 3 / 레짐별 검토 포인트 / 확률 시나리오 / 면책 포함
- [x] 확률·조건부 표현 강제, 매수/매도 권유 금지 명시
- [~] 안 B(신설) 미선택 — 안 A로 진행

#### src/agents/orchestrator.js
- [x] analysis 라우팅 키워드 보강 ("장 전망"·"증시 전망"·"시장 국면"·"선행지표"·"변동성"·"오늘 장")
- [~] 라우팅 단위 테스트 생략 — 기존 orchestratorRouting 테스트 회귀 없음으로 갈음

#### (선택) 확장적 사고
- [x] analysis-brief는 14.04에서 적용된 `AGENT_THINKING` adaptive 그대로 유지 (추가 작업 없음)

### P4(UI) — "오늘의 시장 국면" 카드

#### src/components/research/MarketBriefCard.jsx
- [x] 방향 배지(5단계) × 변동성 레짐 배지(4단계) 추가
- [x] 선행지표 미니 라인 (조회 성공 지표 등락)
- [x] 한국 색상 컨벤션 (상승=빨강/하락=파랑), 레짐 위험도 색 (정상=중립/고변동=주황/위기=빨강)
- [x] 데이터 없음·조회 실패 시 기존 텍스트 브리핑만 표시 (graceful degradation) + 객체 캐시 하위호환

#### P3·P4 검증
- [x] ✋ "오늘 시장 어때?" → 선행지표 반영 국면 분석 (서버 배포본, 2026-06-26) — 방향 약세 × 고변동(VIX 72 백분위)
- [x] ✋ 응답에 방향×변동성 레짐 + 확률 시나리오(상승/횡보/하락) + 면책 포함
- [x] ✋ 시장 국면 카드(Research)에 배지·선행지표 미니 라인 표시 — 6종 정상
- [x] ✋ 매수/매도 권유 문구 없음 ("매수·매도 권유가 아닙니다" 면책 확인)
- [x] ✋ 선행지표 조회 실패 폴백 — `catch` 코드 구현 (라이브 6종 성공으로 미발생)

#### 후속 수정 (라이브 검증 중 발견)
- [x] fix: 보유종목 있어도 시장 의도 질의는 시장 국면(경로3) 우선 라우팅 (d297b80c)
- [x] UI: 선행지표 미니 라인을 `종가 (등락율%)` 형식으로 표시

### 완료 처리 (Phase 2)
- [x] `npx vitest run` — marketBriefContext 5건 포함 113건 통과 (htsParser 4건 사전 실패, 무관)
- [x] `npm run build` 성공
- [x] 커밋 분리 (컨텍스트·프롬프트 65f5ff59 / claudeApi 결합 b19eeb8d / 라우팅·UI b8cfe8c6)
- [x] master 머지·푸시 (fast-forward, origin/master 3e5f7126 → d297b80c) — 2026-06-23~26
- [x] ✋ 배포 후 실제 시장 국면 분석·카드 라이브 확인 (2026-06-26)

---

## ✅ Phase 2 완료 (2026-06-26)

시장 브리핑에 미국 선행지표·overnightBias·VIX 변동성 레짐을 결합하고, 채팅·Research 카드 양쪽에서 라이브 검증 완료.
방향 5단계 × 변동성 레짐 4단계 + 확률 시나리오 + 검토 포인트가 정상 출력되며, 매수/매도 권유 없이 면책 포함.
다음은 Phase 3(사후 적중률 트래킹).

---

## Phase 3 — 사후 검증 트래킹 (방향만, 추후 상세화)

### 사전 결정
- [ ] ⑥ "실제 방향" 판정 기준 확정 (전일 종가 대비 당일 종가)

### 구현 방향
- [ ] `regimeForecastLog` 스토어/IDB 테이블 (`date`·`overnightBias`·`regime`·`predictedDirection`)
- [ ] 익일 실제 KOSPI 등락과 대조 → `actualDirection`·`hit` 갱신
- [ ] 적중률(`hitRate`) 집계 → "AI 신뢰도" 배지
- [ ] `BIAS_WEIGHTS` 사후 보정 근거로 활용
- [~] (선택) 변동성 레짐 추이 미니 차트 (Dashboard/Reports)

---

## 수작업 검증 안내 (✋ 항목 경로)

| 검증 | 앱 내 경로 |
|------|-----------|
| 선행지표 시세 조회 | 브라우저 개발자도구 → Network 탭 → `/api/yahoo` 응답 확인 |
| 시장 국면 분석 응답 | 좌측 사이드바 → **AI 코치**(채팅) → "오늘 시장 어때?" / "장 전망" 입력 |
| 시장 국면 카드 | **종목 탐색(Research)** 페이지 → 시장 브리핑 카드 영역 |
| 웹 검색·thinking 토글 | **설정(Settings)** → AI 및 API 키 섹션 |
| 권유 문구 없음 | AI 응답 본문에 "사세요/파세요" 류 없음, 면책 문구 존재 |

> Phase 1 완료 후 데이터·계산이 안정되면 Phase 2(AI·UI)로 진행한다. 각 Phase 완료 시 ✋ 항목은 사용자 라이브 확인 후 사인.
